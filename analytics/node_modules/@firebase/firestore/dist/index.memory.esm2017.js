import t from "@firebase/app";

import { Logger as n, LogLevel as e } from "@firebase/logger";

import { isMobileCordova as s, isReactNative as i, isElectron as r, isIE as o, isUWP as u, isBrowserExtension as c } from "@firebase/util";

import { XhrIo as a, EventType as h, ErrorCode as l, createWebChannelTransport as _, WebChannel as f } from "@firebase/webchannel-wrapper";

import { Component as d } from "@firebase/component";

/**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */ class w {
    /**
     * Constructs a DatabaseInfo using the provided host, databaseId and
     * persistenceKey.
     *
     * @param databaseId The database to use.
     * @param persistenceKey A unique identifier for this Firestore's local
     * storage (used in conjunction with the databaseId).
     * @param host The Firestore backend host to connect to.
     * @param ssl Whether to use SSL when connecting.
     * @param forceLongPolling Whether to use the forceLongPolling option
     * when using WebChannel as the network transport.
     * @param autoDetectLongPolling Whether to use the detectBufferingProxy
     * option when using WebChannel as the network transport.
     */
    constructor(t, n, e, s, i, r) {
        this.t = t, this.persistenceKey = n, this.host = e, this.ssl = s, this.forceLongPolling = i, 
        this.i = r;
    }
}

/** The default database name for a project. */
/** Represents the database ID a Firestore client is associated with. */
class E {
    constructor(t, n) {
        this.projectId = t, this.database = n || "(default)";
    }
    get o() {
        return "(default)" === this.database;
    }
    isEqual(t) {
        return t instanceof E && t.projectId === this.projectId && t.database === this.database;
    }
}

/**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
const T = new n("@firebase/firestore");

// Helper methods are needed because variables can't be exported as read/write
function m() {
    return T.logLevel;
}

/**
 * Sets the verbosity of Cloud Firestore logs (debug, error, or silent).
 *
 * @param logLevel
 *   The verbosity you set for activity and error logging. Can be any of
 *   the following values:
 *
 *   <ul>
 *     <li>`debug` for the most verbose logging level, primarily for
 *     debugging.</li>
 *     <li>`error` to log errors only.</li>
 *     <li><code>`silent` to turn off logging.</li>
 *   </ul>
 */ function I(t, ...n) {
    if (T.logLevel <= e.DEBUG) {
        const e = n.map(P);
        T.debug("Firestore (8.0.2): " + t, ...e);
    }
}

function A(t, ...n) {
    if (T.logLevel <= e.ERROR) {
        const e = n.map(P);
        T.error("Firestore (8.0.2): " + t, ...e);
    }
}

function R(t, ...n) {
    if (T.logLevel <= e.WARN) {
        const e = n.map(P);
        T.warn("Firestore (8.0.2): " + t, ...e);
    }
}

/**
 * Converts an additional log parameter to a string representation.
 */ function P(t) {
    if ("string" == typeof t) return t;
    try {
        return n = t, JSON.stringify(n);
    } catch (n) {
        // Converting to JSON failed, just log the object directly
        return t;
    }
    /**
 * @license
 * Copyright 2020 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
    /** Formats an object as a JSON string, suitable for logging. */
    var n;
}

/**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/**
 * Unconditionally fails, throwing an Error with the given message.
 * Messages are stripped in production builds.
 *
 * Returns `never` and can be used in expressions:
 * @example
 * let futureVar = fail('not implemented yet');
 */ function V(t = "Unexpected state") {
    // Log the failure in addition to throw an exception, just in case the
    // exception is swallowed.
    const n = "FIRESTORE (8.0.2) INTERNAL ASSERTION FAILED: " + t;
    // NOTE: We don't use FirestoreError here because these are internal failures
    // that cannot be handled by the user. (Also it would create a circular
    // dependency between the error and assert modules which doesn't work.)
    throw A(n), new Error(n);
}

/**
 * Fails if the given assertion condition is false, throwing an Error with the
 * given message if it did.
 *
 * Messages are stripped in production builds.
 */ function y(t, n) {
    t || V();
}

/**
 * Casts `obj` to `T`. In non-production builds, verifies that `obj` is an
 * instance of `T` before casting.
 */ function g(t, 
// eslint-disable-next-line @typescript-eslint/no-explicit-any
n) {
    return t;
}

/**
 * @license
 * Copyright 2020 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/**
 * Generates `nBytes` of random bytes.
 *
 * If `nBytes < 0` , an error will be thrown.
 */ function p(t) {
    // Polyfills for IE and WebWorker by using `self` and `msCrypto` when `crypto` is not available.
    const n = 
    // eslint-disable-next-line @typescript-eslint/no-explicit-any
    "undefined" != typeof self && (self.crypto || self.msCrypto), e = new Uint8Array(t);
    if (n && "function" == typeof n.getRandomValues) n.getRandomValues(e); else 
    // Falls back to Math.random
    for (let n = 0; n < t; n++) e[n] = Math.floor(256 * Math.random());
    return e;
}

/**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */ class v {
    static u() {
        // Alphanumeric characters
        const t = "ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789", n = Math.floor(256 / t.length) * t.length;
        // The largest byte value that is a multiple of `char.length`.
                let e = "";
        for (;e.length < 20; ) {
            const s = p(40);
            for (let i = 0; i < s.length; ++i) 
            // Only accept values that are [0, maxMultiple), this ensures they can
            // be evenly mapped to indices of `chars` via a modulo operation.
            e.length < 20 && s[i] < n && (e += t.charAt(s[i] % t.length));
        }
        return e;
    }
}

function b(t, n) {
    return t < n ? -1 : t > n ? 1 : 0;
}

/** Helper to compare arrays using isEqual(). */ function C(t, n, e) {
    return t.length === n.length && t.every(((t, s) => e(t, n[s])));
}

/**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */ function S(t) {
    let n = 0;
    for (const e in t) Object.prototype.hasOwnProperty.call(t, e) && n++;
    return n;
}

function D(t, n) {
    for (const e in t) Object.prototype.hasOwnProperty.call(t, e) && n(e, t[e]);
}

function N(t) {
    for (const n in t) if (Object.prototype.hasOwnProperty.call(t, n)) return !1;
    return !0;
}

/**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/**
 * A map implementation that uses objects as keys. Objects must have an
 * associated equals function and must be immutable. Entries in the map are
 * stored together with the key being produced from the mapKeyFn. This map
 * automatically handles collisions of keys.
 */ class F {
    constructor(t, n) {
        this.h = t, this.l = n, 
        /**
         * The inner map for a key -> value pair. Due to the possibility of
         * collisions we keep a list of entries that we do a linear search through
         * to find an actual match. Note that collisions should be rare, so we still
         * expect near constant time lookups in practice.
         */
        this._ = {};
    }
    /** Get a value for this key, or undefined if it does not exist. */    get(t) {
        const n = this.h(t), e = this._[n];
        if (void 0 !== e) for (const [n, s] of e) if (this.l(n, t)) return s;
    }
    has(t) {
        return void 0 !== this.get(t);
    }
    /** Put this key and value in the map. */    set(t, n) {
        const e = this.h(t), s = this._[e];
        if (void 0 !== s) {
            for (let e = 0; e < s.length; e++) if (this.l(s[e][0], t)) return void (s[e] = [ t, n ]);
            s.push([ t, n ]);
        } else this._[e] = [ [ t, n ] ];
    }
    /**
     * Remove this key from the map. Returns a boolean if anything was deleted.
     */    delete(t) {
        const n = this.h(t), e = this._[n];
        if (void 0 === e) return !1;
        for (let s = 0; s < e.length; s++) if (this.l(e[s][0], t)) return 1 === e.length ? delete this._[n] : e.splice(s, 1), 
        !0;
        return !1;
    }
    forEach(t) {
        D(this._, ((n, e) => {
            for (const [n, s] of e) t(n, s);
        }));
    }
    T() {
        return N(this._);
    }
}

/**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */ const x = {
    // Causes are copied from:
    // https://github.com/grpc/grpc/blob/bceec94ea4fc5f0085d81235d8e1c06798dc341a/include/grpc%2B%2B/impl/codegen/status_code_enum.h
    /** Not an error; returned on success. */
    OK: "ok",
    /** The operation was cancelled (typically by the caller). */
    CANCELLED: "cancelled",
    /** Unknown error or an error from a different error domain. */
    UNKNOWN: "unknown",
    /**
     * Client specified an invalid argument. Note that this differs from
     * FAILED_PRECONDITION. INVALID_ARGUMENT indicates arguments that are
     * problematic regardless of the state of the system (e.g., a malformed file
     * name).
     */
    INVALID_ARGUMENT: "invalid-argument",
    /**
     * Deadline expired before operation could complete. For operations that
     * change the state of the system, this error may be returned even if the
     * operation has completed successfully. For example, a successful response
     * from a server could have been delayed long enough for the deadline to
     * expire.
     */
    DEADLINE_EXCEEDED: "deadline-exceeded",
    /** Some requested entity (e.g., file or directory) was not found. */
    NOT_FOUND: "not-found",
    /**
     * Some entity that we attempted to create (e.g., file or directory) already
     * exists.
     */
    ALREADY_EXISTS: "already-exists",
    /**
     * The caller does not have permission to execute the specified operation.
     * PERMISSION_DENIED must not be used for rejections caused by exhausting
     * some resource (use RESOURCE_EXHAUSTED instead for those errors).
     * PERMISSION_DENIED must not be used if the caller can not be identified
     * (use UNAUTHENTICATED instead for those errors).
     */
    PERMISSION_DENIED: "permission-denied",
    /**
     * The request does not have valid authentication credentials for the
     * operation.
     */
    UNAUTHENTICATED: "unauthenticated",
    /**
     * Some resource has been exhausted, perhaps a per-user quota, or perhaps the
     * entire file system is out of space.
     */
    RESOURCE_EXHAUSTED: "resource-exhausted",
    /**
     * Operation was rejected because the system is not in a state required for
     * the operation's execution. For example, directory to be deleted may be
     * non-empty, an rmdir operation is applied to a non-directory, etc.
     *
     * A litmus test that may help a service implementor in deciding
     * between FAILED_PRECONDITION, ABORTED, and UNAVAILABLE:
     *  (a) Use UNAVAILABLE if the client can retry just the failing call.
     *  (b) Use ABORTED if the client should retry at a higher-level
     *      (e.g., restarting a read-modify-write sequence).
     *  (c) Use FAILED_PRECONDITION if the client should not retry until
     *      the system state has been explicitly fixed. E.g., if an "rmdir"
     *      fails because the directory is non-empty, FAILED_PRECONDITION
     *      should be returned since the client should not retry unless
     *      they have first fixed up the directory by deleting files from it.
     *  (d) Use FAILED_PRECONDITION if the client performs conditional
     *      REST Get/Update/Delete on a resource and the resource on the
     *      server does not match the condition. E.g., conflicting
     *      read-modify-write on the same resource.
     */
    FAILED_PRECONDITION: "failed-precondition",
    /**
     * The operation was aborted, typically due to a concurrency issue like
     * sequencer check failures, transaction aborts, etc.
     *
     * See litmus test above for deciding between FAILED_PRECONDITION, ABORTED,
     * and UNAVAILABLE.
     */
    ABORTED: "aborted",
    /**
     * Operation was attempted past the valid range. E.g., seeking or reading
     * past end of file.
     *
     * Unlike INVALID_ARGUMENT, this error indicates a problem that may be fixed
     * if the system state changes. For example, a 32-bit file system will
     * generate INVALID_ARGUMENT if asked to read at an offset that is not in the
     * range [0,2^32-1], but it will generate OUT_OF_RANGE if asked to read from
     * an offset past the current file size.
     *
     * There is a fair bit of overlap between FAILED_PRECONDITION and
     * OUT_OF_RANGE. We recommend using OUT_OF_RANGE (the more specific error)
     * when it applies so that callers who are iterating through a space can
     * easily look for an OUT_OF_RANGE error to detect when they are done.
     */
    OUT_OF_RANGE: "out-of-range",
    /** Operation is not implemented or not supported/enabled in this service. */
    UNIMPLEMENTED: "unimplemented",
    /**
     * Internal errors. Means some invariants expected by underlying System has
     * been broken. If you see one of these errors, Something is very broken.
     */
    INTERNAL: "internal",
    /**
     * The service is currently unavailable. This is a most likely a transient
     * condition and may be corrected by retrying with a backoff.
     *
     * See litmus test above for deciding between FAILED_PRECONDITION, ABORTED,
     * and UNAVAILABLE.
     */
    UNAVAILABLE: "unavailable",
    /** Unrecoverable data loss or corruption. */
    DATA_LOSS: "data-loss"
};

/** An error returned by a Firestore operation. */ class O extends Error {
    constructor(t, n) {
        super(n), this.code = t, this.message = n, this.name = "FirebaseError", 
        // HACK: We write a toString property directly because Error is not a real
        // class and so inheritance does not work correctly. We could alternatively
        // do the same "back-door inheritance" trick that FirebaseError does.
        this.toString = () => `${this.name}: [code=${this.code}]: ${this.message}`;
    }
}

/**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
// The earlist date supported by Firestore timestamps (0001-01-01T00:00:00Z).
/**
 * A `Timestamp` represents a point in time independent of any time zone or
 * calendar, represented as seconds and fractions of seconds at nanosecond
 * resolution in UTC Epoch time.
 *
 * It is encoded using the Proleptic Gregorian Calendar which extends the
 * Gregorian calendar backwards to year one. It is encoded assuming all minutes
 * are 60 seconds long, i.e. leap seconds are "smeared" so that no leap second
 * table is needed for interpretation. Range is from 0001-01-01T00:00:00Z to
 * 9999-12-31T23:59:59.999999999Z.
 *
 * @see https://github.com/google/protobuf/blob/master/src/google/protobuf/timestamp.proto
 */
class k {
    /**
     * Creates a new timestamp.
     *
     * @param seconds The number of seconds of UTC time since Unix epoch
     *     1970-01-01T00:00:00Z. Must be from 0001-01-01T00:00:00Z to
     *     9999-12-31T23:59:59Z inclusive.
     * @param nanoseconds The non-negative fractions of a second at nanosecond
     *     resolution. Negative second values with fractions must still have
     *     non-negative nanoseconds values that count forward in time. Must be
     *     from 0 to 999,999,999 inclusive.
     */
    constructor(t, n) {
        if (this.seconds = t, this.nanoseconds = n, n < 0) throw new O(x.INVALID_ARGUMENT, "Timestamp nanoseconds out of range: " + n);
        if (n >= 1e9) throw new O(x.INVALID_ARGUMENT, "Timestamp nanoseconds out of range: " + n);
        if (t < -62135596800) throw new O(x.INVALID_ARGUMENT, "Timestamp seconds out of range: " + t);
        // This will break in the year 10,000.
                if (t >= 253402300800) throw new O(x.INVALID_ARGUMENT, "Timestamp seconds out of range: " + t);
    }
    /**
     * Creates a new timestamp with the current date, with millisecond precision.
     *
     * @return a new timestamp representing the current date.
     */    static now() {
        return k.fromMillis(Date.now());
    }
    /**
     * Creates a new timestamp from the given date.
     *
     * @param date The date to initialize the `Timestamp` from.
     * @return A new `Timestamp` representing the same point in time as the given
     *     date.
     */    static fromDate(t) {
        return k.fromMillis(t.getTime());
    }
    /**
     * Creates a new timestamp from the given number of milliseconds.
     *
     * @param milliseconds Number of milliseconds since Unix epoch
     *     1970-01-01T00:00:00Z.
     * @return A new `Timestamp` representing the same point in time as the given
     *     number of milliseconds.
     */    static fromMillis(t) {
        const n = Math.floor(t / 1e3);
        return new k(n, 1e6 * (t - 1e3 * n));
    }
    /**
     * Converts a `Timestamp` to a JavaScript `Date` object. This conversion causes
     * a loss of precision since `Date` objects only support millisecond precision.
     *
     * @return JavaScript `Date` object representing the same point in time as
     *     this `Timestamp`, with millisecond precision.
     */    toDate() {
        return new Date(this.toMillis());
    }
    /**
     * Converts a `Timestamp` to a numeric timestamp (in milliseconds since
     * epoch). This operation causes a loss of precision.
     *
     * @return The point in time corresponding to this timestamp, represented as
     *     the number of milliseconds since Unix epoch 1970-01-01T00:00:00Z.
     */    toMillis() {
        return 1e3 * this.seconds + this.nanoseconds / 1e6;
    }
    m(t) {
        return this.seconds === t.seconds ? b(this.nanoseconds, t.nanoseconds) : b(this.seconds, t.seconds);
    }
    /**
     * Returns true if this `Timestamp` is equal to the provided one.
     *
     * @param other The `Timestamp` to compare against.
     * @return true if this `Timestamp` is equal to the provided one.
     */    isEqual(t) {
        return t.seconds === this.seconds && t.nanoseconds === this.nanoseconds;
    }
    toString() {
        return "Timestamp(seconds=" + this.seconds + ", nanoseconds=" + this.nanoseconds + ")";
    }
    toJSON() {
        return {
            seconds: this.seconds,
            nanoseconds: this.nanoseconds
        };
    }
    /**
     * Converts this object to a primitive string, which allows Timestamp objects to be compared
     * using the `>`, `<=`, `>=` and `>` operators.
     */    valueOf() {
        // This method returns a string of the form <seconds>.<nanoseconds> where <seconds> is
        // translated to have a non-negative value and both <seconds> and <nanoseconds> are left-padded
        // with zeroes to be a consistent length. Strings with this format then have a lexiographical
        // ordering that matches the expected ordering. The <seconds> translation is done to avoid
        // having a leading negative sign (i.e. a leading '-' character) in its string representation,
        // which would affect its lexiographical ordering.
        const t = this.seconds - -62135596800;
        // Note: Up to 12 decimal digits are required to represent all valid 'seconds' values.
                return String(t).padStart(12, "0") + "." + String(this.nanoseconds).padStart(9, "0");
    }
}

/**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/**
 * A version of a document in Firestore. This corresponds to the version
 * timestamp, such as update_time or read_time.
 */ class L {
    constructor(t) {
        this.timestamp = t;
    }
    static I(t) {
        return new L(t);
    }
    static min() {
        return new L(new k(0, 0));
    }
    A(t) {
        return this.timestamp.m(t.timestamp);
    }
    isEqual(t) {
        return this.timestamp.isEqual(t.timestamp);
    }
    /** Returns a number representation of the version for use in spec tests. */    R() {
        // Convert to microseconds.
        return 1e6 * this.timestamp.seconds + this.timestamp.nanoseconds / 1e3;
    }
    toString() {
        return "SnapshotVersion(" + this.timestamp.toString() + ")";
    }
    P() {
        return this.timestamp;
    }
}

/**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/**
 * Path represents an ordered sequence of string segments.
 */
class $ {
    constructor(t, n, e) {
        void 0 === n ? n = 0 : n > t.length && V(), void 0 === e ? e = t.length - n : e > t.length - n && V(), 
        this.segments = t, this.offset = n, this.V = e;
    }
    get length() {
        return this.V;
    }
    isEqual(t) {
        return 0 === $.g(this, t);
    }
    child(t) {
        const n = this.segments.slice(this.offset, this.limit());
        return t instanceof $ ? t.forEach((t => {
            n.push(t);
        })) : n.push(t), this.p(n);
    }
    /** The index of one past the last segment of the path. */    limit() {
        return this.offset + this.length;
    }
    v(t) {
        return t = void 0 === t ? 1 : t, this.p(this.segments, this.offset + t, this.length - t);
    }
    C() {
        return this.p(this.segments, this.offset, this.length - 1);
    }
    S() {
        return this.segments[this.offset];
    }
    D() {
        return this.get(this.length - 1);
    }
    get(t) {
        return this.segments[this.offset + t];
    }
    T() {
        return 0 === this.length;
    }
    N(t) {
        if (t.length < this.length) return !1;
        for (let n = 0; n < this.length; n++) if (this.get(n) !== t.get(n)) return !1;
        return !0;
    }
    F(t) {
        if (this.length + 1 !== t.length) return !1;
        for (let n = 0; n < this.length; n++) if (this.get(n) !== t.get(n)) return !1;
        return !0;
    }
    forEach(t) {
        for (let n = this.offset, e = this.limit(); n < e; n++) t(this.segments[n]);
    }
    O() {
        return this.segments.slice(this.offset, this.limit());
    }
    static g(t, n) {
        const e = Math.min(t.length, n.length);
        for (let s = 0; s < e; s++) {
            const e = t.get(s), i = n.get(s);
            if (e < i) return -1;
            if (e > i) return 1;
        }
        return t.length < n.length ? -1 : t.length > n.length ? 1 : 0;
    }
}

/**
 * A slash-separated path for navigating resources (documents and collections)
 * within Firestore.
 */ class M extends $ {
    p(t, n, e) {
        return new M(t, n, e);
    }
    k() {
        // NOTE: The client is ignorant of any path segments containing escape
        // sequences (e.g. __id123__) and just passes them through raw (they exist
        // for legacy reasons and should not be used frequently).
        return this.O().join("/");
    }
    toString() {
        return this.k();
    }
    /**
     * Creates a resource path from the given slash-delimited string. If multiple
     * arguments are provided, all components are combined. Leading and trailing
     * slashes from all components are ignored.
     */    static L(...t) {
        // NOTE: The client is ignorant of any path segments containing escape
        // sequences (e.g. __id123__) and just passes them through raw (they exist
        // for legacy reasons and should not be used frequently).
        const n = [];
        for (const e of t) {
            if (e.indexOf("//") >= 0) throw new O(x.INVALID_ARGUMENT, `Invalid segment (${e}). Paths must not contain // in them.`);
            // Strip leading and traling slashed.
                        n.push(...e.split("/").filter((t => t.length > 0)));
        }
        return new M(n);
    }
    static $() {
        return new M([]);
    }
}

const U = /^[_a-zA-Z][_a-zA-Z0-9]*$/;

/** A dot-separated path for navigating sub-objects within a document. */ class q extends $ {
    p(t, n, e) {
        return new q(t, n, e);
    }
    /**
     * Returns true if the string could be used as a segment in a field path
     * without escaping.
     */    static M(t) {
        return U.test(t);
    }
    k() {
        return this.O().map((t => (t = t.replace("\\", "\\\\").replace("`", "\\`"), q.M(t) || (t = "`" + t + "`"), 
        t))).join(".");
    }
    toString() {
        return this.k();
    }
    /**
     * Returns true if this field references the key of a document.
     */    U() {
        return 1 === this.length && "__name__" === this.get(0);
    }
    /**
     * The field designating the key of a document.
     */    static q() {
        return new q([ "__name__" ]);
    }
    /**
     * Parses a field string from the given server-formatted string.
     *
     * - Splitting the empty string is not allowed (for now at least).
     * - Empty segments within the string (e.g. if there are two consecutive
     *   separators) are not allowed.
     *
     * TODO(b/37244157): we should make this more strict. Right now, it allows
     * non-identifier path components, even if they aren't escaped.
     */    static B(t) {
        const n = [];
        let e = "", s = 0;
        const i = () => {
            if (0 === e.length) throw new O(x.INVALID_ARGUMENT, `Invalid field path (${t}). Paths must not be empty, begin with '.', end with '.', or contain '..'`);
            n.push(e), e = "";
        };
        let r = !1;
        for (;s < t.length; ) {
            const n = t[s];
            if ("\\" === n) {
                if (s + 1 === t.length) throw new O(x.INVALID_ARGUMENT, "Path has trailing escape character: " + t);
                const n = t[s + 1];
                if ("\\" !== n && "." !== n && "`" !== n) throw new O(x.INVALID_ARGUMENT, "Path has invalid escape sequence: " + t);
                e += n, s += 2;
            } else "`" === n ? (r = !r, s++) : "." !== n || r ? (e += n, s++) : (i(), s++);
        }
        if (i(), r) throw new O(x.INVALID_ARGUMENT, "Unterminated ` in path: " + t);
        return new q(n);
    }
    static $() {
        return new q([]);
    }
}

/**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */ class B {
    constructor(t) {
        this.path = t;
    }
    static j(t) {
        return new B(M.L(t));
    }
    static W(t) {
        return new B(M.L(t).v(5));
    }
    /** Returns true if the document is in the specified collectionId. */    K(t) {
        return this.path.length >= 2 && this.path.get(this.path.length - 2) === t;
    }
    isEqual(t) {
        return null !== t && 0 === M.g(this.path, t.path);
    }
    toString() {
        return this.path.toString();
    }
    static g(t, n) {
        return M.g(t.path, n.path);
    }
    static G(t) {
        return t.length % 2 == 0;
    }
    /**
     * Creates and returns a new document key with the given segments.
     *
     * @param segments The segments of the path to the document
     * @return A new instance of DocumentKey
     */    static H(t) {
        return new B(new M(t.slice()));
    }
}

/**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/**
 * Returns whether a variable is either undefined or null.
 */ function Q(t) {
    return null == t;
}

/** Returns whether the value represents -0. */ function j(t) {
    // Detect if the value is -0.0. Based on polyfill from
    // https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Object/is
    return 0 === t && 1 / t == -1 / 0;
}

/**
 * Returns whether a value is an integer and in the safe integer range
 * @param value The value to test for being an integer and in the safe range
 */
/**
 * @license
 * Copyright 2019 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
// Visible for testing
class W {
    constructor(t, n = null, e = [], s = [], i = null, r = null, o = null) {
        this.path = t, this.collectionGroup = n, this.orderBy = e, this.filters = s, this.limit = i, 
        this.startAt = r, this.endAt = o, this.Y = null;
    }
}

/**
 * Initializes a Target with a path and optional additional query constraints.
 * Path must currently be empty if this is a collection group query.
 *
 * NOTE: you should always construct `Target` from `Query.toTarget` instead of
 * using this factory method, because `Query` provides an implicit `orderBy`
 * property.
 */ function K(t, n = null, e = [], s = [], i = null, r = null, o = null) {
    return new W(t, n, e, s, i, r, o);
}

function G(t) {
    const n = g(t);
    if (null === n.Y) {
        let t = n.path.k();
        null !== n.collectionGroup && (t += "|cg:" + n.collectionGroup), t += "|f:", t += n.filters.map((t => $e(t))).join(","), 
        t += "|ob:", t += n.orderBy.map((t => {
            return (n = t).field.k() + n.dir;
            var n;
        })).join(","), Q(n.limit) || (t += "|l:", t += n.limit), n.startAt && (t += "|lb:", 
        t += ze(n.startAt)), n.endAt && (t += "|ub:", t += ze(n.endAt)), n.Y = t;
    }
    return n.Y;
}

function z(t) {
    let n = t.path.k();
    return null !== t.collectionGroup && (n += " collectionGroup=" + t.collectionGroup), 
    t.filters.length > 0 && (n += `, filters: [${t.filters.map((t => {
        return `${(n = t).field.k()} ${n.op} ${Ut(n.value)}`;
        /** Returns a debug description for `filter`. */
        var n;
        /** Filter that matches on key fields (i.e. '__name__'). */    })).join(", ")}]`), 
    Q(t.limit) || (n += ", limit: " + t.limit), t.orderBy.length > 0 && (n += `, orderBy: [${t.orderBy.map((t => {
        return `${(n = t).field.k()} (${n.dir})`;
        var n;
    })).join(", ")}]`), t.startAt && (n += ", startAt: " + ze(t.startAt)), t.endAt && (n += ", endAt: " + ze(t.endAt)), 
    `Target(${n})`;
}

function H(t, n) {
    if (t.limit !== n.limit) return !1;
    if (t.orderBy.length !== n.orderBy.length) return !1;
    for (let e = 0; e < t.orderBy.length; e++) if (!Ze(t.orderBy[e], n.orderBy[e])) return !1;
    if (t.filters.length !== n.filters.length) return !1;
    for (let i = 0; i < t.filters.length; i++) if (e = t.filters[i], s = n.filters[i], 
    e.op !== s.op || !e.field.isEqual(s.field) || !kt(e.value, s.value)) return !1;
    var e, s;
    return t.collectionGroup === n.collectionGroup && (!!t.path.isEqual(n.path) && (!!Ye(t.startAt, n.startAt) && Ye(t.endAt, n.endAt)));
}

function Y(t) {
    return B.G(t.path) && null === t.collectionGroup && 0 === t.filters.length;
}

/**
 * @license
 * Copyright 2020 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/** Converts a Base64 encoded string to a binary string. */
/**
 * @license
 * Copyright 2020 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/**
 * Immutable class that represents a "proto" byte string.
 *
 * Proto byte strings can either be Base64-encoded strings or Uint8Arrays when
 * sent on the wire. This class abstracts away this differentiation by holding
 * the proto byte string in a common class that must be converted into a string
 * before being sent as a proto.
 */
class J {
    constructor(t) {
        this.J = t;
    }
    static fromBase64String(t) {
        const n = atob(t);
        return new J(n);
    }
    static fromUint8Array(t) {
        const n = 
        /**
 * Helper function to convert an Uint8array to a binary string.
 */
        function(t) {
            let n = "";
            for (let e = 0; e < t.length; ++e) n += String.fromCharCode(t[e]);
            return n;
        }
        /**
 * Helper function to convert a binary string to an Uint8Array.
 */ (t);
        return new J(n);
    }
    toBase64() {
        return t = this.J, btoa(t);
        /** Converts a binary string to a Base64 encoded string. */
        var t;
        /** True if and only if the Base64 conversion functions are available. */    }
    toUint8Array() {
        return function(t) {
            const n = new Uint8Array(t.length);
            for (let e = 0; e < t.length; e++) n[e] = t.charCodeAt(e);
            return n;
        }
        /**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
        /**
 * An immutable set of metadata that the local store tracks for each target.
 */ (this.J);
    }
    X() {
        return 2 * this.J.length;
    }
    A(t) {
        return b(this.J, t.J);
    }
    isEqual(t) {
        return this.J === t.J;
    }
}

J.Z = new J("");

class X {
    constructor(
    /** The target being listened to. */
    t, 
    /**
     * The target ID to which the target corresponds; Assigned by the
     * LocalStore for user listens and by the SyncEngine for limbo watches.
     */
    n, 
    /** The purpose of the target. */
    e, 
    /**
     * The sequence number of the last transaction during which this target data
     * was modified.
     */
    s, 
    /** The latest snapshot version seen for this target. */
    i = L.min()
    /**
     * The maximum snapshot version at which the associated view
     * contained no limbo documents.
     */ , r = L.min()
    /**
     * An opaque, server-assigned token that allows watching a target to be
     * resumed after disconnecting without retransmitting all the data that
     * matches the target. The resume token essentially identifies a point in
     * time from which the server should resume sending results.
     */ , o = J.Z) {
        this.target = t, this.targetId = n, this.tt = e, this.sequenceNumber = s, this.nt = i, 
        this.lastLimboFreeSnapshotVersion = r, this.resumeToken = o;
    }
    /** Creates a new target data instance with an updated sequence number. */    et(t) {
        return new X(this.target, this.targetId, this.tt, t, this.nt, this.lastLimboFreeSnapshotVersion, this.resumeToken);
    }
    /**
     * Creates a new target data instance with an updated resume token and
     * snapshot version.
     */    st(t, n) {
        return new X(this.target, this.targetId, this.tt, this.sequenceNumber, n, this.lastLimboFreeSnapshotVersion, t);
    }
    /**
     * Creates a new target data instance with an updated last limbo free
     * snapshot version number.
     */    it(t) {
        return new X(this.target, this.targetId, this.tt, this.sequenceNumber, this.nt, t, this.resumeToken);
    }
}

/**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */ class Z {
    // TODO(b/33078163): just use simplest form of existence filter for now
    constructor(t) {
        this.count = t;
    }
}

/**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/**
 * Error Codes describing the different ways GRPC can fail. These are copied
 * directly from GRPC's sources here:
 *
 * https://github.com/grpc/grpc/blob/bceec94ea4fc5f0085d81235d8e1c06798dc341a/include/grpc%2B%2B/impl/codegen/status_code_enum.h
 *
 * Important! The names of these identifiers matter because the string forms
 * are used for reverse lookups from the webchannel stream. Do NOT change the
 * names of these identifiers or change this into a const enum.
 */ var tt, nt;

/**
 * Determines whether an error code represents a permanent error when received
 * in response to a non-write operation.
 *
 * See isPermanentWriteError for classifying write errors.
 */
function et(t) {
    switch (t) {
      case x.OK:
        return V();

      case x.CANCELLED:
      case x.UNKNOWN:
      case x.DEADLINE_EXCEEDED:
      case x.RESOURCE_EXHAUSTED:
      case x.INTERNAL:
      case x.UNAVAILABLE:
 // Unauthenticated means something went wrong with our token and we need
        // to retry with new credentials which will happen automatically.
              case x.UNAUTHENTICATED:
        return !1;

      case x.INVALID_ARGUMENT:
      case x.NOT_FOUND:
      case x.ALREADY_EXISTS:
      case x.PERMISSION_DENIED:
      case x.FAILED_PRECONDITION:
 // Aborted might be retried in some scenarios, but that is dependant on
        // the context and should handled individually by the calling code.
        // See https://cloud.google.com/apis/design/errors.
              case x.ABORTED:
      case x.OUT_OF_RANGE:
      case x.UNIMPLEMENTED:
      case x.DATA_LOSS:
        return !0;

      default:
        return V();
    }
}

/**
 * Determines whether an error code represents a permanent error when received
 * in response to a write operation.
 *
 * Write operations must be handled specially because as of b/119437764, ABORTED
 * errors on the write stream should be retried too (even though ABORTED errors
 * are not generally retryable).
 *
 * Note that during the initial handshake on the write stream an ABORTED error
 * signals that we should discard our stream token (i.e. it is permanent). This
 * means a handshake error should be classified with isPermanentError, above.
 */
/**
 * Maps an error Code from GRPC status code number, like 0, 1, or 14. These
 * are not the same as HTTP status codes.
 *
 * @returns The Code equivalent to the given GRPC status code. Fails if there
 *     is no match.
 */
function st(t) {
    if (void 0 === t) 
    // This shouldn't normally happen, but in certain error cases (like trying
    // to send invalid proto messages) we may get an error with no GRPC code.
    return A("GRPC error has no .code"), x.UNKNOWN;
    switch (t) {
      case tt.OK:
        return x.OK;

      case tt.CANCELLED:
        return x.CANCELLED;

      case tt.UNKNOWN:
        return x.UNKNOWN;

      case tt.DEADLINE_EXCEEDED:
        return x.DEADLINE_EXCEEDED;

      case tt.RESOURCE_EXHAUSTED:
        return x.RESOURCE_EXHAUSTED;

      case tt.INTERNAL:
        return x.INTERNAL;

      case tt.UNAVAILABLE:
        return x.UNAVAILABLE;

      case tt.UNAUTHENTICATED:
        return x.UNAUTHENTICATED;

      case tt.INVALID_ARGUMENT:
        return x.INVALID_ARGUMENT;

      case tt.NOT_FOUND:
        return x.NOT_FOUND;

      case tt.ALREADY_EXISTS:
        return x.ALREADY_EXISTS;

      case tt.PERMISSION_DENIED:
        return x.PERMISSION_DENIED;

      case tt.FAILED_PRECONDITION:
        return x.FAILED_PRECONDITION;

      case tt.ABORTED:
        return x.ABORTED;

      case tt.OUT_OF_RANGE:
        return x.OUT_OF_RANGE;

      case tt.UNIMPLEMENTED:
        return x.UNIMPLEMENTED;

      case tt.DATA_LOSS:
        return x.DATA_LOSS;

      default:
        return V();
    }
}

/**
 * Converts an HTTP response's error status to the equivalent error code.
 *
 * @param status An HTTP error response status ("FAILED_PRECONDITION",
 * "UNKNOWN", etc.)
 * @returns The equivalent Code. Non-matching responses are mapped to
 *     Code.UNKNOWN.
 */ (nt = tt || (tt = {}))[nt.OK = 0] = "OK", nt[nt.CANCELLED = 1] = "CANCELLED", 
nt[nt.UNKNOWN = 2] = "UNKNOWN", nt[nt.INVALID_ARGUMENT = 3] = "INVALID_ARGUMENT", 
nt[nt.DEADLINE_EXCEEDED = 4] = "DEADLINE_EXCEEDED", nt[nt.NOT_FOUND = 5] = "NOT_FOUND", 
nt[nt.ALREADY_EXISTS = 6] = "ALREADY_EXISTS", nt[nt.PERMISSION_DENIED = 7] = "PERMISSION_DENIED", 
nt[nt.UNAUTHENTICATED = 16] = "UNAUTHENTICATED", nt[nt.RESOURCE_EXHAUSTED = 8] = "RESOURCE_EXHAUSTED", 
nt[nt.FAILED_PRECONDITION = 9] = "FAILED_PRECONDITION", nt[nt.ABORTED = 10] = "ABORTED", 
nt[nt.OUT_OF_RANGE = 11] = "OUT_OF_RANGE", nt[nt.UNIMPLEMENTED = 12] = "UNIMPLEMENTED", 
nt[nt.INTERNAL = 13] = "INTERNAL", nt[nt.UNAVAILABLE = 14] = "UNAVAILABLE", nt[nt.DATA_LOSS = 15] = "DATA_LOSS";

/**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
// An immutable sorted map implementation, based on a Left-leaning Red-Black
// tree.
class it {
    constructor(t, n) {
        this.g = t, this.root = n || ot.EMPTY;
    }
    // Returns a copy of the map, with the specified key/value added or replaced.
    rt(t, n) {
        return new it(this.g, this.root.rt(t, n, this.g).copy(null, null, ot.ot, null, null));
    }
    // Returns a copy of the map, with the specified key removed.
    remove(t) {
        return new it(this.g, this.root.remove(t, this.g).copy(null, null, ot.ot, null, null));
    }
    // Returns the value of the node with the given key, or null.
    get(t) {
        let n = this.root;
        for (;!n.T(); ) {
            const e = this.g(t, n.key);
            if (0 === e) return n.value;
            e < 0 ? n = n.left : e > 0 && (n = n.right);
        }
        return null;
    }
    // Returns the index of the element in this sorted map, or -1 if it doesn't
    // exist.
    indexOf(t) {
        // Number of nodes that were pruned when descending right
        let n = 0, e = this.root;
        for (;!e.T(); ) {
            const s = this.g(t, e.key);
            if (0 === s) return n + e.left.size;
            s < 0 ? e = e.left : (
            // Count all nodes left of the node plus the node itself
            n += e.left.size + 1, e = e.right);
        }
        // Node not found
                return -1;
    }
    T() {
        return this.root.T();
    }
    // Returns the total number of nodes in the map.
    get size() {
        return this.root.size;
    }
    // Returns the minimum key in the map.
    ut() {
        return this.root.ut();
    }
    // Returns the maximum key in the map.
    ct() {
        return this.root.ct();
    }
    // Traverses the map in key order and calls the specified action function
    // for each key/value pair. If action returns true, traversal is aborted.
    // Returns the first truthy value returned by action, or the last falsey
    // value returned by action.
    at(t) {
        return this.root.at(t);
    }
    forEach(t) {
        this.at(((n, e) => (t(n, e), !1)));
    }
    toString() {
        const t = [];
        return this.at(((n, e) => (t.push(`${n}:${e}`), !1))), `{${t.join(", ")}}`;
    }
    // Traverses the map in reverse key order and calls the specified action
    // function for each key/value pair. If action returns true, traversal is
    // aborted.
    // Returns the first truthy value returned by action, or the last falsey
    // value returned by action.
    ht(t) {
        return this.root.ht(t);
    }
    // Returns an iterator over the SortedMap.
    lt() {
        return new rt(this.root, null, this.g, !1);
    }
    _t(t) {
        return new rt(this.root, t, this.g, !1);
    }
    ft() {
        return new rt(this.root, null, this.g, !0);
    }
    dt(t) {
        return new rt(this.root, t, this.g, !0);
    }
}

 // end SortedMap
// An iterator over an LLRBNode.
class rt {
    constructor(t, n, e, s) {
        this.wt = s, this.Et = [];
        let i = 1;
        for (;!t.T(); ) if (i = n ? e(t.key, n) : 1, 
        // flip the comparison if we're going in reverse
        s && (i *= -1), i < 0) 
        // This node is less than our start key. ignore it
        t = this.wt ? t.left : t.right; else {
            if (0 === i) {
                // This node is exactly equal to our start key. Push it on the stack,
                // but stop iterating;
                this.Et.push(t);
                break;
            }
            // This node is greater than our start key, add it to the stack and move
            // to the next one
            this.Et.push(t), t = this.wt ? t.right : t.left;
        }
    }
    Tt() {
        let t = this.Et.pop();
        const n = {
            key: t.key,
            value: t.value
        };
        if (this.wt) for (t = t.left; !t.T(); ) this.Et.push(t), t = t.right; else for (t = t.right; !t.T(); ) this.Et.push(t), 
        t = t.left;
        return n;
    }
    It() {
        return this.Et.length > 0;
    }
    At() {
        if (0 === this.Et.length) return null;
        const t = this.Et[this.Et.length - 1];
        return {
            key: t.key,
            value: t.value
        };
    }
}

 // end SortedMapIterator
// Represents a node in a Left-leaning Red-Black tree.
class ot {
    constructor(t, n, e, s, i) {
        this.key = t, this.value = n, this.color = null != e ? e : ot.RED, this.left = null != s ? s : ot.EMPTY, 
        this.right = null != i ? i : ot.EMPTY, this.size = this.left.size + 1 + this.right.size;
    }
    // Returns a copy of the current node, optionally replacing pieces of it.
    copy(t, n, e, s, i) {
        return new ot(null != t ? t : this.key, null != n ? n : this.value, null != e ? e : this.color, null != s ? s : this.left, null != i ? i : this.right);
    }
    T() {
        return !1;
    }
    // Traverses the tree in key order and calls the specified action function
    // for each node. If action returns true, traversal is aborted.
    // Returns the first truthy value returned by action, or the last falsey
    // value returned by action.
    at(t) {
        return this.left.at(t) || t(this.key, this.value) || this.right.at(t);
    }
    // Traverses the tree in reverse key order and calls the specified action
    // function for each node. If action returns true, traversal is aborted.
    // Returns the first truthy value returned by action, or the last falsey
    // value returned by action.
    ht(t) {
        return this.right.ht(t) || t(this.key, this.value) || this.left.ht(t);
    }
    // Returns the minimum node in the tree.
    min() {
        return this.left.T() ? this : this.left.min();
    }
    // Returns the maximum key in the tree.
    ut() {
        return this.min().key;
    }
    // Returns the maximum key in the tree.
    ct() {
        return this.right.T() ? this.key : this.right.ct();
    }
    // Returns new tree, with the key/value added.
    rt(t, n, e) {
        let s = this;
        const i = e(t, s.key);
        return s = i < 0 ? s.copy(null, null, null, s.left.rt(t, n, e), null) : 0 === i ? s.copy(null, n, null, null, null) : s.copy(null, null, null, null, s.right.rt(t, n, e)), 
        s.Rt();
    }
    Pt() {
        if (this.left.T()) return ot.EMPTY;
        let t = this;
        return t.left.Vt() || t.left.left.Vt() || (t = t.yt()), t = t.copy(null, null, null, t.left.Pt(), null), 
        t.Rt();
    }
    // Returns new tree, with the specified item removed.
    remove(t, n) {
        let e, s = this;
        if (n(t, s.key) < 0) s.left.T() || s.left.Vt() || s.left.left.Vt() || (s = s.yt()), 
        s = s.copy(null, null, null, s.left.remove(t, n), null); else {
            if (s.left.Vt() && (s = s.gt()), s.right.T() || s.right.Vt() || s.right.left.Vt() || (s = s.vt()), 
            0 === n(t, s.key)) {
                if (s.right.T()) return ot.EMPTY;
                e = s.right.min(), s = s.copy(e.key, e.value, null, null, s.right.Pt());
            }
            s = s.copy(null, null, null, null, s.right.remove(t, n));
        }
        return s.Rt();
    }
    Vt() {
        return this.color;
    }
    // Returns new tree after performing any needed rotations.
    Rt() {
        let t = this;
        return t.right.Vt() && !t.left.Vt() && (t = t.bt()), t.left.Vt() && t.left.left.Vt() && (t = t.gt()), 
        t.left.Vt() && t.right.Vt() && (t = t.Ct()), t;
    }
    yt() {
        let t = this.Ct();
        return t.right.left.Vt() && (t = t.copy(null, null, null, null, t.right.gt()), t = t.bt(), 
        t = t.Ct()), t;
    }
    vt() {
        let t = this.Ct();
        return t.left.left.Vt() && (t = t.gt(), t = t.Ct()), t;
    }
    bt() {
        const t = this.copy(null, null, ot.RED, null, this.right.left);
        return this.right.copy(null, null, this.color, t, null);
    }
    gt() {
        const t = this.copy(null, null, ot.RED, this.left.right, null);
        return this.left.copy(null, null, this.color, null, t);
    }
    Ct() {
        const t = this.left.copy(null, null, !this.left.color, null, null), n = this.right.copy(null, null, !this.right.color, null, null);
        return this.copy(null, null, !this.color, t, n);
    }
    // For testing.
    St() {
        const t = this.Dt();
        return Math.pow(2, t) <= this.size + 1;
    }
    // In a balanced RB tree, the black-depth (number of black nodes) from root to
    // leaves is equal on both sides.  This function verifies that or asserts.
    Dt() {
        if (this.Vt() && this.left.Vt()) throw V();
        if (this.right.Vt()) throw V();
        const t = this.left.Dt();
        if (t !== this.right.Dt()) throw V();
        return t + (this.Vt() ? 0 : 1);
    }
}

 // end LLRBNode
// Empty node is shared between all LLRB trees.
// eslint-disable-next-line @typescript-eslint/no-explicit-any
ot.EMPTY = null, ot.RED = !0, ot.ot = !1;

// end LLRBEmptyNode
ot.EMPTY = new 
// Represents an empty node (a leaf node in the Red-Black Tree).
class {
    constructor() {
        this.size = 0;
    }
    get key() {
        throw V();
    }
    get value() {
        throw V();
    }
    get color() {
        throw V();
    }
    get left() {
        throw V();
    }
    get right() {
        throw V();
    }
    // Returns a copy of the current node.
    copy(t, n, e, s, i) {
        return this;
    }
    // Returns a copy of the tree, with the specified key/value added.
    rt(t, n, e) {
        return new ot(t, n);
    }
    // Returns a copy of the tree, with the specified key removed.
    remove(t, n) {
        return this;
    }
    T() {
        return !0;
    }
    at(t) {
        return !1;
    }
    ht(t) {
        return !1;
    }
    ut() {
        return null;
    }
    ct() {
        return null;
    }
    Vt() {
        return !1;
    }
    // For testing.
    St() {
        return !0;
    }
    Dt() {
        return 0;
    }
};

/**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/**
 * SortedSet is an immutable (copy-on-write) collection that holds elements
 * in order specified by the provided comparator.
 *
 * NOTE: if provided comparator returns 0 for two elements, we consider them to
 * be equal!
 */
class ut {
    constructor(t) {
        this.g = t, this.data = new it(this.g);
    }
    has(t) {
        return null !== this.data.get(t);
    }
    first() {
        return this.data.ut();
    }
    last() {
        return this.data.ct();
    }
    get size() {
        return this.data.size;
    }
    indexOf(t) {
        return this.data.indexOf(t);
    }
    /** Iterates elements in order defined by "comparator" */    forEach(t) {
        this.data.at(((n, e) => (t(n), !1)));
    }
    /** Iterates over `elem`s such that: range[0] <= elem < range[1]. */    Nt(t, n) {
        const e = this.data._t(t[0]);
        for (;e.It(); ) {
            const s = e.Tt();
            if (this.g(s.key, t[1]) >= 0) return;
            n(s.key);
        }
    }
    /**
     * Iterates over `elem`s such that: start <= elem until false is returned.
     */    Ft(t, n) {
        let e;
        for (e = void 0 !== n ? this.data._t(n) : this.data.lt(); e.It(); ) {
            if (!t(e.Tt().key)) return;
        }
    }
    /** Finds the least element greater than or equal to `elem`. */    xt(t) {
        const n = this.data._t(t);
        return n.It() ? n.Tt().key : null;
    }
    lt() {
        return new ct(this.data.lt());
    }
    _t(t) {
        return new ct(this.data._t(t));
    }
    /** Inserts or updates an element */    add(t) {
        return this.copy(this.data.remove(t).rt(t, !0));
    }
    /** Deletes an element */    delete(t) {
        return this.has(t) ? this.copy(this.data.remove(t)) : this;
    }
    T() {
        return this.data.T();
    }
    Ot(t) {
        let n = this;
        // Make sure `result` always refers to the larger one of the two sets.
                return n.size < t.size && (n = t, t = this), t.forEach((t => {
            n = n.add(t);
        })), n;
    }
    isEqual(t) {
        if (!(t instanceof ut)) return !1;
        if (this.size !== t.size) return !1;
        const n = this.data.lt(), e = t.data.lt();
        for (;n.It(); ) {
            const t = n.Tt().key, s = e.Tt().key;
            if (0 !== this.g(t, s)) return !1;
        }
        return !0;
    }
    O() {
        const t = [];
        return this.forEach((n => {
            t.push(n);
        })), t;
    }
    toString() {
        const t = [];
        return this.forEach((n => t.push(n))), "SortedSet(" + t.toString() + ")";
    }
    copy(t) {
        const n = new ut(this.g);
        return n.data = t, n;
    }
}

class ct {
    constructor(t) {
        this.kt = t;
    }
    Tt() {
        return this.kt.Tt().key;
    }
    It() {
        return this.kt.It();
    }
}

/**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */ const at = new it(B.g);

function ht() {
    return at;
}

function lt() {
    return ht();
}

const _t = new it(B.g);

function ft() {
    return _t;
}

const dt = new it(B.g);

const wt = new ut(B.g);

function Et(...t) {
    let n = wt;
    for (const e of t) n = n.add(e);
    return n;
}

const Tt = new ut(b);

function mt() {
    return Tt;
}

/**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/**
 * DocumentSet is an immutable (copy-on-write) collection that holds documents
 * in order specified by the provided comparator. We always add a document key
 * comparator on top of what is provided to guarantee document equality based on
 * the key.
 */ class It {
    /** The default ordering is by key if the comparator is omitted */
    constructor(t) {
        // We are adding document key comparator to the end as it's the only
        // guaranteed unique property of a document.
        this.g = t ? (n, e) => t(n, e) || B.g(n.key, e.key) : (t, n) => B.g(t.key, n.key), 
        this.Lt = ft(), this.$t = new it(this.g);
    }
    /**
     * Returns an empty copy of the existing DocumentSet, using the same
     * comparator.
     */    static Mt(t) {
        return new It(t.g);
    }
    has(t) {
        return null != this.Lt.get(t);
    }
    get(t) {
        return this.Lt.get(t);
    }
    first() {
        return this.$t.ut();
    }
    last() {
        return this.$t.ct();
    }
    T() {
        return this.$t.T();
    }
    /**
     * Returns the index of the provided key in the document set, or -1 if the
     * document key is not present in the set;
     */    indexOf(t) {
        const n = this.Lt.get(t);
        return n ? this.$t.indexOf(n) : -1;
    }
    get size() {
        return this.$t.size;
    }
    /** Iterates documents in order defined by "comparator" */    forEach(t) {
        this.$t.at(((n, e) => (t(n), !1)));
    }
    /** Inserts or updates a document with the same key */    add(t) {
        // First remove the element if we have it.
        const n = this.delete(t.key);
        return n.copy(n.Lt.rt(t.key, t), n.$t.rt(t, null));
    }
    /** Deletes a document with a given key */    delete(t) {
        const n = this.get(t);
        return n ? this.copy(this.Lt.remove(t), this.$t.remove(n)) : this;
    }
    isEqual(t) {
        if (!(t instanceof It)) return !1;
        if (this.size !== t.size) return !1;
        const n = this.$t.lt(), e = t.$t.lt();
        for (;n.It(); ) {
            const t = n.Tt().key, s = e.Tt().key;
            if (!t.isEqual(s)) return !1;
        }
        return !0;
    }
    toString() {
        const t = [];
        return this.forEach((n => {
            t.push(n.toString());
        })), 0 === t.length ? "DocumentSet ()" : "DocumentSet (\n  " + t.join("  \n") + "\n)";
    }
    copy(t, n) {
        const e = new It;
        return e.g = this.g, e.Lt = t, e.$t = n, e;
    }
}

/**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/**
 * DocumentChangeSet keeps track of a set of changes to docs in a query, merging
 * duplicate events for the same doc.
 */ class At {
    constructor() {
        this.Ut = new it(B.g);
    }
    track(t) {
        const n = t.doc.key, e = this.Ut.get(n);
        e ? 
        // Merge the new change with the existing change.
        0 /* Added */ !== t.type && 3 /* Metadata */ === e.type ? this.Ut = this.Ut.rt(n, t) : 3 /* Metadata */ === t.type && 1 /* Removed */ !== e.type ? this.Ut = this.Ut.rt(n, {
            type: e.type,
            doc: t.doc
        }) : 2 /* Modified */ === t.type && 2 /* Modified */ === e.type ? this.Ut = this.Ut.rt(n, {
            type: 2 /* Modified */ ,
            doc: t.doc
        }) : 2 /* Modified */ === t.type && 0 /* Added */ === e.type ? this.Ut = this.Ut.rt(n, {
            type: 0 /* Added */ ,
            doc: t.doc
        }) : 1 /* Removed */ === t.type && 0 /* Added */ === e.type ? this.Ut = this.Ut.remove(n) : 1 /* Removed */ === t.type && 2 /* Modified */ === e.type ? this.Ut = this.Ut.rt(n, {
            type: 1 /* Removed */ ,
            doc: e.doc
        }) : 0 /* Added */ === t.type && 1 /* Removed */ === e.type ? this.Ut = this.Ut.rt(n, {
            type: 2 /* Modified */ ,
            doc: t.doc
        }) : 
        // This includes these cases, which don't make sense:
        // Added->Added
        // Removed->Removed
        // Modified->Added
        // Removed->Modified
        // Metadata->Added
        // Removed->Metadata
        V() : this.Ut = this.Ut.rt(n, t);
    }
    qt() {
        const t = [];
        return this.Ut.at(((n, e) => {
            t.push(e);
        })), t;
    }
}

class Rt {
    constructor(t, n, e, s, i, r, o, u) {
        this.query = t, this.docs = n, this.Bt = e, this.docChanges = s, this.Qt = i, this.fromCache = r, 
        this.jt = o, this.Wt = u;
    }
    /** Returns a view snapshot as if all documents in the snapshot were added. */    static Kt(t, n, e, s) {
        const i = [];
        return n.forEach((t => {
            i.push({
                type: 0 /* Added */ ,
                doc: t
            });
        })), new Rt(t, n, It.Mt(n), i, e, s, 
        /* syncStateChanged= */ !0, 
        /* excludesMetadataChanges= */ !1);
    }
    get hasPendingWrites() {
        return !this.Qt.T();
    }
    isEqual(t) {
        if (!(this.fromCache === t.fromCache && this.jt === t.jt && this.Qt.isEqual(t.Qt) && Ne(this.query, t.query) && this.docs.isEqual(t.docs) && this.Bt.isEqual(t.Bt))) return !1;
        const n = this.docChanges, e = t.docChanges;
        if (n.length !== e.length) return !1;
        for (let t = 0; t < n.length; t++) if (n[t].type !== e[t].type || !n[t].doc.isEqual(e[t].doc)) return !1;
        return !0;
    }
}

/**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/**
 * An event from the RemoteStore. It is split into targetChanges (changes to the
 * state or the set of documents in our watched targets) and documentUpdates
 * (changes to the actual documents).
 */ class Pt {
    constructor(
    /**
     * The snapshot version this event brings us up to, or MIN if not set.
     */
    t, 
    /**
     * A map from target to changes to the target. See TargetChange.
     */
    n, 
    /**
     * A set of targets that is known to be inconsistent. Listens for these
     * targets should be re-established without resume tokens.
     */
    e, 
    /**
     * A set of which documents have changed or been deleted, along with the
     * doc's new values (if not deleted).
     */
    s, 
    /**
     * A set of which document updates are due only to limbo resolution targets.
     */
    i) {
        this.nt = t, this.Gt = n, this.zt = e, this.Ht = s, this.Yt = i;
    }
    /**
     * HACK: Views require RemoteEvents in order to determine whether the view is
     * CURRENT, but secondary tabs don't receive remote events. So this method is
     * used to create a synthesized RemoteEvent that can be used to apply a
     * CURRENT status change to a View, for queries executed in a different tab.
     */
    // PORTING NOTE: Multi-tab only
    static Jt(t, n) {
        const e = new Map;
        return e.set(t, Vt.Xt(t, n)), new Pt(L.min(), e, mt(), ht(), Et());
    }
}

/**
 * A TargetChange specifies the set of changes for a specific target as part of
 * a RemoteEvent. These changes track which documents are added, modified or
 * removed, as well as the target's resume token and whether the target is
 * marked CURRENT.
 * The actual changes *to* documents are not part of the TargetChange since
 * documents may be part of multiple targets.
 */ class Vt {
    constructor(
    /**
     * An opaque, server-assigned token that allows watching a query to be resumed
     * after disconnecting without retransmitting all the data that matches the
     * query. The resume token essentially identifies a point in time from which
     * the server should resume sending results.
     */
    t, 
    /**
     * The "current" (synced) status of this target. Note that "current"
     * has special meaning in the RPC protocol that implies that a target is
     * both up-to-date and consistent with the rest of the watch stream.
     */
    n, 
    /**
     * The set of documents that were newly assigned to this target as part of
     * this remote event.
     */
    e, 
    /**
     * The set of documents that were already assigned to this target but received
     * an update during this remote event.
     */
    s, 
    /**
     * The set of documents that were removed from this target as part of this
     * remote event.
     */
    i) {
        this.resumeToken = t, this.Zt = n, this.tn = e, this.nn = s, this.en = i;
    }
    /**
     * This method is used to create a synthesized TargetChanges that can be used to
     * apply a CURRENT status change to a View (for queries executed in a different
     * tab) or for new queries (to raise snapshots with correct CURRENT status).
     */    static Xt(t, n) {
        return new Vt(J.Z, n, Et(), Et(), Et());
    }
}

/**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/**
 * Represents a changed document and a list of target ids to which this change
 * applies.
 *
 * If document has been deleted NoDocument will be provided.
 */ class yt {
    constructor(
    /** The new document applies to all of these targets. */
    t, 
    /** The new document is removed from all of these targets. */
    n, 
    /** The key of the document for this change. */
    e, 
    /**
     * The new document or NoDocument if it was deleted. Is null if the
     * document went out of view without the server sending a new document.
     */
    s) {
        this.sn = t, this.removedTargetIds = n, this.key = e, this.rn = s;
    }
}

class gt {
    constructor(t, n) {
        this.targetId = t, this.on = n;
    }
}

class pt {
    constructor(
    /** What kind of change occurred to the watch target. */
    t, 
    /** The target IDs that were added/removed/set. */
    n, 
    /**
     * An opaque, server-assigned token that allows watching a target to be
     * resumed after disconnecting without retransmitting all the data that
     * matches the target. The resume token essentially identifies a point in
     * time from which the server should resume sending results.
     */
    e = J.Z
    /** An RPC error indicating why the watch failed. */ , s = null) {
        this.state = t, this.targetIds = n, this.resumeToken = e, this.cause = s;
    }
}

/** Tracks the internal state of a Watch target. */ class vt {
    constructor() {
        /**
         * The number of pending responses (adds or removes) that we are waiting on.
         * We only consider targets active that have no pending responses.
         */
        this.un = 0, 
        /**
         * Keeps track of the document changes since the last raised snapshot.
         *
         * These changes are continuously updated as we receive document updates and
         * always reflect the current set of changes against the last issued snapshot.
         */
        this.cn = St(), 
        /** See public getters for explanations of these fields. */
        this.an = J.Z, this.hn = !1, 
        /**
         * Whether this target state should be included in the next snapshot. We
         * initialize to true so that newly-added targets are included in the next
         * RemoteEvent.
         */
        this.ln = !0;
    }
    /**
     * Whether this target has been marked 'current'.
     *
     * 'Current' has special meaning in the RPC protocol: It implies that the
     * Watch backend has sent us all changes up to the point at which the target
     * was added and that the target is consistent with the rest of the watch
     * stream.
     */    get Zt() {
        return this.hn;
    }
    /** The last resume token sent to us for this target. */    get resumeToken() {
        return this.an;
    }
    /** Whether this target has pending target adds or target removes. */    get _n() {
        return 0 !== this.un;
    }
    /** Whether we have modified any state that should trigger a snapshot. */    get fn() {
        return this.ln;
    }
    /**
     * Applies the resume token to the TargetChange, but only when it has a new
     * value. Empty resumeTokens are discarded.
     */    dn(t) {
        t.X() > 0 && (this.ln = !0, this.an = t);
    }
    /**
     * Creates a target change from the current set of changes.
     *
     * To reset the document changes after raising this snapshot, call
     * `clearPendingChanges()`.
     */    wn() {
        let t = Et(), n = Et(), e = Et();
        return this.cn.forEach(((s, i) => {
            switch (i) {
              case 0 /* Added */ :
                t = t.add(s);
                break;

              case 2 /* Modified */ :
                n = n.add(s);
                break;

              case 1 /* Removed */ :
                e = e.add(s);
                break;

              default:
                V();
            }
        })), new Vt(this.an, this.hn, t, n, e);
    }
    /**
     * Resets the document changes and sets `hasPendingChanges` to false.
     */    En() {
        this.ln = !1, this.cn = St();
    }
    Tn(t, n) {
        this.ln = !0, this.cn = this.cn.rt(t, n);
    }
    mn(t) {
        this.ln = !0, this.cn = this.cn.remove(t);
    }
    In() {
        this.un += 1;
    }
    An() {
        this.un -= 1;
    }
    Rn() {
        this.ln = !0, this.hn = !0;
    }
}

/**
 * A helper class to accumulate watch changes into a RemoteEvent.
 */
class bt {
    constructor(t) {
        this.Pn = t, 
        /** The internal state of all tracked targets. */
        this.Vn = new Map, 
        /** Keeps track of the documents to update since the last raised snapshot. */
        this.yn = ht(), 
        /** A mapping of document keys to their set of target IDs. */
        this.gn = Ct(), 
        /**
         * A list of targets with existence filter mismatches. These targets are
         * known to be inconsistent and their listens needs to be re-established by
         * RemoteStore.
         */
        this.pn = new ut(b);
    }
    /**
     * Processes and adds the DocumentWatchChange to the current set of changes.
     */    vn(t) {
        for (const n of t.sn) t.rn instanceof Te ? this.bn(n, t.rn) : t.rn instanceof me && this.Cn(n, t.key, t.rn);
        for (const n of t.removedTargetIds) this.Cn(n, t.key, t.rn);
    }
    /** Processes and adds the WatchTargetChange to the current set of changes. */    Sn(t) {
        this.Dn(t, (n => {
            const e = this.Nn(n);
            switch (t.state) {
              case 0 /* NoChange */ :
                this.Fn(n) && e.dn(t.resumeToken);
                break;

              case 1 /* Added */ :
                // We need to decrement the number of pending acks needed from watch
                // for this targetId.
                e.An(), e._n || 
                // We have a freshly added target, so we need to reset any state
                // that we had previously. This can happen e.g. when remove and add
                // back a target for existence filter mismatches.
                e.En(), e.dn(t.resumeToken);
                break;

              case 2 /* Removed */ :
                // We need to keep track of removed targets to we can post-filter and
                // remove any target changes.
                // We need to decrement the number of pending acks needed from watch
                // for this targetId.
                e.An(), e._n || this.removeTarget(n);
                break;

              case 3 /* Current */ :
                this.Fn(n) && (e.Rn(), e.dn(t.resumeToken));
                break;

              case 4 /* Reset */ :
                this.Fn(n) && (
                // Reset the target and synthesizes removes for all existing
                // documents. The backend will re-add any documents that still
                // match the target before it sends the next global snapshot.
                this.xn(n), e.dn(t.resumeToken));
                break;

              default:
                V();
            }
        }));
    }
    /**
     * Iterates over all targetIds that the watch change applies to: either the
     * targetIds explicitly listed in the change or the targetIds of all currently
     * active targets.
     */    Dn(t, n) {
        t.targetIds.length > 0 ? t.targetIds.forEach(n) : this.Vn.forEach(((t, e) => {
            this.Fn(e) && n(e);
        }));
    }
    /**
     * Handles existence filters and synthesizes deletes for filter mismatches.
     * Targets that are invalidated by filter mismatches are added to
     * `pendingTargetResets`.
     */    On(t) {
        const n = t.targetId, e = t.on.count, s = this.kn(n);
        if (s) {
            const t = s.target;
            if (Y(t)) if (0 === e) {
                // The existence filter told us the document does not exist. We deduce
                // that this document does not exist and apply a deleted document to
                // our updates. Without applying this deleted document there might be
                // another query that will raise this document as part of a snapshot
                // until it is resolved, essentially exposing inconsistency between
                // queries.
                const e = new B(t.path);
                this.Cn(n, e, new me(e, L.min()));
            } else y(1 === e); else {
                this.Ln(n) !== e && (
                // Existence filter mismatch: We reset the mapping and raise a new
                // snapshot with `isFromCache:true`.
                this.xn(n), this.pn = this.pn.add(n));
            }
        }
    }
    /**
     * Converts the currently accumulated state into a remote event at the
     * provided snapshot version. Resets the accumulated changes before returning.
     */    $n(t) {
        const n = new Map;
        this.Vn.forEach(((e, s) => {
            const i = this.kn(s);
            if (i) {
                if (e.Zt && Y(i.target)) {
                    // Document queries for document that don't exist can produce an empty
                    // result set. To update our local cache, we synthesize a document
                    // delete if we have not previously received the document. This
                    // resolves the limbo state of the document, removing it from
                    // limboDocumentRefs.
                    // TODO(dimond): Ideally we would have an explicit lookup target
                    // instead resulting in an explicit delete message and we could
                    // remove this special logic.
                    const n = new B(i.target.path);
                    null !== this.yn.get(n) || this.Mn(s, n) || this.Cn(s, n, new me(n, t));
                }
                e.fn && (n.set(s, e.wn()), e.En());
            }
        }));
        let e = Et();
        // We extract the set of limbo-only document updates as the GC logic
        // special-cases documents that do not appear in the target cache.
        
        // TODO(gsoltis): Expand on this comment once GC is available in the JS
        // client.
                this.gn.forEach(((t, n) => {
            let s = !0;
            n.Ft((t => {
                const n = this.kn(t);
                return !n || 2 /* LimboResolution */ === n.tt || (s = !1, !1);
            })), s && (e = e.add(t));
        }));
        const s = new Pt(t, n, this.pn, this.yn, e);
        return this.yn = ht(), this.gn = Ct(), this.pn = new ut(b), s;
    }
    /**
     * Adds the provided document to the internal list of document updates and
     * its document key to the given target's mapping.
     */
    // Visible for testing.
    bn(t, n) {
        if (!this.Fn(t)) return;
        const e = this.Mn(t, n.key) ? 2 /* Modified */ : 0 /* Added */;
        this.Nn(t).Tn(n.key, e), this.yn = this.yn.rt(n.key, n), this.gn = this.gn.rt(n.key, this.Un(n.key).add(t));
    }
    /**
     * Removes the provided document from the target mapping. If the
     * document no longer matches the target, but the document's state is still
     * known (e.g. we know that the document was deleted or we received the change
     * that caused the filter mismatch), the new document can be provided
     * to update the remote document cache.
     */
    // Visible for testing.
    Cn(t, n, e) {
        if (!this.Fn(t)) return;
        const s = this.Nn(t);
        this.Mn(t, n) ? s.Tn(n, 1 /* Removed */) : 
        // The document may have entered and left the target before we raised a
        // snapshot, so we can just ignore the change.
        s.mn(n), this.gn = this.gn.rt(n, this.Un(n).delete(t)), e && (this.yn = this.yn.rt(n, e));
    }
    removeTarget(t) {
        this.Vn.delete(t);
    }
    /**
     * Returns the current count of documents in the target. This includes both
     * the number of documents that the LocalStore considers to be part of the
     * target as well as any accumulated changes.
     */    Ln(t) {
        const n = this.Nn(t).wn();
        return this.Pn.qn(t).size + n.tn.size - n.en.size;
    }
    /**
     * Increment the number of acks needed from watch before we can consider the
     * server to be 'in-sync' with the client's active targets.
     */    In(t) {
        this.Nn(t).In();
    }
    Nn(t) {
        let n = this.Vn.get(t);
        return n || (n = new vt, this.Vn.set(t, n)), n;
    }
    Un(t) {
        let n = this.gn.get(t);
        return n || (n = new ut(b), this.gn = this.gn.rt(t, n)), n;
    }
    /**
     * Verifies that the user is still interested in this target (by calling
     * `getTargetDataForTarget()`) and that we are not waiting for pending ADDs
     * from watch.
     */    Fn(t) {
        const n = null !== this.kn(t);
        return n || I("WatchChangeAggregator", "Detected inactive target", t), n;
    }
    /**
     * Returns the TargetData for an active target (i.e. a target that the user
     * is still interested in that has no outstanding target change requests).
     */    kn(t) {
        const n = this.Vn.get(t);
        return n && n._n ? null : this.Pn.Bn(t);
    }
    /**
     * Resets the state of a Watch target to its initial state (e.g. sets
     * 'current' to false, clears the resume token and removes its target mapping
     * from all documents).
     */    xn(t) {
        this.Vn.set(t, new vt);
        this.Pn.qn(t).forEach((n => {
            this.Cn(t, n, /*updatedDocument=*/ null);
        }));
    }
    /**
     * Returns whether the LocalStore considers the document to be part of the
     * specified target.
     */    Mn(t, n) {
        return this.Pn.qn(t).has(n);
    }
}

function Ct() {
    return new it(B.g);
}

function St() {
    return new it(B.g);
}

/**
 * @license
 * Copyright 2020 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/**
 * Represents a locally-applied ServerTimestamp.
 *
 * Server Timestamps are backed by MapValues that contain an internal field
 * `__type__` with a value of `server_timestamp`. The previous value and local
 * write time are stored in its `__previous_value__` and `__local_write_time__`
 * fields respectively.
 *
 * Notes:
 * - ServerTimestampValue instances are created as the result of applying a
 *   TransformMutation (see TransformMutation.applyTo()). They can only exist in
 *   the local view of a document. Therefore they do not need to be parsed or
 *   serialized.
 * - When evaluated locally (e.g. for snapshot.data()), they by default
 *   evaluate to `null`. This behavior can be configured by passing custom
 *   FieldValueOptions to value().
 * - With respect to other ServerTimestampValues, they sort by their
 *   localWriteTime.
 */ function Dt(t) {
    var n, e;
    return "server_timestamp" === (null === (e = ((null === (n = null == t ? void 0 : t.mapValue) || void 0 === n ? void 0 : n.fields) || {}).__type__) || void 0 === e ? void 0 : e.stringValue);
}

/**
 * Creates a new ServerTimestamp proto value (using the internal format).
 */
/**
 * Returns the value of the field before this ServerTimestamp was set.
 *
 * Preserving the previous values allows the user to display the last resoled
 * value until the backend responds with the timestamp.
 */
function Nt(t) {
    const n = t.mapValue.fields.__previous_value__;
    return Dt(n) ? Nt(n) : n;
}

/**
 * Returns the local time at which this timestamp was first set.
 */ function Ft(t) {
    const n = Bt(t.mapValue.fields.__local_write_time__.timestampValue);
    return new k(n.seconds, n.nanos);
}

/**
 * @license
 * Copyright 2020 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
// A RegExp matching ISO 8601 UTC timestamps with optional fraction.
const xt = new RegExp(/^\d{4}-\d\d-\d\dT\d\d:\d\d:\d\d(?:\.(\d+))?Z$/);

/** Extracts the backend's type order for the provided value. */ function Ot(t) {
    return "nullValue" in t ? 0 /* NullValue */ : "booleanValue" in t ? 1 /* BooleanValue */ : "integerValue" in t || "doubleValue" in t ? 2 /* NumberValue */ : "timestampValue" in t ? 3 /* TimestampValue */ : "stringValue" in t ? 5 /* StringValue */ : "bytesValue" in t ? 6 /* BlobValue */ : "referenceValue" in t ? 7 /* RefValue */ : "geoPointValue" in t ? 8 /* GeoPointValue */ : "arrayValue" in t ? 9 /* ArrayValue */ : "mapValue" in t ? Dt(t) ? 4 /* ServerTimestampValue */ : 10 /* ObjectValue */ : V();
}

/** Tests `left` and `right` for equality based on the backend semantics. */ function kt(t, n) {
    const e = Ot(t);
    if (e !== Ot(n)) return !1;
    switch (e) {
      case 0 /* NullValue */ :
        return !0;

      case 1 /* BooleanValue */ :
        return t.booleanValue === n.booleanValue;

      case 4 /* ServerTimestampValue */ :
        return Ft(t).isEqual(Ft(n));

      case 3 /* TimestampValue */ :
        return function(t, n) {
            if ("string" == typeof t.timestampValue && "string" == typeof n.timestampValue && t.timestampValue.length === n.timestampValue.length) 
            // Use string equality for ISO 8601 timestamps
            return t.timestampValue === n.timestampValue;
            const e = Bt(t.timestampValue), s = Bt(n.timestampValue);
            return e.seconds === s.seconds && e.nanos === s.nanos;
        }(t, n);

      case 5 /* StringValue */ :
        return t.stringValue === n.stringValue;

      case 6 /* BlobValue */ :
        return function(t, n) {
            return jt(t.bytesValue).isEqual(jt(n.bytesValue));
        }(t, n);

      case 7 /* RefValue */ :
        return t.referenceValue === n.referenceValue;

      case 8 /* GeoPointValue */ :
        return function(t, n) {
            return Qt(t.geoPointValue.latitude) === Qt(n.geoPointValue.latitude) && Qt(t.geoPointValue.longitude) === Qt(n.geoPointValue.longitude);
        }(t, n);

      case 2 /* NumberValue */ :
        return function(t, n) {
            if ("integerValue" in t && "integerValue" in n) return Qt(t.integerValue) === Qt(n.integerValue);
            if ("doubleValue" in t && "doubleValue" in n) {
                const e = Qt(t.doubleValue), s = Qt(n.doubleValue);
                return e === s ? j(e) === j(s) : isNaN(e) && isNaN(s);
            }
            return !1;
        }(t, n);

      case 9 /* ArrayValue */ :
        return C(t.arrayValue.values || [], n.arrayValue.values || [], kt);

      case 10 /* ObjectValue */ :
        return function(t, n) {
            const e = t.mapValue.fields || {}, s = n.mapValue.fields || {};
            if (S(e) !== S(s)) return !1;
            for (const t in e) if (e.hasOwnProperty(t) && (void 0 === s[t] || !kt(e[t], s[t]))) return !1;
            return !0;
        }
        /** Returns true if the ArrayValue contains the specified element. */ (t, n);

      default:
        return V();
    }
}

function Lt(t, n) {
    return void 0 !== (t.values || []).find((t => kt(t, n)));
}

function $t(t, n) {
    const e = Ot(t), s = Ot(n);
    if (e !== s) return b(e, s);
    switch (e) {
      case 0 /* NullValue */ :
        return 0;

      case 1 /* BooleanValue */ :
        return b(t.booleanValue, n.booleanValue);

      case 2 /* NumberValue */ :
        return function(t, n) {
            const e = Qt(t.integerValue || t.doubleValue), s = Qt(n.integerValue || n.doubleValue);
            return e < s ? -1 : e > s ? 1 : e === s ? 0 : 
            // one or both are NaN.
            isNaN(e) ? isNaN(s) ? 0 : -1 : 1;
        }(t, n);

      case 3 /* TimestampValue */ :
        return Mt(t.timestampValue, n.timestampValue);

      case 4 /* ServerTimestampValue */ :
        return Mt(Ft(t), Ft(n));

      case 5 /* StringValue */ :
        return b(t.stringValue, n.stringValue);

      case 6 /* BlobValue */ :
        return function(t, n) {
            const e = jt(t), s = jt(n);
            return e.A(s);
        }(t.bytesValue, n.bytesValue);

      case 7 /* RefValue */ :
        return function(t, n) {
            const e = t.split("/"), s = n.split("/");
            for (let t = 0; t < e.length && t < s.length; t++) {
                const n = b(e[t], s[t]);
                if (0 !== n) return n;
            }
            return b(e.length, s.length);
        }(t.referenceValue, n.referenceValue);

      case 8 /* GeoPointValue */ :
        return function(t, n) {
            const e = b(Qt(t.latitude), Qt(n.latitude));
            if (0 !== e) return e;
            return b(Qt(t.longitude), Qt(n.longitude));
        }(t.geoPointValue, n.geoPointValue);

      case 9 /* ArrayValue */ :
        return function(t, n) {
            const e = t.values || [], s = n.values || [];
            for (let t = 0; t < e.length && t < s.length; ++t) {
                const n = $t(e[t], s[t]);
                if (n) return n;
            }
            return b(e.length, s.length);
        }(t.arrayValue, n.arrayValue);

      case 10 /* ObjectValue */ :
        return function(t, n) {
            const e = t.fields || {}, s = Object.keys(e), i = n.fields || {}, r = Object.keys(i);
            // Even though MapValues are likely sorted correctly based on their insertion
            // order (e.g. when received from the backend), local modifications can bring
            // elements out of order. We need to re-sort the elements to ensure that
            // canonical IDs are independent of insertion order.
            s.sort(), r.sort();
            for (let t = 0; t < s.length && t < r.length; ++t) {
                const n = b(s[t], r[t]);
                if (0 !== n) return n;
                const o = $t(e[s[t]], i[r[t]]);
                if (0 !== o) return o;
            }
            return b(s.length, r.length);
        }
        /**
 * Generates the canonical ID for the provided field value (as used in Target
 * serialization).
 */ (t.mapValue, n.mapValue);

      default:
        throw V();
    }
}

function Mt(t, n) {
    if ("string" == typeof t && "string" == typeof n && t.length === n.length) return b(t, n);
    const e = Bt(t), s = Bt(n), i = b(e.seconds, s.seconds);
    return 0 !== i ? i : b(e.nanos, s.nanos);
}

function Ut(t) {
    return qt(t);
}

function qt(t) {
    return "nullValue" in t ? "null" : "booleanValue" in t ? "" + t.booleanValue : "integerValue" in t ? "" + t.integerValue : "doubleValue" in t ? "" + t.doubleValue : "timestampValue" in t ? function(t) {
        const n = Bt(t);
        return `time(${n.seconds},${n.nanos})`;
    }(t.timestampValue) : "stringValue" in t ? t.stringValue : "bytesValue" in t ? jt(t.bytesValue).toBase64() : "referenceValue" in t ? (e = t.referenceValue, 
    B.W(e).toString()) : "geoPointValue" in t ? `geo(${(n = t.geoPointValue).latitude},${n.longitude})` : "arrayValue" in t ? function(t) {
        let n = "[", e = !0;
        for (const s of t.values || []) e ? e = !1 : n += ",", n += qt(s);
        return n + "]";
    }
    /**
 * Converts the possible Proto values for a timestamp value into a "seconds and
 * nanos" representation.
 */ (t.arrayValue) : "mapValue" in t ? function(t) {
        // Iteration order in JavaScript is not guaranteed. To ensure that we generate
        // matching canonical IDs for identical maps, we need to sort the keys.
        const n = Object.keys(t.fields || {}).sort();
        let e = "{", s = !0;
        for (const i of n) s ? s = !1 : e += ",", e += `${i}:${qt(t.fields[i])}`;
        return e + "}";
    }(t.mapValue) : V();
    var n, e;
}

function Bt(t) {
    // The json interface (for the browser) will return an iso timestamp string,
    // while the proto js library (for node) will return a
    // google.protobuf.Timestamp instance.
    if (y(!!t), "string" == typeof t) {
        // The date string can have higher precision (nanos) than the Date class
        // (millis), so we do some custom parsing here.
        // Parse the nanos right out of the string.
        let n = 0;
        const e = xt.exec(t);
        if (y(!!e), e[1]) {
            // Pad the fraction out to 9 digits (nanos).
            let t = e[1];
            t = (t + "000000000").substr(0, 9), n = Number(t);
        }
        // Parse the date to get the seconds.
                const s = new Date(t);
        return {
            seconds: Math.floor(s.getTime() / 1e3),
            nanos: n
        };
    }
    return {
        seconds: Qt(t.seconds),
        nanos: Qt(t.nanos)
    };
}

/**
 * Converts the possible Proto types for numbers into a JavaScript number.
 * Returns 0 if the value is not numeric.
 */ function Qt(t) {
    // TODO(bjornick): Handle int64 greater than 53 bits.
    return "number" == typeof t ? t : "string" == typeof t ? Number(t) : 0;
}

/** Converts the possible Proto types for Blobs into a ByteString. */ function jt(t) {
    return "string" == typeof t ? J.fromBase64String(t) : J.fromUint8Array(t);
}

/** Returns a reference value for the provided database and key. */ function Wt(t, n) {
    return {
        referenceValue: `projects/${t.projectId}/databases/${t.database}/documents/${n.path.k()}`
    };
}

/** Returns true if `value` is an IntegerValue . */ function Kt(t) {
    return !!t && "integerValue" in t;
}

/** Returns true if `value` is a DoubleValue. */
/** Returns true if `value` is an ArrayValue. */
function Gt(t) {
    return !!t && "arrayValue" in t;
}

/** Returns true if `value` is a NullValue. */ function zt(t) {
    return !!t && "nullValue" in t;
}

/** Returns true if `value` is NaN. */ function Ht(t) {
    return !!t && "doubleValue" in t && isNaN(Number(t.doubleValue));
}

/** Returns true if `value` is a MapValue. */ function Yt(t) {
    return !!t && "mapValue" in t;
}

/**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */ const Jt = (() => {
    const t = {
        asc: "ASCENDING",
        desc: "DESCENDING"
    };
    return t;
})(), Xt = (() => {
    const t = {
        "<": "LESS_THAN",
        "<=": "LESS_THAN_OR_EQUAL",
        ">": "GREATER_THAN",
        ">=": "GREATER_THAN_OR_EQUAL",
        "==": "EQUAL",
        "!=": "NOT_EQUAL",
        "array-contains": "ARRAY_CONTAINS",
        in: "IN",
        "not-in": "NOT_IN",
        "array-contains-any": "ARRAY_CONTAINS_ANY"
    };
    return t;
})();

/**
 * This class generates JsonObject values for the Datastore API suitable for
 * sending to either GRPC stub methods or via the JSON/HTTP REST API.
 *
 * The serializer supports both Protobuf.js and Proto3 JSON formats. By
 * setting `useProto3Json` to true, the serializer will use the Proto3 JSON
 * format.
 *
 * For a description of the Proto3 JSON format check
 * https://developers.google.com/protocol-buffers/docs/proto3#json
 *
 * TODO(klimt): We can remove the databaseId argument if we keep the full
 * resource name in documents.
 */
class Zt {
    constructor(t, n) {
        this.t = t, this.Qn = n;
    }
}

/**
 * Returns an IntegerValue for `value`.
 */
function tn(t) {
    return {
        integerValue: "" + t
    };
}

/**
 * Returns an DoubleValue for `value` that is encoded based the serializer's
 * `useProto3Json` setting.
 */ function nn(t, n) {
    if (t.Qn) {
        if (isNaN(n)) return {
            doubleValue: "NaN"
        };
        if (n === 1 / 0) return {
            doubleValue: "Infinity"
        };
        if (n === -1 / 0) return {
            doubleValue: "-Infinity"
        };
    }
    return {
        doubleValue: j(n) ? "-0" : n
    };
}

/**
 * Returns a value for a number that's appropriate to put into a proto.
 * The return value is an IntegerValue if it can safely represent the value,
 * otherwise a DoubleValue is returned.
 */ function en(t, n) {
    return function(t) {
        return "number" == typeof t && Number.isInteger(t) && !j(t) && t <= Number.MAX_SAFE_INTEGER && t >= Number.MIN_SAFE_INTEGER;
    }(n) ? tn(n) : nn(t, n);
}

/**
 * Returns a value for a Date that's appropriate to put into a proto.
 */ function sn(t, n) {
    if (t.Qn) {
        return `${new Date(1e3 * n.seconds).toISOString().replace(/\.\d*/, "").replace("Z", "")}.${("000000000" + n.nanoseconds).slice(-9)}Z`;
    }
    return {
        seconds: "" + n.seconds,
        nanos: n.nanoseconds
    };
}

/**
 * Returns a value for bytes that's appropriate to put in a proto.
 *
 * Visible for testing.
 */
function rn(t, n) {
    return t.Qn ? n.toBase64() : n.toUint8Array();
}

/**
 * Returns a ByteString based on the proto string value.
 */ function on(t, n) {
    return sn(t, n.P());
}

function un(t) {
    return y(!!t), L.I(function(t) {
        const n = Bt(t);
        return new k(n.seconds, n.nanos);
    }(t));
}

function cn(t, n) {
    return function(t) {
        return new M([ "projects", t.projectId, "databases", t.database ]);
    }(t).child("documents").child(n).k();
}

function an(t) {
    const n = M.L(t);
    return y(xn(n)), n;
}

function hn(t, n) {
    return cn(t.t, n.path);
}

function ln(t, n) {
    const e = an(n);
    if (e.get(1) !== t.t.projectId) throw new O(x.INVALID_ARGUMENT, "Tried to deserialize key from different project: " + e.get(1) + " vs " + t.t.projectId);
    if (e.get(3) !== t.t.database) throw new O(x.INVALID_ARGUMENT, "Tried to deserialize key from different database: " + e.get(3) + " vs " + t.t.database);
    return new B(dn(e));
}

function _n(t, n) {
    return cn(t.t, n);
}

function fn(t) {
    return new M([ "projects", t.t.projectId, "databases", t.t.database ]).k();
}

function dn(t) {
    return y(t.length > 4 && "documents" === t.get(4)), t.v(5);
}

/** Creates a Document proto from key and fields (but no create/update time) */ function wn(t, n, e) {
    return {
        name: hn(t, n),
        fields: e.proto.mapValue.fields
    };
}

function En(t, n) {
    return "found" in n ? function(t, n) {
        y(!!n.found), n.found.name, n.found.updateTime;
        const e = ln(t, n.found.name), s = un(n.found.updateTime), i = new fe({
            mapValue: {
                fields: n.found.fields
            }
        });
        return new Te(e, s, i, {});
    }(t, n) : "missing" in n ? function(t, n) {
        y(!!n.missing), y(!!n.readTime);
        const e = ln(t, n.missing), s = un(n.readTime);
        return new me(e, s);
    }(t, n) : V();
}

function Tn(t, n) {
    let e;
    if ("targetChange" in n) {
        n.targetChange;
        // proto3 default value is unset in JSON (undefined), so use 'NO_CHANGE'
        // if unset
        const s = function(t) {
            return "NO_CHANGE" === t ? 0 /* NoChange */ : "ADD" === t ? 1 /* Added */ : "REMOVE" === t ? 2 /* Removed */ : "CURRENT" === t ? 3 /* Current */ : "RESET" === t ? 4 /* Reset */ : V();
        }(n.targetChange.targetChangeType || "NO_CHANGE"), i = n.targetChange.targetIds || [], r = function(t, n) {
            return t.Qn ? (y(void 0 === n || "string" == typeof n), J.fromBase64String(n || "")) : (y(void 0 === n || n instanceof Uint8Array), 
            J.fromUint8Array(n || new Uint8Array));
        }(t, n.targetChange.resumeToken), o = n.targetChange.cause, u = o && function(t) {
            const n = void 0 === t.code ? x.UNKNOWN : st(t.code);
            return new O(n, t.message || "");
        }
        /**
 * Returns a value for a number (or null) that's appropriate to put into
 * a google.protobuf.Int32Value proto.
 * DO NOT USE THIS FOR ANYTHING ELSE.
 * This method cheats. It's typed as returning "number" because that's what
 * our generated proto interfaces say Int32Value must be. But GRPC actually
 * expects a { value: <number> } struct.
 */ (o);
        e = new pt(s, i, r, u || null);
    } else if ("documentChange" in n) {
        n.documentChange;
        const s = n.documentChange;
        s.document, s.document.name, s.document.updateTime;
        const i = ln(t, s.document.name), r = un(s.document.updateTime), o = new fe({
            mapValue: {
                fields: s.document.fields
            }
        }), u = new Te(i, r, o, {}), c = s.targetIds || [], a = s.removedTargetIds || [];
        e = new yt(c, a, u.key, u);
    } else if ("documentDelete" in n) {
        n.documentDelete;
        const s = n.documentDelete;
        s.document;
        const i = ln(t, s.document), r = s.readTime ? un(s.readTime) : L.min(), o = new me(i, r), u = s.removedTargetIds || [];
        e = new yt([], u, o.key, o);
    } else if ("documentRemove" in n) {
        n.documentRemove;
        const s = n.documentRemove;
        s.document;
        const i = ln(t, s.document), r = s.removedTargetIds || [];
        e = new yt([], r, i, null);
    } else {
        if (!("filter" in n)) return V();
        {
            n.filter;
            const t = n.filter;
            t.targetId;
            const s = t.count || 0, i = new Z(s), r = t.targetId;
            e = new gt(r, i);
        }
    }
    return e;
}

function mn(t, n) {
    let e;
    if (n instanceof re) e = {
        update: wn(t, n.key, n.value)
    }; else if (n instanceof le) e = {
        delete: hn(t, n.key)
    }; else if (n instanceof oe) e = {
        update: wn(t, n.key, n.data),
        updateMask: Fn(n.jn)
    }; else if (n instanceof ce) e = {
        transform: {
            document: hn(t, n.key),
            fieldTransforms: n.fieldTransforms.map((t => function(t, n) {
                const e = n.transform;
                if (e instanceof Mn) return {
                    fieldPath: n.field.k(),
                    setToServerValue: "REQUEST_TIME"
                };
                if (e instanceof Un) return {
                    fieldPath: n.field.k(),
                    appendMissingElements: {
                        values: e.elements
                    }
                };
                if (e instanceof Bn) return {
                    fieldPath: n.field.k(),
                    removeAllFromArray: {
                        values: e.elements
                    }
                };
                if (e instanceof jn) return {
                    fieldPath: n.field.k(),
                    increment: e.Wn
                };
                throw V();
            }(0, t)))
        }
    }; else {
        if (!(n instanceof _e)) return V();
        e = {
            verify: hn(t, n.key)
        };
    }
    return n.Gn.Kn || (e.currentDocument = function(t, n) {
        return void 0 !== n.updateTime ? {
            updateTime: on(t, n.updateTime)
        } : void 0 !== n.exists ? {
            exists: n.exists
        } : V();
    }(t, n.Gn)), e;
}

function In(t, n) {
    return t && t.length > 0 ? (y(void 0 !== n), t.map((t => function(t, n) {
        // NOTE: Deletes don't have an updateTime.
        let e = t.updateTime ? un(t.updateTime) : un(n);
        e.isEqual(L.min()) && (
        // The Firestore Emulator currently returns an update time of 0 for
        // deletes of non-existing documents (rather than null). This breaks the
        // test "get deleted doc while offline with source=cache" as NoDocuments
        // with version 0 are filtered by IndexedDb's RemoteDocumentCache.
        // TODO(#2149): Remove this when Emulator is fixed
        e = un(n));
        let s = null;
        return t.transformResults && t.transformResults.length > 0 && (s = t.transformResults), 
        new Yn(e, s);
    }(t, n)))) : [];
}

function An(t, n) {
    return {
        documents: [ _n(t, n.path) ]
    };
}

function Rn(t, n) {
    // Dissect the path into parent, collectionId, and optional key filter.
    const e = {
        structuredQuery: {}
    }, s = n.path;
    null !== n.collectionGroup ? (e.parent = _n(t, s), e.structuredQuery.from = [ {
        collectionId: n.collectionGroup,
        allDescendants: !0
    } ]) : (e.parent = _n(t, s.C()), e.structuredQuery.from = [ {
        collectionId: s.D()
    } ]);
    const i = function(t) {
        if (0 === t.length) return;
        const n = t.map((t => 
        // visible for testing
        function(t) {
            if ("==" /* EQUAL */ === t.op) {
                if (Ht(t.value)) return {
                    unaryFilter: {
                        field: Cn(t.field),
                        op: "IS_NAN"
                    }
                };
                if (zt(t.value)) return {
                    unaryFilter: {
                        field: Cn(t.field),
                        op: "IS_NULL"
                    }
                };
            } else if ("!=" /* NOT_EQUAL */ === t.op) {
                if (Ht(t.value)) return {
                    unaryFilter: {
                        field: Cn(t.field),
                        op: "IS_NOT_NAN"
                    }
                };
                if (zt(t.value)) return {
                    unaryFilter: {
                        field: Cn(t.field),
                        op: "IS_NOT_NULL"
                    }
                };
            }
            return {
                fieldFilter: {
                    field: Cn(t.field),
                    op: bn(t.op),
                    value: t.value
                }
            };
        }(t)));
        if (1 === n.length) return n[0];
        return {
            compositeFilter: {
                op: "AND",
                filters: n
            }
        };
    }(n.filters);
    i && (e.structuredQuery.where = i);
    const r = function(t) {
        if (0 === t.length) return;
        return t.map((t => 
        // visible for testing
        function(t) {
            return {
                field: Cn(t.field),
                direction: vn(t.dir)
            };
        }(t)));
    }(n.orderBy);
    r && (e.structuredQuery.orderBy = r);
    const o = function(t, n) {
        return t.Qn || Q(n) ? n : {
            value: n
        };
    }
    /**
 * Returns a number (or null) from a google.protobuf.Int32Value proto.
 */ (t, n.limit);
    return null !== o && (e.structuredQuery.limit = o), n.startAt && (e.structuredQuery.startAt = gn(n.startAt)), 
    n.endAt && (e.structuredQuery.endAt = gn(n.endAt)), e;
}

function Pn(t) {
    let n = function(t) {
        const n = an(t);
        // In v1beta1 queries for collections at the root did not have a trailing
        // "/documents". In v1 all resource paths contain "/documents". Preserve the
        // ability to read the v1beta1 form for compatibility with queries persisted
        // in the local target cache.
                return 4 === n.length ? M.$() : dn(n);
    }(t.parent);
    const e = t.structuredQuery, s = e.from ? e.from.length : 0;
    let i = null;
    if (s > 0) {
        y(1 === s);
        const t = e.from[0];
        t.allDescendants ? i = t.collectionId : n = n.child(t.collectionId);
    }
    let r = [];
    e.where && (r = yn(e.where));
    let o = [];
    e.orderBy && (o = e.orderBy.map((t => function(t) {
        return new Je(Sn(t.field), 
        // visible for testing
        function(t) {
            switch (t) {
              case "ASCENDING":
                return "asc" /* ASCENDING */;

              case "DESCENDING":
                return "desc" /* DESCENDING */;

              default:
                return;
            }
        }
        // visible for testing
        (t.direction));
    }(t))));
    let u = null;
    e.limit && (u = function(t) {
        let n;
        return n = "object" == typeof t ? t.value : t, Q(n) ? null : n;
    }(e.limit));
    let c = null;
    e.startAt && (c = pn(e.startAt));
    let a = null;
    return e.endAt && (a = pn(e.endAt)), 
    /** Creates a new Query instance with the options provided. */
    function(t, n, e, s, i, r, o, u) {
        return new Ae(t, n, e, s, i, r, o, u);
    }
    /** Creates a new Query for a query that matches all documents at `path` */ (n, i, o, r, u, "F" /* First */ , c, a);
}

function Vn(t, n) {
    const e = function(t, n) {
        switch (n) {
          case 0 /* Listen */ :
            return null;

          case 1 /* ExistenceFilterMismatch */ :
            return "existence-filter-mismatch";

          case 2 /* LimboResolution */ :
            return "limbo-document";

          default:
            return V();
        }
    }(0, n.tt);
    return null == e ? null : {
        "goog-listen-tags": e
    };
}

function yn(t) {
    return t ? void 0 !== t.unaryFilter ? [ Nn(t) ] : void 0 !== t.fieldFilter ? [ Dn(t) ] : void 0 !== t.compositeFilter ? t.compositeFilter.filters.map((t => yn(t))).reduce(((t, n) => t.concat(n))) : V() : [];
}

function gn(t) {
    return {
        before: t.before,
        values: t.position
    };
}

function pn(t) {
    const n = !!t.before, e = t.values || [];
    return new Ge(e, n);
}

// visible for testing
function vn(t) {
    return Jt[t];
}

function bn(t) {
    return Xt[t];
}

function Cn(t) {
    return {
        fieldPath: t.k()
    };
}

function Sn(t) {
    return q.B(t.fieldPath);
}

function Dn(t) {
    return Le.create(Sn(t.fieldFilter.field), function(t) {
        switch (t) {
          case "EQUAL":
            return "==" /* EQUAL */;

          case "NOT_EQUAL":
            return "!=" /* NOT_EQUAL */;

          case "GREATER_THAN":
            return ">" /* GREATER_THAN */;

          case "GREATER_THAN_OR_EQUAL":
            return ">=" /* GREATER_THAN_OR_EQUAL */;

          case "LESS_THAN":
            return "<" /* LESS_THAN */;

          case "LESS_THAN_OR_EQUAL":
            return "<=" /* LESS_THAN_OR_EQUAL */;

          case "ARRAY_CONTAINS":
            return "array-contains" /* ARRAY_CONTAINS */;

          case "IN":
            return "in" /* IN */;

          case "NOT_IN":
            return "not-in" /* NOT_IN */;

          case "ARRAY_CONTAINS_ANY":
            return "array-contains-any" /* ARRAY_CONTAINS_ANY */;

          case "OPERATOR_UNSPECIFIED":
          default:
            return V();
        }
    }(t.fieldFilter.op), t.fieldFilter.value);
}

function Nn(t) {
    switch (t.unaryFilter.op) {
      case "IS_NAN":
        const n = Sn(t.unaryFilter.field);
        return Le.create(n, "==" /* EQUAL */ , {
            doubleValue: NaN
        });

      case "IS_NULL":
        const e = Sn(t.unaryFilter.field);
        return Le.create(e, "==" /* EQUAL */ , {
            nullValue: "NULL_VALUE"
        });

      case "IS_NOT_NAN":
        const s = Sn(t.unaryFilter.field);
        return Le.create(s, "!=" /* NOT_EQUAL */ , {
            doubleValue: NaN
        });

      case "IS_NOT_NULL":
        const i = Sn(t.unaryFilter.field);
        return Le.create(i, "!=" /* NOT_EQUAL */ , {
            nullValue: "NULL_VALUE"
        });

      case "OPERATOR_UNSPECIFIED":
      default:
        return V();
    }
}

function Fn(t) {
    const n = [];
    return t.fields.forEach((t => n.push(t.k()))), {
        fieldPaths: n
    };
}

function xn(t) {
    // Resource names have at least 4 components (project ID, database ID)
    return t.length >= 4 && "projects" === t.get(0) && "databases" === t.get(2);
}

/**
 * @license
 * Copyright 2018 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/** Represents a transform within a TransformMutation. */ class On {
    constructor() {
        // Make sure that the structural type of `TransformOperation` is unique.
        // See https://github.com/microsoft/TypeScript/issues/5451
        this.zn = void 0;
    }
}

/**
 * Computes the local transform result against the provided `previousValue`,
 * optionally using the provided localWriteTime.
 */ function kn(t, n, e) {
    return t instanceof Mn ? function(t, n) {
        const e = {
            fields: {
                __type__: {
                    stringValue: "server_timestamp"
                },
                __local_write_time__: {
                    timestampValue: {
                        seconds: t.seconds,
                        nanos: t.nanoseconds
                    }
                }
            }
        };
        return n && (e.fields.__previous_value__ = n), {
            mapValue: e
        };
    }(e, n) : t instanceof Un ? qn(t, n) : t instanceof Bn ? Qn(t, n) : function(t, n) {
        // PORTING NOTE: Since JavaScript's integer arithmetic is limited to 53 bit
        // precision and resolves overflows by reducing precision, we do not
        // manually cap overflows at 2^63.
        const e = $n(t, n), s = Wn(e) + Wn(t.Wn);
        return Kt(e) && Kt(t.Wn) ? tn(s) : nn(t.serializer, s);
    }(t, n);
}

/**
 * Computes a final transform result after the transform has been acknowledged
 * by the server, potentially using the server-provided transformResult.
 */ function Ln(t, n, e) {
    // The server just sends null as the transform result for array operations,
    // so we have to calculate a result the same as we do for local
    // applications.
    return t instanceof Un ? qn(t, n) : t instanceof Bn ? Qn(t, n) : e;
}

/**
 * If this transform operation is not idempotent, returns the base value to
 * persist for this transform. If a base value is returned, the transform
 * operation is always applied to this base value, even if document has
 * already been updated.
 *
 * Base values provide consistent behavior for non-idempotent transforms and
 * allow us to return the same latency-compensated value even if the backend
 * has already applied the transform operation. The base value is null for
 * idempotent transforms, as they can be re-played even if the backend has
 * already applied them.
 *
 * @return a base value to store along with the mutation, or null for
 * idempotent transforms.
 */ function $n(t, n) {
    return t instanceof jn ? Kt(e = n) || function(t) {
        return !!t && "doubleValue" in t;
    }
    /** Returns true if `value` is either an IntegerValue or a DoubleValue. */ (e) ? n : {
        integerValue: 0
    } : null;
    var e;
}

/** Transforms a value into a server-generated timestamp. */
class Mn extends On {}

/** Transforms an array value via a union operation. */ class Un extends On {
    constructor(t) {
        super(), this.elements = t;
    }
}

function qn(t, n) {
    const e = Kn(n);
    for (const n of t.elements) e.some((t => kt(t, n))) || e.push(n);
    return {
        arrayValue: {
            values: e
        }
    };
}

/** Transforms an array value via a remove operation. */ class Bn extends On {
    constructor(t) {
        super(), this.elements = t;
    }
}

function Qn(t, n) {
    let e = Kn(n);
    for (const n of t.elements) e = e.filter((t => !kt(t, n)));
    return {
        arrayValue: {
            values: e
        }
    };
}

/**
 * Implements the backend semantics for locally computed NUMERIC_ADD (increment)
 * transforms. Converts all field values to integers or doubles, but unlike the
 * backend does not cap integer values at 2^63. Instead, JavaScript number
 * arithmetic is used and precision loss can occur for values greater than 2^53.
 */ class jn extends On {
    constructor(t, n) {
        super(), this.serializer = t, this.Wn = n;
    }
}

function Wn(t) {
    return Qt(t.integerValue || t.doubleValue);
}

function Kn(t) {
    return Gt(t) && t.arrayValue.values ? t.arrayValue.values.slice() : [];
}

/**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/**
 * Provides a set of fields that can be used to partially patch a document.
 * FieldMask is used in conjunction with ObjectValue.
 * Examples:
 *   foo - Overwrites foo entirely with the provided value. If foo is not
 *         present in the companion ObjectValue, the field is deleted.
 *   foo.bar - Overwrites only the field bar of the object foo.
 *             If foo is not an object, foo is replaced with an object
 *             containing foo
 */ class Gn {
    constructor(t) {
        this.fields = t, 
        // TODO(dimond): validation of FieldMask
        // Sort the field mask to support `FieldMask.isEqual()` and assert below.
        t.sort(q.g);
    }
    /**
     * Verifies that `fieldPath` is included by at least one field in this field
     * mask.
     *
     * This is an O(n) operation, where `n` is the size of the field mask.
     */    Hn(t) {
        for (const n of this.fields) if (n.N(t)) return !0;
        return !1;
    }
    isEqual(t) {
        return C(this.fields, t.fields, ((t, n) => t.isEqual(n)));
    }
}

/** A field path and the TransformOperation to perform upon it. */ class zn {
    constructor(t, n) {
        this.field = t, this.transform = n;
    }
}

function Hn(t, n) {
    return t.field.isEqual(n.field) && function(t, n) {
        return t instanceof Un && n instanceof Un || t instanceof Bn && n instanceof Bn ? C(t.elements, n.elements, kt) : t instanceof jn && n instanceof jn ? kt(t.Wn, n.Wn) : t instanceof Mn && n instanceof Mn;
    }(t.transform, n.transform);
}

/** The result of successfully applying a mutation to the backend. */ class Yn {
    constructor(
    /**
     * The version at which the mutation was committed:
     *
     * - For most operations, this is the updateTime in the WriteResult.
     * - For deletes, the commitTime of the WriteResponse (because deletes are
     *   not stored and have no updateTime).
     *
     * Note that these versions can be different: No-op writes will not change
     * the updateTime even though the commitTime advances.
     */
    t, 
    /**
     * The resulting fields returned from the backend after a
     * TransformMutation has been committed. Contains one FieldValue for each
     * FieldTransform that was in the mutation.
     *
     * Will be null if the mutation was not a TransformMutation.
     */
    n) {
        this.version = t, this.transformResults = n;
    }
}

/**
 * Encodes a precondition for a mutation. This follows the model that the
 * backend accepts with the special case of an explicit "empty" precondition
 * (meaning no precondition).
 */ class Jn {
    constructor(t, n) {
        this.updateTime = t, this.exists = n;
    }
    /** Creates a new empty Precondition. */    static Yn() {
        return new Jn;
    }
    /** Creates a new Precondition with an exists flag. */    static exists(t) {
        return new Jn(void 0, t);
    }
    /** Creates a new Precondition based on a version a document exists at. */    static updateTime(t) {
        return new Jn(t);
    }
    /** Returns whether this Precondition is empty. */    get Kn() {
        return void 0 === this.updateTime && void 0 === this.exists;
    }
    isEqual(t) {
        return this.exists === t.exists && (this.updateTime ? !!t.updateTime && this.updateTime.isEqual(t.updateTime) : !t.updateTime);
    }
}

/**
 * Returns true if the preconditions is valid for the given document
 * (or null if no document is available).
 */ function Xn(t, n) {
    return void 0 !== t.updateTime ? n instanceof Te && n.version.isEqual(t.updateTime) : void 0 === t.exists || t.exists === n instanceof Te;
}

/**
 * A mutation describes a self-contained change to a document. Mutations can
 * create, replace, delete, and update subsets of documents.
 *
 * Mutations not only act on the value of the document but also its version.
 *
 * For local mutations (mutations that haven't been committed yet), we preserve
 * the existing version for Set, Patch, and Transform mutations. For Delete
 * mutations, we reset the version to 0.
 *
 * Here's the expected transition table.
 *
 * MUTATION           APPLIED TO            RESULTS IN
 *
 * SetMutation        Document(v3)          Document(v3)
 * SetMutation        NoDocument(v3)        Document(v0)
 * SetMutation        null                  Document(v0)
 * PatchMutation      Document(v3)          Document(v3)
 * PatchMutation      NoDocument(v3)        NoDocument(v3)
 * PatchMutation      null                  null
 * TransformMutation  Document(v3)          Document(v3)
 * TransformMutation  NoDocument(v3)        NoDocument(v3)
 * TransformMutation  null                  null
 * DeleteMutation     Document(v3)          NoDocument(v0)
 * DeleteMutation     NoDocument(v3)        NoDocument(v0)
 * DeleteMutation     null                  NoDocument(v0)
 *
 * For acknowledged mutations, we use the updateTime of the WriteResponse as
 * the resulting version for Set, Patch, and Transform mutations. As deletes
 * have no explicit update time, we use the commitTime of the WriteResponse for
 * Delete mutations.
 *
 * If a mutation is acknowledged by the backend but fails the precondition check
 * locally, we return an `UnknownDocument` and rely on Watch to send us the
 * updated version.
 *
 * Note that TransformMutations don't create Documents (in the case of being
 * applied to a NoDocument), even though they would on the backend. This is
 * because the client always combines the TransformMutation with a SetMutation
 * or PatchMutation and we only want to apply the transform if the prior
 * mutation resulted in a Document (always true for a SetMutation, but not
 * necessarily for a PatchMutation).
 *
 * ## Subclassing Notes
 *
 * Subclasses of Mutation need to implement applyToRemoteDocument() and
 * applyToLocalView() to implement the actual behavior of applying the mutation
 * to some source document.
 */ class Zn {}

/**
 * Applies this mutation to the given MaybeDocument or null for the purposes
 * of computing a new remote document. If the input document doesn't match the
 * expected state (e.g. it is null or outdated), an `UnknownDocument` can be
 * returned.
 *
 * @param mutation The mutation to apply.
 * @param maybeDoc The document to mutate. The input document can be null if
 *     the client has no knowledge of the pre-mutation state of the document.
 * @param mutationResult The result of applying the mutation from the backend.
 * @return The mutated document. The returned document may be an
 *     UnknownDocument if the mutation could not be applied to the locally
 *     cached base document.
 */ function te(t, n, e) {
    return t instanceof re ? function(t, n, e) {
        // Unlike applySetMutationToLocalView, if we're applying a mutation to a
        // remote document the server has accepted the mutation so the precondition
        // must have held.
        return new Te(t.key, e.version, t.value, {
            hasCommittedMutations: !0
        });
    }(t, 0, e) : t instanceof oe ? function(t, n, e) {
        if (!Xn(t.Gn, n)) 
        // Since the mutation was not rejected, we know that the  precondition
        // matched on the backend. We therefore must not have the expected version
        // of the document in our cache and return an UnknownDocument with the
        // known updateTime.
        return new Ie(t.key, e.version);
        const s = ue(t, n);
        return new Te(t.key, e.version, s, {
            hasCommittedMutations: !0
        });
    }(t, n, e) : t instanceof ce ? function(t, n, e) {
        if (y(null != e.transformResults), !Xn(t.Gn, n)) 
        // Since the mutation was not rejected, we know that the  precondition
        // matched on the backend. We therefore must not have the expected version
        // of the document in our cache and return an UnknownDocument with the
        // known updateTime.
        return new Ie(t.key, e.version);
        const s = ae(t, n), i = 
        /**
 * Creates a list of "transform results" (a transform result is a field value
 * representing the result of applying a transform) for use after a
 * TransformMutation has been acknowledged by the server.
 *
 * @param fieldTransforms The field transforms to apply the result to.
 * @param baseDoc The document prior to applying this mutation batch.
 * @param serverTransformResults The transform results received by the server.
 * @return The transform results list.
 */
        function(t, n, e) {
            const s = [];
            y(t.length === e.length);
            for (let i = 0; i < e.length; i++) {
                const r = t[i], o = r.transform;
                let u = null;
                n instanceof Te && (u = n.field(r.field)), s.push(Ln(o, u, e[i]));
            }
            return s;
        }
        /**
 * Creates a list of "transform results" (a transform result is a field value
 * representing the result of applying a transform) for use when applying a
 * TransformMutation locally.
 *
 * @param fieldTransforms The field transforms to apply the result to.
 * @param localWriteTime The local time of the transform mutation (used to
 *     generate ServerTimestampValues).
 * @param maybeDoc The current state of the document after applying all
 *     previous mutations.
 * @param baseDoc The document prior to applying this mutation batch.
 * @return The transform results list.
 */ (t.fieldTransforms, n, e.transformResults), r = e.version, o = he(t, s.data(), i);
        return new Te(t.key, r, o, {
            hasCommittedMutations: !0
        });
    }(t, n, e) : function(t, n, e) {
        // Unlike applyToLocalView, if we're applying a mutation to a remote
        // document the server has accepted the mutation so the precondition must
        // have held.
        return new me(t.key, e.version, {
            hasCommittedMutations: !0
        });
    }(t, 0, e);
}

/**
 * Applies this mutation to the given MaybeDocument or null for the purposes
 * of computing the new local view of a document. Both the input and returned
 * documents can be null.
 *
 * @param mutation The mutation to apply.
 * @param maybeDoc The document to mutate. The input document can be null if
 *     the client has no knowledge of the pre-mutation state of the document.
 * @param baseDoc The state of the document prior to this mutation batch. The
 *     input document can be null if the client has no knowledge of the
 *     pre-mutation state of the document.
 * @param localWriteTime A timestamp indicating the local write time of the
 *     batch this mutation is a part of.
 * @return The mutated document. The returned document may be null, but only
 *     if maybeDoc was null and the mutation would not create a new document.
 */ function ne(t, n, e, s) {
    return t instanceof re ? function(t, n) {
        if (!Xn(t.Gn, n)) return n;
        const e = ie(n);
        return new Te(t.key, e, t.value, {
            Jn: !0
        });
    }
    /**
 * A mutation that modifies fields of the document at the given key with the
 * given values. The values are applied through a field mask:
 *
 *  * When a field is in both the mask and the values, the corresponding field
 *    is updated.
 *  * When a field is in neither the mask nor the values, the corresponding
 *    field is unmodified.
 *  * When a field is in the mask but not in the values, the corresponding field
 *    is deleted.
 *  * When a field is not in the mask but is in the values, the values map is
 *    ignored.
 */ (t, n) : t instanceof oe ? function(t, n) {
        if (!Xn(t.Gn, n)) return n;
        const e = ie(n), s = ue(t, n);
        return new Te(t.key, e, s, {
            Jn: !0
        });
    }
    /**
 * Patches the data of document if available or creates a new document. Note
 * that this does not check whether or not the precondition of this patch
 * holds.
 */ (t, n) : t instanceof ce ? function(t, n, e, s) {
        if (!Xn(t.Gn, n)) return n;
        const i = ae(t, n), r = function(t, n, e, s) {
            const i = [];
            for (const r of t) {
                const t = r.transform;
                let o = null;
                e instanceof Te && (o = e.field(r.field)), null === o && s instanceof Te && (
                // If the current document does not contain a value for the mutated
                // field, use the value that existed before applying this mutation
                // batch. This solves an edge case where a PatchMutation clears the
                // values in a nested map before the TransformMutation is applied.
                o = s.field(r.field)), i.push(kn(t, o, n));
            }
            return i;
        }(t.fieldTransforms, e, n, s), o = he(t, i.data(), r);
        return new Te(t.key, i.version, o, {
            Jn: !0
        });
    }(t, n, s, e) : function(t, n) {
        if (!Xn(t.Gn, n)) return n;
        return new me(t.key, L.min());
    }
    /**
 * A mutation that verifies the existence of the document at the given key with
 * the provided precondition.
 *
 * The `verify` operation is only used in Transactions, and this class serves
 * primarily to facilitate serialization into protos.
 */ (t, n);
}

/**
 * If this mutation is not idempotent, returns the base value to persist with
 * this mutation. If a base value is returned, the mutation is always applied
 * to this base value, even if document has already been updated.
 *
 * The base value is a sparse object that consists of only the document
 * fields for which this mutation contains a non-idempotent transformation
 * (e.g. a numeric increment). The provided value guarantees consistent
 * behavior for non-idempotent transforms and allow us to return the same
 * latency-compensated value even if the backend has already applied the
 * mutation. The base value is null for idempotent mutations, as they can be
 * re-played even if the backend has already applied them.
 *
 * @return a base value to store along with the mutation, or null for
 * idempotent mutations.
 */ function ee(t, n) {
    return t instanceof ce ? function(t, n) {
        let e = null;
        for (const s of t.fieldTransforms) {
            const t = n instanceof Te ? n.field(s.field) : void 0, i = $n(s.transform, t || null);
            null != i && (e = null == e ? (new de).set(s.field, i) : e.set(s.field, i));
        }
        return e ? e.Xn() : null;
    }
    /**
 * Asserts that the given MaybeDocument is actually a Document and verifies
 * that it matches the key for this mutation. Since we only support
 * transformations with precondition exists this method is guaranteed to be
 * safe.
 */ (t, n) : null;
}

function se(t, n) {
    return t.type === n.type && (!!t.key.isEqual(n.key) && (!!t.Gn.isEqual(n.Gn) && (0 /* Set */ === t.type ? t.value.isEqual(n.value) : 1 /* Patch */ === t.type ? t.data.isEqual(n.data) && t.jn.isEqual(n.jn) : 2 /* Transform */ !== t.type || C(t.fieldTransforms, t.fieldTransforms, ((t, n) => Hn(t, n))))));
}

/**
 * Returns the version from the given document for use as the result of a
 * mutation. Mutations are defined to return the version of the base document
 * only if it is an existing document. Deleted and unknown documents have a
 * post-mutation version of SnapshotVersion.min().
 */ function ie(t) {
    return t instanceof Te ? t.version : L.min();
}

/**
 * A mutation that creates or replaces the document at the given key with the
 * object value contents.
 */ class re extends Zn {
    constructor(t, n, e) {
        super(), this.key = t, this.value = n, this.Gn = e, this.type = 0 /* Set */;
    }
}

class oe extends Zn {
    constructor(t, n, e, s) {
        super(), this.key = t, this.data = n, this.jn = e, this.Gn = s, this.type = 1 /* Patch */;
    }
}

function ue(t, n) {
    let e;
    return e = n instanceof Te ? n.data() : fe.empty(), function(t, n) {
        const e = new de(n);
        return t.jn.fields.forEach((n => {
            if (!n.T()) {
                const s = t.data.field(n);
                null !== s ? e.set(n, s) : e.delete(n);
            }
        })), e.Xn();
    }
    /**
 * A mutation that modifies specific fields of the document with transform
 * operations. Currently the only supported transform is a server timestamp, but
 * IP Address, increment(n), etc. could be supported in the future.
 *
 * It is somewhat similar to a PatchMutation in that it patches specific fields
 * and has no effect when applied to a null or NoDocument (see comment on
 * Mutation for rationale).
 */ (t, e);
}

class ce extends Zn {
    constructor(t, n) {
        super(), this.key = t, this.fieldTransforms = n, this.type = 2 /* Transform */ , 
        // NOTE: We set a precondition of exists: true as a safety-check, since we
        // always combine TransformMutations with a SetMutation or PatchMutation which
        // (if successful) should end up with an existing document.
        this.Gn = Jn.exists(!0);
    }
}

function ae(t, n) {
    return n;
}

function he(t, n, e) {
    const s = new de(n);
    for (let n = 0; n < t.fieldTransforms.length; n++) {
        const i = t.fieldTransforms[n];
        s.set(i.field, e[n]);
    }
    return s.Xn();
}

/** A mutation that deletes the document at the given key. */ class le extends Zn {
    constructor(t, n) {
        super(), this.key = t, this.Gn = n, this.type = 3 /* Delete */;
    }
}

class _e extends Zn {
    constructor(t, n) {
        super(), this.key = t, this.Gn = n, this.type = 4 /* Verify */;
    }
}

/**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/**
 * An ObjectValue represents a MapValue in the Firestore Proto and offers the
 * ability to add and remove fields (via the ObjectValueBuilder).
 */ class fe {
    constructor(t) {
        this.proto = t;
    }
    static empty() {
        return new fe({
            mapValue: {}
        });
    }
    /**
     * Returns the value at the given path or null.
     *
     * @param path the path to search
     * @return The value at the path or if there it doesn't exist.
     */    field(t) {
        if (t.T()) return this.proto;
        {
            let n = this.proto;
            for (let e = 0; e < t.length - 1; ++e) {
                if (!n.mapValue.fields) return null;
                if (n = n.mapValue.fields[t.get(e)], !Yt(n)) return null;
            }
            return n = (n.mapValue.fields || {})[t.D()], n || null;
        }
    }
    isEqual(t) {
        return kt(this.proto, t.proto);
    }
}

/**
 * An ObjectValueBuilder provides APIs to set and delete fields from an
 * ObjectValue.
 */ class de {
    /**
     * @param baseObject The object to mutate.
     */
    constructor(t = fe.empty()) {
        this.Zn = t, 
        /** A map that contains the accumulated changes in this builder. */
        this.te = new Map;
    }
    /**
     * Sets the field to the provided value.
     *
     * @param path The field path to set.
     * @param value The value to set.
     * @return The current Builder instance.
     */    set(t, n) {
        return this.ne(t, n), this;
    }
    /**
     * Removes the field at the specified path. If there is no field at the
     * specified path, nothing is changed.
     *
     * @param path The field path to remove.
     * @return The current Builder instance.
     */    delete(t) {
        return this.ne(t, null), this;
    }
    /**
     * Adds `value` to the overlay map at `path`. Creates nested map entries if
     * needed.
     */    ne(t, n) {
        let e = this.te;
        for (let n = 0; n < t.length - 1; ++n) {
            const s = t.get(n);
            let i = e.get(s);
            i instanceof Map ? 
            // Re-use a previously created map
            e = i : i && 10 /* ObjectValue */ === Ot(i) ? (
            // Convert the existing Protobuf MapValue into a map
            i = new Map(Object.entries(i.mapValue.fields || {})), e.set(s, i), e = i) : (
            // Create an empty map to represent the current nesting level
            i = new Map, e.set(s, i), e = i);
        }
        e.set(t.D(), n);
    }
    /** Returns an ObjectValue with all mutations applied. */    Xn() {
        const t = this.ee(q.$(), this.te);
        return null != t ? new fe(t) : this.Zn;
    }
    /**
     * Applies any overlays from `currentOverlays` that exist at `currentPath`
     * and returns the merged data at `currentPath` (or null if there were no
     * changes).
     *
     * @param currentPath The path at the current nesting level. Can be set to
     * FieldValue.emptyPath() to represent the root.
     * @param currentOverlays The overlays at the current nesting level in the
     * same format as `overlayMap`.
     * @return The merged data at `currentPath` or null if no modifications
     * were applied.
     */    ee(t, n) {
        let e = !1;
        const s = this.Zn.field(t), i = Yt(s) ? // If there is already data at the current path, base our
        Object.assign({}, s.mapValue.fields) : {};
        return n.forEach(((n, s) => {
            if (n instanceof Map) {
                const r = this.ee(t.child(s), n);
                null != r && (i[s] = r, e = !0);
            } else null !== n ? (i[s] = n, e = !0) : i.hasOwnProperty(s) && (delete i[s], e = !0);
        })), e ? {
            mapValue: {
                fields: i
            }
        } : null;
    }
}

/**
 * Returns a FieldMask built from all fields in a MapValue.
 */ function we(t) {
    const n = [];
    return D(t.fields || {}, ((t, e) => {
        const s = new q([ t ]);
        if (Yt(e)) {
            const t = we(e.mapValue).fields;
            if (0 === t.length) 
            // Preserve the empty map by adding it to the FieldMask.
            n.push(s); else 
            // For nested and non-empty ObjectValues, add the FieldPath of the
            // leaf nodes.
            for (const e of t) n.push(s.child(e));
        } else 
        // For nested and non-empty ObjectValues, add the FieldPath of the leaf
        // nodes.
        n.push(s);
    })), new Gn(n);
}

/**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/**
 * The result of a lookup for a given path may be an existing document or a
 * marker that this document does not exist at a given version.
 */ class Ee {
    constructor(t, n) {
        this.key = t, this.version = n;
    }
}

/**
 * Represents a document in Firestore with a key, version, data and whether the
 * data has local mutations applied to it.
 */ class Te extends Ee {
    constructor(t, n, e, s) {
        super(t, n), this.se = e, this.Jn = !!s.Jn, this.hasCommittedMutations = !!s.hasCommittedMutations;
    }
    field(t) {
        return this.se.field(t);
    }
    data() {
        return this.se;
    }
    ie() {
        return this.se.proto;
    }
    isEqual(t) {
        return t instanceof Te && this.key.isEqual(t.key) && this.version.isEqual(t.version) && this.Jn === t.Jn && this.hasCommittedMutations === t.hasCommittedMutations && this.se.isEqual(t.se);
    }
    toString() {
        return `Document(${this.key}, ${this.version}, ${this.se.toString()}, {hasLocalMutations: ${this.Jn}}), {hasCommittedMutations: ${this.hasCommittedMutations}})`;
    }
    get hasPendingWrites() {
        return this.Jn || this.hasCommittedMutations;
    }
}

/**
 * Compares the value for field `field` in the provided documents. Throws if
 * the field does not exist in both documents.
 */
/**
 * A class representing a deleted document.
 * Version is set to 0 if we don't point to any specific time, otherwise it
 * denotes time we know it didn't exist at.
 */
class me extends Ee {
    constructor(t, n, e) {
        super(t, n), this.hasCommittedMutations = !(!e || !e.hasCommittedMutations);
    }
    toString() {
        return `NoDocument(${this.key}, ${this.version})`;
    }
    get hasPendingWrites() {
        return this.hasCommittedMutations;
    }
    isEqual(t) {
        return t instanceof me && t.hasCommittedMutations === this.hasCommittedMutations && t.version.isEqual(this.version) && t.key.isEqual(this.key);
    }
}

/**
 * A class representing an existing document whose data is unknown (e.g. a
 * document that was updated without a known base document).
 */ class Ie extends Ee {
    toString() {
        return `UnknownDocument(${this.key}, ${this.version})`;
    }
    get hasPendingWrites() {
        return !0;
    }
    isEqual(t) {
        return t instanceof Ie && t.version.isEqual(this.version) && t.key.isEqual(this.key);
    }
}

/**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/**
 * Query encapsulates all the query attributes we support in the SDK. It can
 * be run against the LocalStore, as well as be converted to a `Target` to
 * query the RemoteStore results.
 *
 * Visible for testing.
 */ class Ae {
    /**
     * Initializes a Query with a path and optional additional query constraints.
     * Path must currently be empty if this is a collection group query.
     */
    constructor(t, n = null, e = [], s = [], i = null, r = "F" /* First */ , o = null, u = null) {
        this.path = t, this.collectionGroup = n, this.re = e, this.filters = s, this.limit = i, 
        this.limitType = r, this.startAt = o, this.endAt = u, this.oe = null, 
        // The corresponding `Target` of this `Query` instance.
        this.ue = null, this.startAt, this.endAt;
    }
}

function Re(t) {
    return new Ae(t);
}

/**
 * Helper to convert a collection group query into a collection query at a
 * specific path. This is used when executing collection group queries, since
 * we have to split the query into a set of collection queries at multiple
 * paths.
 */ function Pe(t) {
    return !Q(t.limit) && "F" /* First */ === t.limitType;
}

function Ve(t) {
    return !Q(t.limit) && "L" /* Last */ === t.limitType;
}

function ye(t) {
    return t.re.length > 0 ? t.re[0].field : null;
}

function ge(t) {
    for (const n of t.filters) if (n.ce()) return n.field;
    return null;
}

/**
 * Checks if any of the provided Operators are included in the query and
 * returns the first one that is, or null if none are.
 */
/**
 * Returns whether the query matches a collection group rather than a specific
 * collection.
 */
function pe(t) {
    return null !== t.collectionGroup;
}

/**
 * Returns the implicit order by constraint that is used to execute the Query,
 * which can be different from the order by constraints the user provided (e.g.
 * the SDK and backend always orders by `__name__`).
 */ function ve(t) {
    const n = g(t);
    if (null === n.oe) {
        n.oe = [];
        const t = ge(n), e = ye(n);
        if (null !== t && null === e) 
        // In order to implicitly add key ordering, we must also add the
        // inequality filter field for it to be a valid query.
        // Note that the default inequality field and key ordering is ascending.
        t.U() || n.oe.push(new Je(t)), n.oe.push(new Je(q.q(), "asc" /* ASCENDING */)); else {
            let t = !1;
            for (const e of n.re) n.oe.push(e), e.field.U() && (t = !0);
            if (!t) {
                // The order of the implicit key ordering always matches the last
                // explicit order by
                const t = n.re.length > 0 ? n.re[n.re.length - 1].dir : "asc" /* ASCENDING */;
                n.oe.push(new Je(q.q(), t));
            }
        }
    }
    return n.oe;
}

/**
 * Converts this `Query` instance to it's corresponding `Target` representation.
 */ function be(t) {
    const n = g(t);
    if (!n.ue) if ("F" /* First */ === n.limitType) n.ue = K(n.path, n.collectionGroup, ve(n), n.filters, n.limit, n.startAt, n.endAt); else {
        // Flip the orderBy directions since we want the last results
        const t = [];
        for (const e of ve(n)) {
            const n = "desc" /* DESCENDING */ === e.dir ? "asc" /* ASCENDING */ : "desc" /* DESCENDING */;
            t.push(new Je(e.field, n));
        }
        // We need to swap the cursors to match the now-flipped query ordering.
                const e = n.endAt ? new Ge(n.endAt.position, !n.endAt.before) : null, s = n.startAt ? new Ge(n.startAt.position, !n.startAt.before) : null;
        // Now return as a LimitType.First query.
        n.ue = K(n.path, n.collectionGroup, t, n.filters, n.limit, e, s);
    }
    return n.ue;
}

function Ce(t, n, e) {
    return new Ae(t.path, t.collectionGroup, t.re.slice(), t.filters.slice(), n, e, t.startAt, t.endAt);
}

function Se(t, n) {
    return new Ae(t.path, t.collectionGroup, t.re.slice(), t.filters.slice(), t.limit, t.limitType, n, t.endAt);
}

function De(t, n) {
    return new Ae(t.path, t.collectionGroup, t.re.slice(), t.filters.slice(), t.limit, t.limitType, t.startAt, n);
}

function Ne(t, n) {
    return H(be(t), be(n)) && t.limitType === n.limitType;
}

// TODO(b/29183165): This is used to get a unique string from a query to, for
// example, use as a dictionary key, but the implementation is subject to
// collisions. Make it collision-free.
function Fe(t) {
    return `${G(be(t))}|lt:${t.limitType}`;
}

function xe(t) {
    return `Query(target=${z(be(t))}; limitType=${t.limitType})`;
}

/** Returns whether `doc` matches the constraints of `query`. */ function Oe(t, n) {
    return function(t, n) {
        const e = n.key.path;
        return null !== t.collectionGroup ? n.key.K(t.collectionGroup) && t.path.N(e) : B.G(t.path) ? t.path.isEqual(e) : t.path.F(e);
    }
    /**
 * A document must have a value for every ordering clause in order to show up
 * in the results.
 */ (t, n) && function(t, n) {
        for (const e of t.re) 
        // order by key always matches
        if (!e.field.U() && null === n.field(e.field)) return !1;
        return !0;
    }(t, n) && function(t, n) {
        for (const e of t.filters) if (!e.matches(n)) return !1;
        return !0;
    }
    /** Makes sure a document is within the bounds, if provided. */ (t, n) && function(t, n) {
        if (t.startAt && !He(t.startAt, ve(t), n)) return !1;
        if (t.endAt && He(t.endAt, ve(t), n)) return !1;
        return !0;
    }
    /**
 * Returns a new comparator function that can be used to compare two documents
 * based on the Query's ordering constraint.
 */ (t, n);
}

function ke(t) {
    return (n, e) => {
        let s = !1;
        for (const i of ve(t)) {
            const t = Xe(i, n, e);
            if (0 !== t) return t;
            s = s || i.field.U();
        }
        return 0;
    };
}

class Le extends class {} {
    constructor(t, n, e) {
        super(), this.field = t, this.op = n, this.value = e;
    }
    /**
     * Creates a filter based on the provided arguments.
     */    static create(t, n, e) {
        return t.U() ? "in" /* IN */ === n || "not-in" /* NOT_IN */ === n ? this.ae(t, n, e) : new Me(t, n, e) : "array-contains" /* ARRAY_CONTAINS */ === n ? new Qe(t, e) : "in" /* IN */ === n ? new je(t, e) : "not-in" /* NOT_IN */ === n ? new We(t, e) : "array-contains-any" /* ARRAY_CONTAINS_ANY */ === n ? new Ke(t, e) : new Le(t, n, e);
    }
    static ae(t, n, e) {
        return "in" /* IN */ === n ? new Ue(t, e) : new qe(t, e);
    }
    matches(t) {
        const n = t.field(this.field);
        // Types do not have to match in NOT_EQUAL filters.
                return "!=" /* NOT_EQUAL */ === this.op ? null !== n && this.he($t(n, this.value)) : null !== n && Ot(this.value) === Ot(n) && this.he($t(n, this.value));
        // Only compare types with matching backend order (such as double and int).
        }
    he(t) {
        switch (this.op) {
          case "<" /* LESS_THAN */ :
            return t < 0;

          case "<=" /* LESS_THAN_OR_EQUAL */ :
            return t <= 0;

          case "==" /* EQUAL */ :
            return 0 === t;

          case "!=" /* NOT_EQUAL */ :
            return 0 !== t;

          case ">" /* GREATER_THAN */ :
            return t > 0;

          case ">=" /* GREATER_THAN_OR_EQUAL */ :
            return t >= 0;

          default:
            return V();
        }
    }
    ce() {
        return [ "<" /* LESS_THAN */ , "<=" /* LESS_THAN_OR_EQUAL */ , ">" /* GREATER_THAN */ , ">=" /* GREATER_THAN_OR_EQUAL */ , "!=" /* NOT_EQUAL */ , "not-in" /* NOT_IN */ ].indexOf(this.op) >= 0;
    }
}

function $e(t) {
    // TODO(b/29183165): Technically, this won't be unique if two values have
    // the same description, such as the int 3 and the string "3". So we should
    // add the types in here somehow, too.
    return t.field.k() + t.op.toString() + Ut(t.value);
}

class Me extends Le {
    constructor(t, n, e) {
        super(t, n, e), this.key = B.W(e.referenceValue);
    }
    matches(t) {
        const n = B.g(t.key, this.key);
        return this.he(n);
    }
}

/** Filter that matches on key fields within an array. */ class Ue extends Le {
    constructor(t, n) {
        super(t, "in" /* IN */ , n), this.keys = Be("in" /* IN */ , n);
    }
    matches(t) {
        return this.keys.some((n => n.isEqual(t.key)));
    }
}

/** Filter that matches on key fields not present within an array. */ class qe extends Le {
    constructor(t, n) {
        super(t, "not-in" /* NOT_IN */ , n), this.keys = Be("not-in" /* NOT_IN */ , n);
    }
    matches(t) {
        return !this.keys.some((n => n.isEqual(t.key)));
    }
}

function Be(t, n) {
    var e;
    return ((null === (e = n.arrayValue) || void 0 === e ? void 0 : e.values) || []).map((t => B.W(t.referenceValue)));
}

/** A Filter that implements the array-contains operator. */ class Qe extends Le {
    constructor(t, n) {
        super(t, "array-contains" /* ARRAY_CONTAINS */ , n);
    }
    matches(t) {
        const n = t.field(this.field);
        return Gt(n) && Lt(n.arrayValue, this.value);
    }
}

/** A Filter that implements the IN operator. */ class je extends Le {
    constructor(t, n) {
        super(t, "in" /* IN */ , n);
    }
    matches(t) {
        const n = t.field(this.field);
        return null !== n && Lt(this.value.arrayValue, n);
    }
}

/** A Filter that implements the not-in operator. */ class We extends Le {
    constructor(t, n) {
        super(t, "not-in" /* NOT_IN */ , n);
    }
    matches(t) {
        if (Lt(this.value.arrayValue, {
            nullValue: "NULL_VALUE"
        })) return !1;
        const n = t.field(this.field);
        return null !== n && !Lt(this.value.arrayValue, n);
    }
}

/** A Filter that implements the array-contains-any operator. */ class Ke extends Le {
    constructor(t, n) {
        super(t, "array-contains-any" /* ARRAY_CONTAINS_ANY */ , n);
    }
    matches(t) {
        const n = t.field(this.field);
        return !(!Gt(n) || !n.arrayValue.values) && n.arrayValue.values.some((t => Lt(this.value.arrayValue, t)));
    }
}

/**
 * Represents a bound of a query.
 *
 * The bound is specified with the given components representing a position and
 * whether it's just before or just after the position (relative to whatever the
 * query order is).
 *
 * The position represents a logical index position for a query. It's a prefix
 * of values for the (potentially implicit) order by clauses of a query.
 *
 * Bound provides a function to determine whether a document comes before or
 * after a bound. This is influenced by whether the position is just before or
 * just after the provided values.
 */ class Ge {
    constructor(t, n) {
        this.position = t, this.before = n;
    }
}

function ze(t) {
    // TODO(b/29183165): Make this collision robust.
    return `${t.before ? "b" : "a"}:${t.position.map((t => Ut(t))).join(",")}`;
}

/**
 * Returns true if a document sorts before a bound using the provided sort
 * order.
 */ function He(t, n, e) {
    let s = 0;
    for (let i = 0; i < t.position.length; i++) {
        const r = n[i], o = t.position[i];
        if (r.field.U()) s = B.g(B.W(o.referenceValue), e.key); else {
            s = $t(o, e.field(r.field));
        }
        if ("desc" /* DESCENDING */ === r.dir && (s *= -1), 0 !== s) break;
    }
    return t.before ? s <= 0 : s < 0;
}

function Ye(t, n) {
    if (null === t) return null === n;
    if (null === n) return !1;
    if (t.before !== n.before || t.position.length !== n.position.length) return !1;
    for (let e = 0; e < t.position.length; e++) {
        if (!kt(t.position[e], n.position[e])) return !1;
    }
    return !0;
}

/**
 * An ordering on a field, in some Direction. Direction defaults to ASCENDING.
 */ class Je {
    constructor(t, n = "asc" /* ASCENDING */) {
        this.field = t, this.dir = n;
    }
}

function Xe(t, n, e) {
    const s = t.field.U() ? B.g(n.key, e.key) : function(t, n, e) {
        const s = n.field(t), i = e.field(t);
        return null !== s && null !== i ? $t(s, i) : V();
    }(t.field, n, e);
    switch (t.dir) {
      case "asc" /* ASCENDING */ :
        return s;

      case "desc" /* DESCENDING */ :
        return -1 * s;

      default:
        return V();
    }
}

function Ze(t, n) {
    return t.dir === n.dir && t.field.isEqual(n.field);
}

/**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */ class ts {
    constructor() {
        this.promise = new Promise(((t, n) => {
            this.resolve = t, this.reject = n;
        }));
    }
}

/**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/**
 * A helper for running delayed tasks following an exponential backoff curve
 * between attempts.
 *
 * Each delay is made up of a "base" delay which follows the exponential
 * backoff curve, and a +/- 50% "jitter" that is calculated and added to the
 * base delay. This prevents clients from accidentally synchronizing their
 * delays causing spikes of load to the backend.
 */
class ns {
    constructor(
    /**
     * The AsyncQueue to run backoff operations on.
     */
    t, 
    /**
     * The ID to use when scheduling backoff operations on the AsyncQueue.
     */
    n, 
    /**
     * The initial delay (used as the base delay on the first retry attempt).
     * Note that jitter will still be applied, so the actual delay could be as
     * little as 0.5*initialDelayMs.
     */
    e = 1e3
    /**
     * The multiplier to use to determine the extended base delay after each
     * attempt.
     */ , s = 1.5
    /**
     * The maximum base delay after which no further backoff is performed.
     * Note that jitter will still be applied, so the actual delay could be as
     * much as 1.5*maxDelayMs.
     */ , i = 6e4) {
        this.le = t, this._e = n, this.fe = e, this.de = s, this.we = i, this.Ee = 0, this.Te = null, 
        /** The last backoff attempt, as epoch milliseconds. */
        this.me = Date.now(), this.reset();
    }
    /**
     * Resets the backoff delay.
     *
     * The very next backoffAndWait() will have no delay. If it is called again
     * (i.e. due to an error), initialDelayMs (plus jitter) will be used, and
     * subsequent ones will increase according to the backoffFactor.
     */    reset() {
        this.Ee = 0;
    }
    /**
     * Resets the backoff delay to the maximum delay (e.g. for use after a
     * RESOURCE_EXHAUSTED error).
     */    Ie() {
        this.Ee = this.we;
    }
    /**
     * Returns a promise that resolves after currentDelayMs, and increases the
     * delay for any subsequent attempts. If there was a pending backoff operation
     * already, it will be canceled.
     */    Ae(t) {
        // Cancel any pending backoff operation.
        this.cancel();
        // First schedule using the current base (which may be 0 and should be
        // honored as such).
        const n = Math.floor(this.Ee + this.Re()), e = Math.max(0, Date.now() - this.me), s = Math.max(0, n - e);
        // Guard against lastAttemptTime being in the future due to a clock change.
                s > 0 && I("ExponentialBackoff", `Backing off for ${s} ms (base delay: ${this.Ee} ms, delay with jitter: ${n} ms, last attempt: ${e} ms ago)`), 
        this.Te = this.le.Pe(this._e, s, (() => (this.me = Date.now(), t()))), 
        // Apply backoff factor to determine next delay and ensure it is within
        // bounds.
        this.Ee *= this.de, this.Ee < this.fe && (this.Ee = this.fe), this.Ee > this.we && (this.Ee = this.we);
    }
    Ve() {
        null !== this.Te && (this.Te.ye(), this.Te = null);
    }
    cancel() {
        null !== this.Te && (this.Te.cancel(), this.Te = null);
    }
    /** Returns a random value in the range [-currentBaseMs/2, currentBaseMs/2] */    Re() {
        return (Math.random() - .5) * this.Ee;
    }
}

/**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/**
 * PersistencePromise<> is essentially a re-implementation of Promise<> except
 * it has a .next() method instead of .then() and .next() and .catch() callbacks
 * are executed synchronously when a PersistencePromise resolves rather than
 * asynchronously (Promise<> implementations use setImmediate() or similar).
 *
 * This is necessary to interoperate with IndexedDB which will automatically
 * commit transactions if control is returned to the event loop without
 * synchronously initiating another operation on the transaction.
 *
 * NOTE: .then() and .catch() only allow a single consumer, unlike normal
 * Promises.
 */ class es {
    constructor(t) {
        // NOTE: next/catchCallback will always point to our own wrapper functions,
        // not the user's raw next() or catch() callbacks.
        this.ge = null, this.pe = null, 
        // When the operation resolves, we'll set result or error and mark isDone.
        this.result = void 0, this.error = void 0, this.ve = !1, 
        // Set to true when .then() or .catch() are called and prevents additional
        // chaining.
        this.be = !1, t((t => {
            this.ve = !0, this.result = t, this.ge && 
            // value should be defined unless T is Void, but we can't express
            // that in the type system.
            this.ge(t);
        }), (t => {
            this.ve = !0, this.error = t, this.pe && this.pe(t);
        }));
    }
    catch(t) {
        return this.next(void 0, t);
    }
    next(t, n) {
        return this.be && V(), this.be = !0, this.ve ? this.error ? this.Ce(n, this.error) : this.Se(t, this.result) : new es(((e, s) => {
            this.ge = n => {
                this.Se(t, n).next(e, s);
            }, this.pe = t => {
                this.Ce(n, t).next(e, s);
            };
        }));
    }
    De() {
        return new Promise(((t, n) => {
            this.next(t, n);
        }));
    }
    Ne(t) {
        try {
            const n = t();
            return n instanceof es ? n : es.resolve(n);
        } catch (t) {
            return es.reject(t);
        }
    }
    Se(t, n) {
        return t ? this.Ne((() => t(n))) : es.resolve(n);
    }
    Ce(t, n) {
        return t ? this.Ne((() => t(n))) : es.reject(n);
    }
    static resolve(t) {
        return new es(((n, e) => {
            n(t);
        }));
    }
    static reject(t) {
        return new es(((n, e) => {
            e(t);
        }));
    }
    static Fe(
    // Accept all Promise types in waitFor().
    // eslint-disable-next-line @typescript-eslint/no-explicit-any
    t) {
        return new es(((n, e) => {
            let s = 0, i = 0, r = !1;
            t.forEach((t => {
                ++s, t.next((() => {
                    ++i, r && i === s && n();
                }), (t => e(t)));
            })), r = !0, i === s && n();
        }));
    }
    /**
     * Given an array of predicate functions that asynchronously evaluate to a
     * boolean, implements a short-circuiting `or` between the results. Predicates
     * will be evaluated until one of them returns `true`, then stop. The final
     * result will be whether any of them returned `true`.
     */    static xe(t) {
        let n = es.resolve(!1);
        for (const e of t) n = n.next((t => t ? es.resolve(t) : e()));
        return n;
    }
    static forEach(t, n) {
        const e = [];
        return t.forEach(((t, s) => {
            e.push(n.call(this, t, s));
        })), this.Fe(e);
    }
}

/**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/** Verifies whether `e` is an IndexedDbTransactionError. */ function ss(t) {
    // Use name equality, as instanceof checks on errors don't work with errors
    // that wrap other errors.
    return "IndexedDbTransactionError" === t.name;
}

/**
 * @license
 * Copyright 2020 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/** The Platform's 'document' implementation or null if not available. */ function is() {
    // `document` is not always available, e.g. in ReactNative and WebWorkers.
    // eslint-disable-next-line no-restricted-globals
    return "undefined" != typeof document ? document : null;
}

/**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/**
 * Represents an operation scheduled to be run in the future on an AsyncQueue.
 *
 * It is created via DelayedOperation.createAndSchedule().
 *
 * Supports cancellation (via cancel()) and early execution (via skipDelay()).
 *
 * Note: We implement `PromiseLike` instead of `Promise`, as the `Promise` type
 * in newer versions of TypeScript defines `finally`, which is not available in
 * IE.
 */
class rs {
    constructor(t, n, e, s, i) {
        this.Oe = t, this._e = n, this.ke = e, this.op = s, this.Le = i, this.$e = new ts, 
        this.then = this.$e.promise.then.bind(this.$e.promise), 
        // It's normal for the deferred promise to be canceled (due to cancellation)
        // and so we attach a dummy catch callback to avoid
        // 'UnhandledPromiseRejectionWarning' log spam.
        this.$e.promise.catch((t => {}));
    }
    /**
     * Creates and returns a DelayedOperation that has been scheduled to be
     * executed on the provided asyncQueue after the provided delayMs.
     *
     * @param asyncQueue The queue to schedule the operation on.
     * @param id A Timer ID identifying the type of operation this is.
     * @param delayMs The delay (ms) before the operation should be scheduled.
     * @param op The operation to run.
     * @param removalCallback A callback to be called synchronously once the
     *   operation is executed or canceled, notifying the AsyncQueue to remove it
     *   from its delayedOperations list.
     *   PORTING NOTE: This exists to prevent making removeDelayedOperation() and
     *   the DelayedOperation class public.
     */    static Me(t, n, e, s, i) {
        const r = Date.now() + e, o = new rs(t, n, r, s, i);
        return o.start(e), o;
    }
    /**
     * Starts the timer. This is called immediately after construction by
     * createAndSchedule().
     */    start(t) {
        this.Ue = setTimeout((() => this.qe()), t);
    }
    /**
     * Queues the operation to run immediately (if it hasn't already been run or
     * canceled).
     */    ye() {
        return this.qe();
    }
    /**
     * Cancels the operation if it hasn't already been executed or canceled. The
     * promise will be rejected.
     *
     * As long as the operation has not yet been run, calling cancel() provides a
     * guarantee that the operation will not be run.
     */    cancel(t) {
        null !== this.Ue && (this.clearTimeout(), this.$e.reject(new O(x.CANCELLED, "Operation cancelled" + (t ? ": " + t : ""))));
    }
    qe() {
        this.Oe.Be((() => null !== this.Ue ? (this.clearTimeout(), this.op().then((t => this.$e.resolve(t)))) : Promise.resolve()));
    }
    clearTimeout() {
        null !== this.Ue && (this.Le(this), clearTimeout(this.Ue), this.Ue = null);
    }
}

class os {
    constructor() {
        // The last promise in the queue.
        this.Qe = Promise.resolve(), 
        // A list of retryable operations. Retryable operations are run in order and
        // retried with backoff.
        this.je = [], 
        // Is this AsyncQueue being shut down? Once it is set to true, it will not
        // be changed again.
        this.We = !1, 
        // Operations scheduled to be queued in the future. Operations are
        // automatically removed after they are run or canceled.
        this.Ke = [], 
        // visible for testing
        this.Ge = null, 
        // Flag set while there's an outstanding AsyncQueue operation, used for
        // assertion sanity-checks.
        this.ze = !1, 
        // List of TimerIds to fast-forward delays for.
        this.He = [], 
        // Backoff timer used to schedule retries for retryable operations
        this.Ye = new ns(this, "async_queue_retry" /* AsyncQueueRetry */), 
        // Visibility handler that triggers an immediate retry of all retryable
        // operations. Meant to speed up recovery when we regain file system access
        // after page comes into foreground.
        this.Je = () => {
            const t = is();
            t && I("AsyncQueue", "Visibility state changed to " + t.visibilityState), this.Ye.Ve();
        };
        const t = is();
        t && "function" == typeof t.addEventListener && t.addEventListener("visibilitychange", this.Je);
    }
    // Is this AsyncQueue being shut down? If true, this instance will not enqueue
    // any new operations, Promises from enqueue requests will not resolve.
    get Xe() {
        return this.We;
    }
    /**
     * Adds a new operation to the queue without waiting for it to complete (i.e.
     * we ignore the Promise result).
     */    Be(t) {
        // eslint-disable-next-line @typescript-eslint/no-floating-promises
        this.enqueue(t);
    }
    /**
     * Regardless if the queue has initialized shutdown, adds a new operation to the
     * queue without waiting for it to complete (i.e. we ignore the Promise result).
     */    Ze(t) {
        this.ts(), 
        // eslint-disable-next-line @typescript-eslint/no-floating-promises
        this.ns(t);
    }
    /**
     * Initialize the shutdown of this queue. Once this method is called, the
     * only possible way to request running an operation is through
     * `enqueueEvenWhileRestricted()`.
     */    es() {
        if (!this.We) {
            this.We = !0;
            const t = is();
            t && "function" == typeof t.removeEventListener && t.removeEventListener("visibilitychange", this.Je);
        }
    }
    /**
     * Adds a new operation to the queue. Returns a promise that will be resolved
     * when the promise returned by the new operation is (with its value).
     */    enqueue(t) {
        return this.ts(), this.We ? new Promise((t => {})) : this.ns(t);
    }
    /**
     * Enqueue a retryable operation.
     *
     * A retryable operation is rescheduled with backoff if it fails with a
     * IndexedDbTransactionError (the error type used by SimpleDb). All
     * retryable operations are executed in order and only run if all prior
     * operations were retried successfully.
     */    ss(t) {
        this.Be((() => (this.je.push(t), this.rs())));
    }
    /**
     * Runs the next operation from the retryable queue. If the operation fails,
     * reschedules with backoff.
     */    async rs() {
        if (0 !== this.je.length) {
            try {
                await this.je[0](), this.je.shift(), this.Ye.reset();
            } catch (t) {
                if (!ss(t)) throw t;
 // Failure will be handled by AsyncQueue
                                I("AsyncQueue", "Operation failed with retryable error: " + t);
            }
            this.je.length > 0 && 
            // If there are additional operations, we re-schedule `retryNextOp()`.
            // This is necessary to run retryable operations that failed during
            // their initial attempt since we don't know whether they are already
            // enqueued. If, for example, `op1`, `op2`, `op3` are enqueued and `op1`
            // needs to  be re-run, we will run `op1`, `op1`, `op2` using the
            // already enqueued calls to `retryNextOp()`. `op3()` will then run in the
            // call scheduled here.
            // Since `backoffAndRun()` cancels an existing backoff and schedules a
            // new backoff on every call, there is only ever a single additional
            // operation in the queue.
            this.Ye.Ae((() => this.rs()));
        }
    }
    ns(t) {
        const n = this.Qe.then((() => (this.ze = !0, t().catch((t => {
            this.Ge = t, this.ze = !1;
            // Re-throw the error so that this.tail becomes a rejected Promise and
            // all further attempts to chain (via .then) will just short-circuit
            // and return the rejected Promise.
            throw A("INTERNAL UNHANDLED ERROR: ", 
            /**
 * Chrome includes Error.message in Error.stack. Other browsers do not.
 * This returns expected output of message + stack when available.
 * @param error Error or FirestoreError
 */
            function(t) {
                let n = t.message || "";
                t.stack && (n = t.stack.includes(t.message) ? t.stack : t.message + "\n" + t.stack);
                return n;
            }
            /**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
            /**
 * Holds the listeners and the last received ViewSnapshot for a query being
 * tracked by EventManager.
 */ (t)), t;
        })).then((t => (this.ze = !1, t))))));
        return this.Qe = n, n;
    }
    /**
     * Schedules an operation to be queued on the AsyncQueue once the specified
     * `delayMs` has elapsed. The returned DelayedOperation can be used to cancel
     * or fast-forward the operation prior to its running.
     */    Pe(t, n, e) {
        this.ts(), 
        // Fast-forward delays for timerIds that have been overriden.
        this.He.indexOf(t) > -1 && (n = 0);
        const s = rs.Me(this, t, n, e, (t => this.os(t)));
        return this.Ke.push(s), s;
    }
    ts() {
        this.Ge && V();
    }
    /**
     * Verifies there's an operation currently in-progress on the AsyncQueue.
     * Unfortunately we can't verify that the running code is in the promise chain
     * of that operation, so this isn't a foolproof check, but it should be enough
     * to catch some bugs.
     */    us() {}
    /**
     * Waits until all currently queued tasks are finished executing. Delayed
     * operations are not run.
     */    async cs() {
        // Operations in the queue prior to draining may have enqueued additional
        // operations. Keep draining the queue until the tail is no longer advanced,
        // which indicates that no more new operations were enqueued and that all
        // operations were executed.
        let t;
        do {
            t = this.Qe, await t;
        } while (t !== this.Qe);
    }
    /**
     * For Tests: Determine if a delayed operation with a particular TimerId
     * exists.
     */    hs(t) {
        for (const n of this.Ke) if (n._e === t) return !0;
        return !1;
    }
    /**
     * For Tests: Runs some or all delayed operations early.
     *
     * @param lastTimerId Delayed operations up to and including this TimerId will
     *  be drained. Pass TimerId.All to run all delayed operations.
     * @returns a Promise that resolves once all operations have been run.
     */    ls(t) {
        // Note that draining may generate more delayed ops, so we do that first.
        return this.cs().then((() => {
            // Run ops in the same order they'd run if they ran naturally.
            this.Ke.sort(((t, n) => t.ke - n.ke));
            for (const n of this.Ke) if (n.ye(), "all" /* All */ !== t && n._e === t) break;
            return this.cs();
        }));
    }
    /**
     * For Tests: Skip all subsequent delays for a timer id.
     */    _s(t) {
        this.He.push(t);
    }
    /** Called once a DelayedOperation is run or canceled. */    os(t) {
        // NOTE: indexOf / slice are O(n), but delayedOperations is expected to be small.
        const n = this.Ke.indexOf(t);
        this.Ke.splice(n, 1);
    }
}

/**
 * Returns a FirestoreError that can be surfaced to the user if the provided
 * error is an IndexedDbTransactionError. Re-throws the error otherwise.
 */ function us(t, n) {
    if (A("AsyncQueue", `${n}: ${t}`), ss(t)) return new O(x.UNAVAILABLE, `${n}: ${t}`);
    throw t;
}

class cs {
    constructor() {
        this.fs = void 0, this.listeners = [];
    }
}

class as {
    constructor() {
        this.queries = new F((t => Fe(t)), Ne), this.onlineState = "Unknown" /* Unknown */ , 
        this.ds = new Set;
    }
}

async function hs(t, n) {
    const e = g(t), s = n.query;
    let i = !1, r = e.queries.get(s);
    if (r || (i = !0, r = new cs), i) try {
        r.fs = await e.ws(s);
    } catch (t) {
        const e = us(t, `Initialization of query '${xe(n.query)}' failed`);
        return void n.onError(e);
    }
    e.queries.set(s, r), r.listeners.push(n);
    // Run global snapshot listeners if a consistent snapshot has been emitted.
    n.Es(e.onlineState);
    if (r.fs) {
        n.Ts(r.fs) && ds(e);
    }
}

async function ls(t, n) {
    const e = g(t), s = n.query;
    let i = !1;
    const r = e.queries.get(s);
    if (r) {
        const t = r.listeners.indexOf(n);
        t >= 0 && (r.listeners.splice(t, 1), i = 0 === r.listeners.length);
    }
    if (i) return e.queries.delete(s), e.Is(s);
}

function _s(t, n) {
    const e = g(t);
    let s = !1;
    for (const t of n) {
        const n = t.query, i = e.queries.get(n);
        if (i) {
            for (const n of i.listeners) n.Ts(t) && (s = !0);
            i.fs = t;
        }
    }
    s && ds(e);
}

function fs(t, n, e) {
    const s = g(t), i = s.queries.get(n);
    if (i) for (const t of i.listeners) t.onError(e);
    // Remove all listeners. NOTE: We don't need to call syncEngine.unlisten()
    // after an error.
        s.queries.delete(n);
}

// Call all global snapshot listeners that have been set.
function ds(t) {
    t.ds.forEach((t => {
        t.next();
    }));
}

/**
 * QueryListener takes a series of internal view snapshots and determines
 * when to raise the event.
 *
 * It uses an Observer to dispatch events.
 */ class ws {
    constructor(t, n, e) {
        this.query = t, this.As = n, 
        /**
         * Initial snapshots (e.g. from cache) may not be propagated to the wrapped
         * observer. This flag is set to true once we've actually raised an event.
         */
        this.Rs = !1, this.Ps = null, this.onlineState = "Unknown" /* Unknown */ , this.options = e || {};
    }
    /**
     * Applies the new ViewSnapshot to this listener, raising a user-facing event
     * if applicable (depending on what changed, whether the user has opted into
     * metadata-only changes, etc.). Returns true if a user-facing event was
     * indeed raised.
     */    Ts(t) {
        if (!this.options.includeMetadataChanges) {
            // Remove the metadata only changes.
            const n = [];
            for (const e of t.docChanges) 3 /* Metadata */ !== e.type && n.push(e);
            t = new Rt(t.query, t.docs, t.Bt, n, t.Qt, t.fromCache, t.jt, 
            /* excludesMetadataChanges= */ !0);
        }
        let n = !1;
        return this.Rs ? this.Vs(t) && (this.As.next(t), n = !0) : this.ys(t, this.onlineState) && (this.gs(t), 
        n = !0), this.Ps = t, n;
    }
    onError(t) {
        this.As.error(t);
    }
    /** Returns whether a snapshot was raised. */    Es(t) {
        this.onlineState = t;
        let n = !1;
        return this.Ps && !this.Rs && this.ys(this.Ps, t) && (this.gs(this.Ps), n = !0), 
        n;
    }
    ys(t, n) {
        // Always raise the first event when we're synced
        if (!t.fromCache) return !0;
        // NOTE: We consider OnlineState.Unknown as online (it should become Offline
        // or Online if we wait long enough).
                const e = "Offline" /* Offline */ !== n;
        // Don't raise the event if we're online, aren't synced yet (checked
        // above) and are waiting for a sync.
                return (!this.options.ps || !e) && (!t.docs.T() || "Offline" /* Offline */ === n);
        // Raise data from cache if we have any documents or we are offline
        }
    Vs(t) {
        // We don't need to handle includeDocumentMetadataChanges here because
        // the Metadata only changes have already been stripped out if needed.
        // At this point the only changes we will see are the ones we should
        // propagate.
        if (t.docChanges.length > 0) return !0;
        const n = this.Ps && this.Ps.hasPendingWrites !== t.hasPendingWrites;
        return !(!t.jt && !n) && !0 === this.options.includeMetadataChanges;
        // Generally we should have hit one of the cases above, but it's possible
        // to get here if there were only metadata docChanges and they got
        // stripped out.
        }
    gs(t) {
        t = Rt.Kt(t.query, t.docs, t.Qt, t.fromCache), this.Rs = !0, this.As.next(t);
    }
}

/**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/**
 * Simple wrapper around a nullable UID. Mostly exists to make code more
 * readable.
 */ class Es {
    constructor(t) {
        this.uid = t;
    }
    vs() {
        return null != this.uid;
    }
    /**
     * Returns a key representing this user, suitable for inclusion in a
     * dictionary.
     */    bs() {
        return this.vs() ? "uid:" + this.uid : "anonymous-user";
    }
    isEqual(t) {
        return t.uid === this.uid;
    }
}

/** A user with a null UID. */ Es.UNAUTHENTICATED = new Es(null), 
// TODO(mikelehen): Look into getting a proper uid-equivalent for
// non-FirebaseAuth providers.
Es.Cs = new Es("google-credentials-uid"), Es.Ss = new Es("first-party-uid");

/**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
class Ts {
    constructor(t, n) {
        this.user = n, this.type = "OAuth", this.Ds = {}, 
        // Set the headers using Object Literal notation to avoid minification
        this.Ds.Authorization = "Bearer " + t;
    }
}

/** A CredentialsProvider that always yields an empty token. */ class ms {
    constructor() {
        /**
         * Stores the listener registered with setChangeListener()
         * This isn't actually necessary since the UID never changes, but we use this
         * to verify the listen contract is adhered to in tests.
         */
        this.Ns = null;
    }
    getToken() {
        return Promise.resolve(null);
    }
    Fs() {}
    xs(t) {
        this.Ns = t, 
        // Fire with initial user.
        t(Es.UNAUTHENTICATED);
    }
    Os() {
        this.Ns = null;
    }
}

class Is {
    constructor(t) {
        /**
         * The auth token listener registered with FirebaseApp, retained here so we
         * can unregister it.
         */
        this.ks = null, 
        /** Tracks the current User. */
        this.currentUser = Es.UNAUTHENTICATED, this.Ls = !1, 
        /**
         * Counter used to detect if the token changed while a getToken request was
         * outstanding.
         */
        this.$s = 0, 
        /** The listener registered with setChangeListener(). */
        this.Ns = null, this.forceRefresh = !1, this.ks = () => {
            this.$s++, this.currentUser = this.Ms(), this.Ls = !0, this.Ns && this.Ns(this.currentUser);
        }, this.$s = 0, this.auth = t.getImmediate({
            optional: !0
        }), this.auth ? this.auth.addAuthTokenListener(this.ks) : (
        // if auth is not available, invoke tokenListener once with null token
        this.ks(null), t.get().then((t => {
            this.auth = t, this.ks && 
            // tokenListener can be removed by removeChangeListener()
            this.auth.addAuthTokenListener(this.ks);
        }), (() => {})));
    }
    getToken() {
        // Take note of the current value of the tokenCounter so that this method
        // can fail (with an ABORTED error) if there is a token change while the
        // request is outstanding.
        const t = this.$s, n = this.forceRefresh;
        return this.forceRefresh = !1, this.auth ? this.auth.getToken(n).then((n => 
        // Cancel the request since the token changed while the request was
        // outstanding so the response is potentially for a previous user (which
        // user, we can't be sure).
        this.$s !== t ? (I("FirebaseCredentialsProvider", "getToken aborted due to token change."), 
        this.getToken()) : n ? (y("string" == typeof n.accessToken), new Ts(n.accessToken, this.currentUser)) : null)) : Promise.resolve(null);
    }
    Fs() {
        this.forceRefresh = !0;
    }
    xs(t) {
        this.Ns = t, 
        // Fire the initial event
        this.Ls && t(this.currentUser);
    }
    Os() {
        this.auth && this.auth.removeAuthTokenListener(this.ks), this.ks = null, this.Ns = null;
    }
    // Auth.getUid() can return null even with a user logged in. It is because
    // getUid() is synchronous, but the auth code populating Uid is asynchronous.
    // This method should only be called in the AuthTokenListener callback
    // to guarantee to get the actual user.
    Ms() {
        const t = this.auth && this.auth.getUid();
        return y(null === t || "string" == typeof t), new Es(t);
    }
}

/*
 * FirstPartyToken provides a fresh token each time its value
 * is requested, because if the token is too old, requests will be rejected.
 * Technically this may no longer be necessary since the SDK should gracefully
 * recover from unauthenticated errors (see b/33147818 for context), but it's
 * safer to keep the implementation as-is.
 */ class As {
    constructor(t, n) {
        this.Us = t, this.qs = n, this.type = "FirstParty", this.user = Es.Ss;
    }
    get Ds() {
        const t = {
            "X-Goog-AuthUser": this.qs
        }, n = this.Us.auth.getAuthHeaderValueForFirstParty([]);
        // Use array notation to prevent minification
                return n && (t.Authorization = n), t;
    }
}

/*
 * Provides user credentials required for the Firestore JavaScript SDK
 * to authenticate the user, using technique that is only available
 * to applications hosted by Google.
 */ class Rs {
    constructor(t, n) {
        this.Us = t, this.qs = n;
    }
    getToken() {
        return Promise.resolve(new As(this.Us, this.qs));
    }
    xs(t) {
        // Fire with initial uid.
        t(Es.Ss);
    }
    Os() {}
    Fs() {}
}

/**
 * Builds a CredentialsProvider depending on the type of
 * the credentials passed in.
 */
/**
 * A batch of mutations that will be sent as one unit to the backend.
 */
class Ps {
    /**
     * @param batchId The unique ID of this mutation batch.
     * @param localWriteTime The original write time of this mutation.
     * @param baseMutations Mutations that are used to populate the base
     * values when this mutation is applied locally. This can be used to locally
     * overwrite values that are persisted in the remote document cache. Base
     * mutations are never sent to the backend.
     * @param mutations The user-provided mutations in this mutation batch.
     * User-provided mutations are applied both locally and remotely on the
     * backend.
     */
    constructor(t, n, e, s) {
        this.batchId = t, this.Bs = n, this.baseMutations = e, this.mutations = s;
    }
    /**
     * Applies all the mutations in this MutationBatch to the specified document
     * to create a new remote document
     *
     * @param docKey The key of the document to apply mutations to.
     * @param maybeDoc The document to apply mutations to.
     * @param batchResult The result of applying the MutationBatch to the
     * backend.
     */    Qs(t, n, e) {
        const s = e.js;
        for (let e = 0; e < this.mutations.length; e++) {
            const i = this.mutations[e];
            if (i.key.isEqual(t)) {
                n = te(i, n, s[e]);
            }
        }
        return n;
    }
    /**
     * Computes the local view of a document given all the mutations in this
     * batch.
     *
     * @param docKey The key of the document to apply mutations to.
     * @param maybeDoc The document to apply mutations to.
     */    Ws(t, n) {
        // First, apply the base state. This allows us to apply non-idempotent
        // transform against a consistent set of values.
        for (const e of this.baseMutations) e.key.isEqual(t) && (n = ne(e, n, n, this.Bs));
        const e = n;
        // Second, apply all user-provided mutations.
                for (const s of this.mutations) s.key.isEqual(t) && (n = ne(s, n, e, this.Bs));
        return n;
    }
    /**
     * Computes the local view for all provided documents given the mutations in
     * this batch.
     */    Ks(t) {
        // TODO(mrschmidt): This implementation is O(n^2). If we apply the mutations
        // directly (as done in `applyToLocalView()`), we can reduce the complexity
        // to O(n).
        let n = t;
        return this.mutations.forEach((e => {
            const s = this.Ws(e.key, t.get(e.key));
            s && (n = n.rt(e.key, s));
        })), n;
    }
    keys() {
        return this.mutations.reduce(((t, n) => t.add(n.key)), Et());
    }
    isEqual(t) {
        return this.batchId === t.batchId && C(this.mutations, t.mutations, ((t, n) => se(t, n))) && C(this.baseMutations, t.baseMutations, ((t, n) => se(t, n)));
    }
}

/** The result of applying a mutation batch to the backend. */ class Vs {
    constructor(t, n, e, 
    /**
     * A pre-computed mapping from each mutated document to the resulting
     * version.
     */
    s) {
        this.batch = t, this.Gs = n, this.js = e, this.zs = s;
    }
    /**
     * Creates a new MutationBatchResult for the given batch and results. There
     * must be one result for each mutation in the batch. This static factory
     * caches a document=>version mapping (docVersions).
     */    static from(t, n, e) {
        y(t.mutations.length === e.length);
        let s = dt;
        const i = t.mutations;
        for (let t = 0; t < i.length; t++) s = s.rt(i[t].key, e[t].version);
        return new Vs(t, n, e, s);
    }
}

/**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/**
 * An in-memory buffer of entries to be written to a RemoteDocumentCache.
 * It can be used to batch up a set of changes to be written to the cache, but
 * additionally supports reading entries back with the `getEntry()` method,
 * falling back to the underlying RemoteDocumentCache if no entry is
 * buffered.
 *
 * Entries added to the cache *must* be read first. This is to facilitate
 * calculating the size delta of the pending changes.
 *
 * PORTING NOTE: This class was implemented then removed from other platforms.
 * If byte-counting ends up being needed on the other platforms, consider
 * porting this class as part of that implementation work.
 */
/**
 * @license
 * Copyright 2020 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
function ys(t) {
    return new Zt(t, /* useProto3Json= */ !0);
}

/**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */ function gs(t) {
    /**
 * Returns true if obj is an object and contains at least one of the specified
 * methods.
 */
    return function(t, n) {
        if ("object" != typeof t || null === t) return !1;
        const e = t;
        for (const t of n) if (t in e && "function" == typeof e[t]) return !0;
        return !1;
    }
    /**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */ (t, [ "next", "error", "complete" ]);
}

/**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/**
 * A readonly view of the local state of all documents we're tracking (i.e. we
 * have a cached version in remoteDocumentCache or local mutations for the
 * document). The view is computed by applying the mutations in the
 * MutationQueue to the RemoteDocumentCache.
 */
class ps {
    constructor(t, n, e) {
        this.Hs = t, this.Ys = n, this.Js = e;
    }
    /**
     * Get the local view of the document identified by `key`.
     *
     * @return Local view of the document or null if we don't have any cached
     * state for it.
     */    Xs(t, n) {
        return this.Ys.Zs(t, n).next((e => this.ti(t, n, e)));
    }
    /** Internal version of `getDocument` that allows reusing batches. */    ti(t, n, e) {
        return this.Hs.ni(t, n).next((t => {
            for (const s of e) t = s.Ws(n, t);
            return t;
        }));
    }
    // Returns the view of the given `docs` as they would appear after applying
    // all mutations in the given `batches`.
    ei(t, n, e) {
        let s = lt();
        return n.forEach(((t, n) => {
            for (const s of e) n = s.Ws(t, n);
            s = s.rt(t, n);
        })), s;
    }
    /**
     * Gets the local view of the documents identified by `keys`.
     *
     * If we don't have cached state for a document in `keys`, a NoDocument will
     * be stored for that key in the resulting set.
     */    si(t, n) {
        return this.Hs.getEntries(t, n).next((n => this.ii(t, n)));
    }
    /**
     * Similar to `getDocuments`, but creates the local view from the given
     * `baseDocs` without retrieving documents from the local store.
     */    ii(t, n) {
        return this.Ys.ri(t, n).next((e => {
            const s = this.ei(t, n, e);
            let i = ht();
            return s.forEach(((t, n) => {
                // TODO(http://b/32275378): Don't conflate missing / deleted.
                n || (n = new me(t, L.min())), i = i.rt(t, n);
            })), i;
        }));
    }
    /**
     * Performs a query against the local view of all documents.
     *
     * @param transaction The persistence transaction.
     * @param query The query to match documents against.
     * @param sinceReadTime If not set to SnapshotVersion.min(), return only
     *     documents that have been read since this snapshot version (exclusive).
     */    oi(t, n, e) {
        /**
 * Returns whether the query matches a single document by path (rather than a
 * collection).
 */
        return function(t) {
            return B.G(t.path) && null === t.collectionGroup && 0 === t.filters.length;
        }(n) ? this.ui(t, n.path) : pe(n) ? this.ci(t, n, e) : this.ai(t, n, e);
    }
    ui(t, n) {
        // Just do a simple document lookup.
        return this.Xs(t, new B(n)).next((t => {
            let n = ft();
            return t instanceof Te && (n = n.rt(t.key, t)), n;
        }));
    }
    ci(t, n, e) {
        const s = n.collectionGroup;
        let i = ft();
        return this.Js.hi(t, s).next((r => es.forEach(r, (r => {
            const o = function(t, n) {
                return new Ae(n, 
                /*collectionGroup=*/ null, t.re.slice(), t.filters.slice(), t.limit, t.limitType, t.startAt, t.endAt);
            }
            /**
 * Returns true if this query does not specify any query constraints that
 * could remove results.
 */ (n, r.child(s));
            return this.ai(t, o, e).next((t => {
                t.forEach(((t, n) => {
                    i = i.rt(t, n);
                }));
            }));
        })).next((() => i))));
    }
    ai(t, n, e) {
        // Query the remote documents and overlay mutations.
        let s, i;
        return this.Hs.oi(t, n, e).next((e => (s = e, this.Ys.li(t, n)))).next((n => (i = n, 
        this._i(t, i, s).next((t => {
            s = t;
            for (const t of i) for (const n of t.mutations) {
                const e = n.key, i = s.get(e), r = ne(n, i, i, t.Bs);
                s = r instanceof Te ? s.rt(e, r) : s.remove(e);
            }
        }))))).next((() => (
        // Finally, filter out any documents that don't actually match
        // the query.
        s.forEach(((t, e) => {
            Oe(n, e) || (s = s.remove(t));
        })), s)));
    }
    _i(t, n, e) {
        let s = Et();
        for (const t of n) for (const n of t.mutations) n instanceof oe && null === e.get(n.key) && (s = s.add(n.key));
        let i = e;
        return this.Hs.getEntries(t, s).next((t => (t.forEach(((t, n) => {
            null !== n && n instanceof Te && (i = i.rt(t, n));
        })), i)));
    }
}

/**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/**
 * A set of changes to what documents are currently in view and out of view for
 * a given query. These changes are sent to the LocalStore by the View (via
 * the SyncEngine) and are used to pin / unpin documents as appropriate.
 */ class vs {
    constructor(t, n, e, s) {
        this.targetId = t, this.fromCache = n, this.fi = e, this.di = s;
    }
    static wi(t, n) {
        let e = Et(), s = Et();
        for (const t of n.docChanges) switch (t.type) {
          case 0 /* Added */ :
            e = e.add(t.doc.key);
            break;

          case 1 /* Removed */ :
            s = s.add(t.doc.key);
 // do nothing
                }
        return new vs(t, n.fromCache, e, s);
    }
}

/**
 * @license
 * Copyright 2018 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/**
 * `ListenSequence` is a monotonic sequence. It is initialized with a minimum value to
 * exceed. All subsequent calls to next will return increasing values. If provided with a
 * `SequenceNumberSyncer`, it will additionally bump its next value when told of a new value, as
 * well as write out sequence numbers that it produces via `next()`.
 */ class bs {
    constructor(t, n) {
        this.previousValue = t, n && (n.Ei = t => this.Ti(t), this.mi = t => n.Ii(t));
    }
    Ti(t) {
        return this.previousValue = Math.max(t, this.previousValue), this.previousValue;
    }
    next() {
        const t = ++this.previousValue;
        return this.mi && this.mi(t), t;
    }
}

bs.Ai = -1;

/**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/** Serializer for values stored in the LocalStore. */
class Cs {
    constructor(t) {
        this.Ri = t;
    }
}

/**
 * Encodes a `BundledQuery` from bundle proto to a Query object.
 *
 * This reconstructs the original query used to build the bundle being loaded,
 * including features exists only in SDKs (for example: limit-to-last).
 */ function Ss(t) {
    const n = Pn({
        parent: t.parent,
        structuredQuery: t.structuredQuery
    });
    return "LAST" === t.limitType ? Ce(n, n.limit, "L" /* Last */) : n;
}

/** Encodes a NamedQuery proto object to a NamedQuery model object. */
/**
 * @license
 * Copyright 2019 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/**
 * An in-memory implementation of IndexManager.
 */
class Ds {
    constructor() {
        this.Pi = new Ns;
    }
    Vi(t, n) {
        return this.Pi.add(n), es.resolve();
    }
    hi(t, n) {
        return es.resolve(this.Pi.getEntries(n));
    }
}

/**
 * Internal implementation of the collection-parent index exposed by MemoryIndexManager.
 * Also used for in-memory caching by IndexedDbIndexManager and initial index population
 * in indexeddb_schema.ts
 */ class Ns {
    constructor() {
        this.index = {};
    }
    // Returns false if the entry already existed.
    add(t) {
        const n = t.D(), e = t.C(), s = this.index[n] || new ut(M.g), i = !s.has(e);
        return this.index[n] = s.add(e), i;
    }
    has(t) {
        const n = t.D(), e = t.C(), s = this.index[n];
        return s && s.has(e);
    }
    getEntries(t) {
        return (this.index[t] || new ut(M.g)).O();
    }
}

/**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/** Offset to ensure non-overlapping target ids. */
/**
 * Generates monotonically increasing target IDs for sending targets to the
 * watch stream.
 *
 * The client constructs two generators, one for the target cache, and one for
 * for the sync engine (to generate limbo documents targets). These
 * generators produce non-overlapping IDs (by using even and odd IDs
 * respectively).
 *
 * By separating the target ID space, the query cache can generate target IDs
 * that persist across client restarts, while sync engine can independently
 * generate in-memory target IDs that are transient and can be reused after a
 * restart.
 */
class Fs {
    constructor(t) {
        this.yi = t;
    }
    next() {
        return this.yi += 2, this.yi;
    }
    static gi() {
        // The target cache generator must return '2' in its first call to `next()`
        // as there is no differentiation in the protocol layer between an unset
        // number and the number '0'. If we were to sent a target with target ID
        // '0', the backend would consider it unset and replace it with its own ID.
        return new Fs(0);
    }
    static pi() {
        // Sync engine assigns target IDs for limbo document detection.
        return new Fs(-1);
    }
}

/**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/**
 * Implements `LocalStore` interface.
 *
 * Note: some field defined in this class might have public access level, but
 * the class is not exported so they are only accessible from this module.
 * This is useful to implement optional features (like bundles) in free
 * functions, such that they are tree-shakeable.
 */
class xs {
    constructor(
    /** Manages our in-memory or durable persistence. */
    t, n, e, s) {
        this.persistence = t, this.vi = n, this.serializer = s, 
        /**
         * Maps a targetID to data about its target.
         *
         * PORTING NOTE: We are using an immutable data structure on Web to make re-runs
         * of `applyRemoteEvent()` idempotent.
         */
        this.bi = new it(b), 
        /** Maps a target to its targetID. */
        // TODO(wuandy): Evaluate if TargetId can be part of Target.
        this.Ci = new F((t => G(t)), H), 
        /**
         * The read time of the last entry processed by `getNewDocumentChanges()`.
         *
         * PORTING NOTE: This is only used for multi-tab synchronization.
         */
        this.Si = L.min(), this.Ys = t.Di(e), this.Ni = t.Fi(), this.xi = t.Oi(), this.ki = new ps(this.Ni, this.Ys, this.persistence.Li()), 
        this.$i = t.Mi(), this.vi.Ui(this.ki);
    }
    qi(t) {
        return this.persistence.runTransaction("Collect garbage", "readwrite-primary", (n => t.Bi(n, this.bi)));
    }
}

/**
 * Tells the LocalStore that the currently authenticated user has changed.
 *
 * In response the local store switches the mutation queue to the new user and
 * returns any resulting document changes.
 */
// PORTING NOTE: Android and iOS only return the documents affected by the
// change.
async function Os(t, n) {
    const e = g(t);
    let s = e.Ys, i = e.ki;
    const r = await e.persistence.runTransaction("Handle user change", "readonly", (t => {
        // Swap out the mutation queue, grabbing the pending mutation batches
        // before and after.
        let r;
        return e.Ys.Qi(t).next((o => (r = o, s = e.persistence.Di(n), 
        // Recreate our LocalDocumentsView using the new
        // MutationQueue.
        i = new ps(e.Ni, s, e.persistence.Li()), s.Qi(t)))).next((n => {
            const e = [], s = [];
            // Union the old/new changed keys.
            let o = Et();
            for (const t of r) {
                e.push(t.batchId);
                for (const n of t.mutations) o = o.add(n.key);
            }
            for (const t of n) {
                s.push(t.batchId);
                for (const n of t.mutations) o = o.add(n.key);
            }
            // Return the set of all (potentially) changed documents and the list
            // of mutation batch IDs that were affected by change.
                        return i.si(t, o).next((t => ({
                ji: t,
                Wi: e,
                Ki: s
            })));
        }));
    }));
    return e.Ys = s, e.ki = i, e.vi.Ui(e.ki), r;
}

/* Accepts locally generated Mutations and commit them to storage. */
/**
 * Acknowledges the given batch.
 *
 * On the happy path when a batch is acknowledged, the local store will
 *
 *  + remove the batch from the mutation queue;
 *  + apply the changes to the remote document cache;
 *  + recalculate the latency compensated view implied by those changes (there
 *    may be mutations in the queue that affect the documents but haven't been
 *    acknowledged yet); and
 *  + give the changed documents back the sync engine
 *
 * @returns The resulting (modified) documents.
 */
function ks(t, n) {
    const e = g(t);
    return e.persistence.runTransaction("Acknowledge batch", "readwrite-primary", (t => {
        const s = n.batch.keys(), i = e.Ni.Gi({
            zi: !0
        });
        return function(t, n, e, s) {
            const i = e.batch, r = i.keys();
            let o = es.resolve();
            return r.forEach((t => {
                o = o.next((() => s.ni(n, t))).next((n => {
                    let r = n;
                    const o = e.zs.get(t);
                    y(null !== o), (!r || r.version.A(o) < 0) && (r = i.Qs(t, r, e), r && 
                    // We use the commitVersion as the readTime rather than the
                    // document's updateTime since the updateTime is not advanced
                    // for updates that do not modify the underlying document.
                    s.Hi(r, e.Gs));
                }));
            })), o.next((() => t.Ys.Yi(n, i)));
        }
        /**
 * Verifies the error thrown by a LocalStore operation. If a LocalStore
 * operation fails because the primary lease has been taken by another client,
 * we ignore the error (the persistence layer will immediately call
 * `applyPrimaryLease` to propagate the primary state change). All other errors
 * are re-thrown.
 *
 * @param err An error returned by a LocalStore operation.
 * @return A Promise that resolves after we recovered, or the original error.
 */ (e, t, n, i).next((() => i.apply(t))).next((() => e.Ys.Ji(t))).next((() => e.ki.si(t, s)));
    }));
}

/**
 * Removes mutations from the MutationQueue for the specified batch;
 * LocalDocuments will be recalculated.
 *
 * @returns The resulting modified documents.
 */
/**
 * Returns the last consistent snapshot processed (used by the RemoteStore to
 * determine whether to buffer incoming snapshots from the backend).
 */
function Ls(t) {
    const n = g(t);
    return n.persistence.runTransaction("Get last remote snapshot version", "readonly", (t => n.xi.Xi(t)));
}

/**
 * Updates the "ground-state" (remote) documents. We assume that the remote
 * event reflects any write batches that have been acknowledged or rejected
 * (i.e. we do not re-apply local mutations to updates from this event).
 *
 * LocalDocuments are re-calculated if there are remaining mutations in the
 * queue.
 */ function $s(t, n) {
    const e = g(t), s = n.nt;
    let i = e.bi;
    return e.persistence.runTransaction("Apply remote event", "readwrite-primary", (t => {
        const r = e.Ni.Gi({
            zi: !0
        });
        // Reset newTargetDataByTargetMap in case this transaction gets re-run.
                i = e.bi;
        const o = [];
        n.Gt.forEach(((n, r) => {
            const u = i.get(r);
            if (!u) return;
            // Only update the remote keys if the target is still active. This
            // ensures that we can persist the updated target data along with
            // the updated assignment.
                        o.push(e.xi.Zi(t, n.en, r).next((() => e.xi.tr(t, n.tn, r))));
            const c = n.resumeToken;
            // Update the resume token if the change includes one.
                        if (c.X() > 0) {
                const a = u.st(c, s).et(t.nr);
                i = i.rt(r, a), 
                // Update the target data if there are target changes (or if
                // sufficient time has passed since the last update).
                /**
 * Returns true if the newTargetData should be persisted during an update of
 * an active target. TargetData should always be persisted when a target is
 * being released and should not call this function.
 *
 * While the target is active, TargetData updates can be omitted when nothing
 * about the target has changed except metadata like the resume token or
 * snapshot version. Occasionally it's worth the extra write to prevent these
 * values from getting too stale after a crash, but this doesn't have to be
 * too frequent.
 */
                function(t, n, e) {
                    // Always persist target data if we don't already have a resume token.
                    if (y(n.resumeToken.X() > 0), 0 === t.resumeToken.X()) return !0;
                    // Don't allow resume token changes to be buffered indefinitely. This
                    // allows us to be reasonably up-to-date after a crash and avoids needing
                    // to loop over all active queries on shutdown. Especially in the browser
                    // we may not get time to do anything interesting while the current tab is
                    // closing.
                                        if (n.nt.R() - t.nt.R() >= 3e8) return !0;
                    // Otherwise if the only thing that has changed about a target is its resume
                    // token it's not worth persisting. Note that the RemoteStore keeps an
                    // in-memory view of the currently active targets which includes the current
                    // resume token, so stream failure or user changes will still use an
                    // up-to-date resume token regardless of what we do here.
                                        return e.tn.size + e.nn.size + e.en.size > 0;
                }
                /**
 * Notifies local store of the changed views to locally pin documents.
 */ (u, a, n) && o.push(e.xi.er(t, a));
            }
        }));
        let u = ht();
        // HACK: The only reason we allow a null snapshot version is so that we
        // can synthesize remote events when we get permission denied errors while
        // trying to resolve the state of a locally cached document that is in
        // limbo.
        if (n.Ht.forEach(((s, i) => {
            n.Yt.has(s) && o.push(e.persistence.ir.sr(t, s));
        })), 
        // Each loop iteration only affects its "own" doc, so it's safe to get all the remote
        // documents in advance in a single call.
        o.push(
        /**
 * Populates document change buffer with documents from backend or a bundle.
 * Returns the document changes resulting from applying those documents.
 *
 * @param txn Transaction to use to read existing documents from storage.
 * @param documentBuffer Document buffer to collect the resulted changes to be
 *        applied to storage.
 * @param documents Documents to be applied.
 * @param globalVersion A `SnapshotVersion` representing the read time if all
 *        documents have the same read time.
 * @param documentVersions A DocumentKey-to-SnapshotVersion map if documents
 *        have their own read time.
 *
 * Note: this function will use `documentVersions` if it is defined;
 * when it is not defined, resorts to `globalVersion`.
 */
        function(t, n, e, s, 
        // TODO(wuandy): We could add `readTime` to MaybeDocument instead to remove
        // this parameter.
        i) {
            let r = Et();
            return e.forEach((t => r = r.add(t))), n.getEntries(t, r).next((t => {
                let r = ht();
                return e.forEach(((e, o) => {
                    const u = t.get(e), c = (null == i ? void 0 : i.get(e)) || s;
                    // Note: The order of the steps below is important, since we want
                    // to ensure that rejected limbo resolutions (which fabricate
                    // NoDocuments with SnapshotVersion.min()) never add documents to
                    // cache.
                    o instanceof me && o.version.isEqual(L.min()) ? (
                    // NoDocuments with SnapshotVersion.min() are used in manufactured
                    // events. We remove these documents from cache since we lost
                    // access.
                    n.rr(e, c), r = r.rt(e, o)) : null == u || o.version.A(u.version) > 0 || 0 === o.version.A(u.version) && u.hasPendingWrites ? (n.Hi(o, c), 
                    r = r.rt(e, o)) : I("LocalStore", "Ignoring outdated watch update for ", e, ". Current version:", u.version, " Watch version:", o.version);
                })), r;
            }));
        }(t, r, n.Ht, s, void 0).next((t => {
            u = t;
        }))), !s.isEqual(L.min())) {
            const n = e.xi.Xi(t).next((n => e.xi.ur(t, t.nr, s)));
            o.push(n);
        }
        return es.Fe(o).next((() => r.apply(t))).next((() => e.ki.ii(t, u)));
    })).then((t => (e.bi = i, t)));
}

/**
 * Gets the mutation batch after the passed in batchId in the mutation queue
 * or null if empty.
 * @param afterBatchId If provided, the batch to search after.
 * @returns The next mutation or null if there wasn't one.
 */
function Ms(t, n) {
    const e = g(t);
    return e.persistence.runTransaction("Get next mutation batch", "readonly", (t => (void 0 === n && (n = -1), 
    e.Ys.cr(t, n))));
}

/**
 * Reads the current value of a Document with a given key or null if not
 * found - used for testing.
 */
/**
 * Unpins all the documents associated with the given target. If
 * `keepPersistedTargetData` is set to false and Eager GC enabled, the method
 * directly removes the associated target data from the target cache.
 *
 * Releasing a non-existing `Target` is a no-op.
 */
// PORTING NOTE: `keepPersistedTargetData` is multi-tab only.
async function Us(t, n, e) {
    const s = g(t), i = s.bi.get(n), r = e ? "readwrite" : "readwrite-primary";
    try {
        e || await s.persistence.runTransaction("Release target", r, (t => s.persistence.ir.removeTarget(t, i)));
    } catch (t) {
        if (!ss(t)) throw t;
        // All `releaseTarget` does is record the final metadata state for the
        // target, but we've been recording this periodically during target
        // activity. If we lose this write this could cause a very slight
        // difference in the order of target deletion during GC, but we
        // don't define exact LRU semantics so this is acceptable.
        I("LocalStore", `Failed to update sequence numbers for target ${n}: ${t}`);
    }
    s.bi = s.bi.remove(n), s.Ci.delete(i.target);
}

/**
 * Runs the specified query against the local store and returns the results,
 * potentially taking advantage of query data from previous executions (such
 * as the set of remote keys).
 *
 * @param usePreviousResults Whether results from previous executions can
 * be used to optimize this query execution.
 */ function qs(t, n, e) {
    const s = g(t);
    let i = L.min(), r = Et();
    return s.persistence.runTransaction("Execute query", "readonly", (t => 
    /**
 * Returns the TargetData as seen by the LocalStore, including updates that may
 * have not yet been persisted to the TargetCache.
 */
    // Visible for testing.
    function(t, n, e) {
        const s = g(t), i = s.Ci.get(e);
        return void 0 !== i ? es.resolve(s.bi.get(i)) : s.xi.ar(n, e);
    }(s, t, be(n)).next((n => {
        if (n) return i = n.lastLimboFreeSnapshotVersion, s.xi.hr(t, n.targetId).next((t => {
            r = t;
        }));
    })).next((() => s.vi.oi(t, n, e ? i : L.min(), e ? r : Et()))).next((t => ({
        documents: t,
        lr: r
    })))));
}

async function Bs(t) {
    if (t.code !== x.FAILED_PRECONDITION || "The current tab is not in the required state to perform this operation. It might be necessary to refresh the browser tab." !== t.message) throw t;
    I("LocalStore", "Unexpectedly lost primary lease");
}

/**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/**
 * A PersistentStream is an abstract base class that represents a streaming RPC
 * to the Firestore backend. It's built on top of the connections own support
 * for streaming RPCs, and adds several critical features for our clients:
 *
 *   - Exponential backoff on failure
 *   - Authentication via CredentialsProvider
 *   - Dispatching all callbacks into the shared worker queue
 *   - Closing idle streams after 60 seconds of inactivity
 *
 * Subclasses of PersistentStream implement serialization of models to and
 * from the JSON representation of the protocol buffers for a specific
 * streaming RPC.
 *
 * ## Starting and Stopping
 *
 * Streaming RPCs are stateful and need to be start()ed before messages can
 * be sent and received. The PersistentStream will call the onOpen() function
 * of the listener once the stream is ready to accept requests.
 *
 * Should a start() fail, PersistentStream will call the registered onClose()
 * listener with a FirestoreError indicating what went wrong.
 *
 * A PersistentStream can be started and stopped repeatedly.
 *
 * Generic types:
 *  SendType: The type of the outgoing message of the underlying
 *    connection stream
 *  ReceiveType: The type of the incoming message of the underlying
 *    connection stream
 *  ListenerType: The type of the listener that will be used for callbacks
 */
class Qs {
    constructor(t, n, e, s, i, r) {
        this.le = t, this._r = e, this.dr = s, this.wr = i, this.listener = r, this.state = 0 /* Initial */ , 
        /**
         * A close count that's incremented every time the stream is closed; used by
         * getCloseGuardedDispatcher() to invalidate callbacks that happen after
         * close.
         */
        this.Er = 0, this.Tr = null, this.stream = null, this.Ye = new ns(t, n);
    }
    /**
     * Returns true if start() has been called and no error has occurred. True
     * indicates the stream is open or in the process of opening (which
     * encompasses respecting backoff, getting auth tokens, and starting the
     * actual RPC). Use isOpen() to determine if the stream is open and ready for
     * outbound requests.
     */    mr() {
        return 1 /* Starting */ === this.state || 2 /* Open */ === this.state || 4 /* Backoff */ === this.state;
    }
    /**
     * Returns true if the underlying RPC is open (the onOpen() listener has been
     * called) and the stream is ready for outbound requests.
     */    Ir() {
        return 2 /* Open */ === this.state;
    }
    /**
     * Starts the RPC. Only allowed if isStarted() returns false. The stream is
     * not immediately ready for use: onOpen() will be invoked when the RPC is
     * ready for outbound requests, at which point isOpen() will return true.
     *
     * When start returns, isStarted() will return true.
     */    start() {
        3 /* Error */ !== this.state ? this.auth() : this.Ar();
    }
    /**
     * Stops the RPC. This call is idempotent and allowed regardless of the
     * current isStarted() state.
     *
     * When stop returns, isStarted() and isOpen() will both return false.
     */    async stop() {
        this.mr() && await this.close(0 /* Initial */);
    }
    /**
     * After an error the stream will usually back off on the next attempt to
     * start it. If the error warrants an immediate restart of the stream, the
     * sender can use this to indicate that the receiver should not back off.
     *
     * Each error will call the onClose() listener. That function can decide to
     * inhibit backoff if required.
     */    Rr() {
        this.state = 0 /* Initial */ , this.Ye.reset();
    }
    /**
     * Marks this stream as idle. If no further actions are performed on the
     * stream for one minute, the stream will automatically close itself and
     * notify the stream's onClose() handler with Status.OK. The stream will then
     * be in a !isStarted() state, requiring the caller to start the stream again
     * before further use.
     *
     * Only streams that are in state 'Open' can be marked idle, as all other
     * states imply pending network operations.
     */    Pr() {
        // Starts the idle time if we are in state 'Open' and are not yet already
        // running a timer (in which case the previous idle timeout still applies).
        this.Ir() && null === this.Tr && (this.Tr = this.le.Pe(this._r, 6e4, (() => this.Vr())));
    }
    /** Sends a message to the underlying stream. */    yr(t) {
        this.gr(), this.stream.send(t);
    }
    /** Called by the idle timer when the stream should close due to inactivity. */    async Vr() {
        if (this.Ir()) 
        // When timing out an idle stream there's no reason to force the stream into backoff when
        // it restarts so set the stream state to Initial instead of Error.
        return this.close(0 /* Initial */);
    }
    /** Marks the stream as active again. */    gr() {
        this.Tr && (this.Tr.cancel(), this.Tr = null);
    }
    /**
     * Closes the stream and cleans up as necessary:
     *
     * * closes the underlying GRPC stream;
     * * calls the onClose handler with the given 'error';
     * * sets internal stream state to 'finalState';
     * * adjusts the backoff timer based on the error
     *
     * A new stream can be opened by calling start().
     *
     * @param finalState the intended state of the stream after closing.
     * @param error the error the connection was closed with.
     */    async close(t, n) {
        // Cancel any outstanding timers (they're guaranteed not to execute).
        this.gr(), this.Ye.cancel(), 
        // Invalidates any stream-related callbacks (e.g. from auth or the
        // underlying stream), guaranteeing they won't execute.
        this.Er++, 3 /* Error */ !== t ? 
        // If this is an intentional close ensure we don't delay our next connection attempt.
        this.Ye.reset() : n && n.code === x.RESOURCE_EXHAUSTED ? (
        // Log the error. (Probably either 'quota exceeded' or 'max queue length reached'.)
        A(n.toString()), A("Using maximum backoff delay to prevent overloading the backend."), 
        this.Ye.Ie()) : n && n.code === x.UNAUTHENTICATED && 
        // "unauthenticated" error means the token was rejected. Try force refreshing it in case it
        // just expired.
        this.wr.Fs(), 
        // Clean up the underlying stream because we are no longer interested in events.
        null !== this.stream && (this.pr(), this.stream.close(), this.stream = null), 
        // This state must be assigned before calling onClose() to allow the callback to
        // inhibit backoff or otherwise manipulate the state in its non-started state.
        this.state = t, 
        // Notify the listener that the stream closed.
        await this.listener.vr(n);
    }
    /**
     * Can be overridden to perform additional cleanup before the stream is closed.
     * Calling super.tearDown() is not required.
     */    pr() {}
    auth() {
        this.state = 1 /* Starting */;
        const t = this.br(this.Er), n = this.Er;
        // TODO(mikelehen): Just use dispatchIfNotClosed, but see TODO below.
                this.wr.getToken().then((t => {
            // Stream can be stopped while waiting for authentication.
            // TODO(mikelehen): We really should just use dispatchIfNotClosed
            // and let this dispatch onto the queue, but that opened a spec test can
            // of worms that I don't want to deal with in this PR.
            this.Er === n && 
            // Normally we'd have to schedule the callback on the AsyncQueue.
            // However, the following calls are safe to be called outside the
            // AsyncQueue since they don't chain asynchronous calls
            this.Cr(t);
        }), (n => {
            t((() => {
                const t = new O(x.UNKNOWN, "Fetching auth token failed: " + n.message);
                return this.Sr(t);
            }));
        }));
    }
    Cr(t) {
        const n = this.br(this.Er);
        this.stream = this.Dr(t), this.stream.Nr((() => {
            n((() => (this.state = 2 /* Open */ , this.listener.Nr())));
        })), this.stream.vr((t => {
            n((() => this.Sr(t)));
        })), this.stream.onMessage((t => {
            n((() => this.onMessage(t)));
        }));
    }
    Ar() {
        this.state = 4 /* Backoff */ , this.Ye.Ae((async () => {
            this.state = 0 /* Initial */ , this.start();
        }));
    }
    // Visible for tests
    Sr(t) {
        // In theory the stream could close cleanly, however, in our current model
        // we never expect this to happen because if we stop a stream ourselves,
        // this callback will never be called. To prevent cases where we retry
        // without a backoff accidentally, we set the stream to error in all cases.
        return I("PersistentStream", "close with error: " + t), this.stream = null, this.close(3 /* Error */ , t);
    }
    /**
     * Returns a "dispatcher" function that dispatches operations onto the
     * AsyncQueue but only runs them if closeCount remains unchanged. This allows
     * us to turn auth / stream callbacks into no-ops if the stream is closed /
     * re-opened, etc.
     */    br(t) {
        return n => {
            this.le.Be((() => this.Er === t ? n() : (I("PersistentStream", "stream callback skipped by getCloseGuardedDispatcher."), 
            Promise.resolve())));
        };
    }
}

/**
 * A PersistentStream that implements the Listen RPC.
 *
 * Once the Listen stream has called the onOpen() listener, any number of
 * listen() and unlisten() calls can be made to control what changes will be
 * sent from the server for ListenResponses.
 */ class js extends Qs {
    constructor(t, n, e, s, i) {
        super(t, "listen_stream_connection_backoff" /* ListenStreamConnectionBackoff */ , "listen_stream_idle" /* ListenStreamIdle */ , n, e, i), 
        this.serializer = s;
    }
    Dr(t) {
        return this.dr.Fr("Listen", t);
    }
    onMessage(t) {
        // A successful response means the stream is healthy
        this.Ye.reset();
        const n = Tn(this.serializer, t), e = function(t) {
            // We have only reached a consistent snapshot for the entire stream if there
            // is a read_time set and it applies to all targets (i.e. the list of
            // targets is empty). The backend is guaranteed to send such responses.
            if (!("targetChange" in t)) return L.min();
            const n = t.targetChange;
            return n.targetIds && n.targetIds.length ? L.min() : n.readTime ? un(n.readTime) : L.min();
        }(t);
        return this.listener.Or(n, e);
    }
    /**
     * Registers interest in the results of the given target. If the target
     * includes a resumeToken it will be included in the request. Results that
     * affect the target will be streamed back as WatchChange messages that
     * reference the targetId.
     */    kr(t) {
        const n = {};
        n.database = fn(this.serializer), n.addTarget = function(t, n) {
            let e;
            const s = n.target;
            return e = Y(s) ? {
                documents: An(t, s)
            } : {
                query: Rn(t, s)
            }, e.targetId = n.targetId, n.resumeToken.X() > 0 ? e.resumeToken = rn(t, n.resumeToken) : n.nt.A(L.min()) > 0 && (
            // TODO(wuandy): Consider removing above check because it is most likely true.
            // Right now, many tests depend on this behaviour though (leaving min() out
            // of serialization).
            e.readTime = sn(t, n.nt.P())), e;
        }(this.serializer, t);
        const e = Vn(this.serializer, t);
        e && (n.labels = e), this.yr(n);
    }
    /**
     * Unregisters interest in the results of the target associated with the
     * given targetId.
     */    Lr(t) {
        const n = {};
        n.database = fn(this.serializer), n.removeTarget = t, this.yr(n);
    }
}

/**
 * A Stream that implements the Write RPC.
 *
 * The Write RPC requires the caller to maintain special streamToken
 * state in between calls, to help the server understand which responses the
 * client has processed by the time the next request is made. Every response
 * will contain a streamToken; this value must be passed to the next
 * request.
 *
 * After calling start() on this stream, the next request must be a handshake,
 * containing whatever streamToken is on hand. Once a response to this
 * request is received, all pending mutations may be submitted. When
 * submitting multiple batches of mutations at the same time, it's
 * okay to use the same streamToken for the calls to writeMutations.
 *
 * TODO(b/33271235): Use proto types
 */ class Ws extends Qs {
    constructor(t, n, e, s, i) {
        super(t, "write_stream_connection_backoff" /* WriteStreamConnectionBackoff */ , "write_stream_idle" /* WriteStreamIdle */ , n, e, i), 
        this.serializer = s, this.$r = !1;
    }
    /**
     * Tracks whether or not a handshake has been successfully exchanged and
     * the stream is ready to accept mutations.
     */    get Mr() {
        return this.$r;
    }
    // Override of PersistentStream.start
    start() {
        this.$r = !1, this.lastStreamToken = void 0, super.start();
    }
    pr() {
        this.$r && this.Ur([]);
    }
    Dr(t) {
        return this.dr.Fr("Write", t);
    }
    onMessage(t) {
        if (
        // Always capture the last stream token.
        y(!!t.streamToken), this.lastStreamToken = t.streamToken, this.$r) {
            // A successful first write response means the stream is healthy,
            // Note, that we could consider a successful handshake healthy, however,
            // the write itself might be causing an error we want to back off from.
            this.Ye.reset();
            const n = In(t.writeResults, t.commitTime), e = un(t.commitTime);
            return this.listener.qr(e, n);
        }
        // The first response is always the handshake response
        return y(!t.writeResults || 0 === t.writeResults.length), this.$r = !0, this.listener.Br();
    }
    /**
     * Sends an initial streamToken to the server, performing the handshake
     * required to make the StreamingWrite RPC work. Subsequent
     * calls should wait until onHandshakeComplete was called.
     */    Qr() {
        // TODO(dimond): Support stream resumption. We intentionally do not set the
        // stream token on the handshake, ignoring any stream token we might have.
        const t = {};
        t.database = fn(this.serializer), this.yr(t);
    }
    /** Sends a group of mutations to the Firestore backend to apply. */    Ur(t) {
        const n = {
            streamToken: this.lastStreamToken,
            writes: t.map((t => mn(this.serializer, t)))
        };
        this.yr(n);
    }
}

/**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/**
 * Datastore and its related methods are a wrapper around the external Google
 * Cloud Datastore grpc API, which provides an interface that is more convenient
 * for the rest of the client SDK architecture to consume.
 */
/**
 * An implementation of Datastore that exposes additional state for internal
 * consumption.
 */
class Ks extends class {} {
    constructor(t, n, e) {
        super(), this.credentials = t, this.dr = n, this.serializer = e, this.jr = !1;
    }
    Wr() {
        if (this.jr) throw new O(x.FAILED_PRECONDITION, "The client has already been terminated.");
    }
    /** Gets an auth token and invokes the provided RPC. */    Kr(t, n, e) {
        return this.Wr(), this.credentials.getToken().then((s => this.dr.Kr(t, n, e, s))).catch((t => {
            throw t.code === x.UNAUTHENTICATED && this.credentials.Fs(), t;
        }));
    }
    /** Gets an auth token and invokes the provided RPC with streamed results. */    Gr(t, n, e) {
        return this.Wr(), this.credentials.getToken().then((s => this.dr.Gr(t, n, e, s))).catch((t => {
            throw t.code === x.UNAUTHENTICATED && this.credentials.Fs(), t;
        }));
    }
    terminate() {
        this.jr = !1;
    }
}

// TODO(firestorexp): Make sure there is only one Datastore instance per
// firestore-exp client.
/**
 * A component used by the RemoteStore to track the OnlineState (that is,
 * whether or not the client as a whole should be considered to be online or
 * offline), implementing the appropriate heuristics.
 *
 * In particular, when the client is trying to connect to the backend, we
 * allow up to MAX_WATCH_STREAM_FAILURES within ONLINE_STATE_TIMEOUT_MS for
 * a connection to succeed. If we have too many failures or the timeout elapses,
 * then we set the OnlineState to Offline, and the client will behave as if
 * it is offline (get()s will return cached data, etc.).
 */
class Gs {
    constructor(t, n) {
        this.Oe = t, this.zr = n, 
        /** The current OnlineState. */
        this.state = "Unknown" /* Unknown */ , 
        /**
         * A count of consecutive failures to open the stream. If it reaches the
         * maximum defined by MAX_WATCH_STREAM_FAILURES, we'll set the OnlineState to
         * Offline.
         */
        this.Hr = 0, 
        /**
         * A timer that elapses after ONLINE_STATE_TIMEOUT_MS, at which point we
         * transition from OnlineState.Unknown to OnlineState.Offline without waiting
         * for the stream to actually fail (MAX_WATCH_STREAM_FAILURES times).
         */
        this.Yr = null, 
        /**
         * Whether the client should log a warning message if it fails to connect to
         * the backend (initially true, cleared after a successful stream, or if we've
         * logged the message already).
         */
        this.Jr = !0;
    }
    /**
     * Called by RemoteStore when a watch stream is started (including on each
     * backoff attempt).
     *
     * If this is the first attempt, it sets the OnlineState to Unknown and starts
     * the onlineStateTimer.
     */    Xr() {
        0 === this.Hr && (this.Zr("Unknown" /* Unknown */), this.Yr = this.Oe.Pe("online_state_timeout" /* OnlineStateTimeout */ , 1e4, (() => (this.Yr = null, 
        this.no("Backend didn't respond within 10 seconds."), this.Zr("Offline" /* Offline */), 
        Promise.resolve()))));
    }
    /**
     * Updates our OnlineState as appropriate after the watch stream reports a
     * failure. The first failure moves us to the 'Unknown' state. We then may
     * allow multiple failures (based on MAX_WATCH_STREAM_FAILURES) before we
     * actually transition to the 'Offline' state.
     */    eo(t) {
        "Online" /* Online */ === this.state ? this.Zr("Unknown" /* Unknown */) : (this.Hr++, 
        this.Hr >= 1 && (this.so(), this.no("Connection failed 1 times. Most recent error: " + t.toString()), 
        this.Zr("Offline" /* Offline */)));
    }
    /**
     * Explicitly sets the OnlineState to the specified state.
     *
     * Note that this resets our timers / failure counters, etc. used by our
     * Offline heuristics, so must not be used in place of
     * handleWatchStreamStart() and handleWatchStreamFailure().
     */    set(t) {
        this.so(), this.Hr = 0, "Online" /* Online */ === t && (
        // We've connected to watch at least once. Don't warn the developer
        // about being offline going forward.
        this.Jr = !1), this.Zr(t);
    }
    Zr(t) {
        t !== this.state && (this.state = t, this.zr(t));
    }
    no(t) {
        const n = `Could not reach Cloud Firestore backend. ${t}\nThis typically indicates that your device does not have a healthy Internet connection at the moment. The client will operate in offline mode until it is able to successfully connect to the backend.`;
        this.Jr ? (A(n), this.Jr = !1) : I("OnlineStateTracker", n);
    }
    so() {
        null !== this.Yr && (this.Yr.cancel(), this.Yr = null);
    }
}

/**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */ class zs {
    constructor(
    /**
     * The local store, used to fill the write pipeline with outbound mutations.
     */
    t, 
    /** The client-side proxy for interacting with the backend. */
    n, e, s, i) {
        this.io = t, this.ro = n, this.Oe = e, this.oo = {}, 
        /**
         * A list of up to MAX_PENDING_WRITES writes that we have fetched from the
         * LocalStore via fillWritePipeline() and have or will send to the write
         * stream.
         *
         * Whenever writePipeline.length > 0 the RemoteStore will attempt to start or
         * restart the write stream. When the stream is established the writes in the
         * pipeline will be sent in order.
         *
         * Writes remain in writePipeline until they are acknowledged by the backend
         * and thus will automatically be re-sent if the stream is interrupted /
         * restarted before they're acknowledged.
         *
         * Write responses from the backend are linked to their originating request
         * purely based on order, and so we can just shift() writes from the front of
         * the writePipeline as we receive responses.
         */
        this.uo = [], 
        /**
         * A mapping of watched targets that the client cares about tracking and the
         * user has explicitly called a 'listen' for this target.
         *
         * These targets may or may not have been sent to or acknowledged by the
         * server. On re-establishing the listen stream, these targets should be sent
         * to the server. The targets removed with unlistens are removed eagerly
         * without waiting for confirmation from the listen stream.
         */
        this.co = new Map, 
        /**
         * A set of reasons for why the RemoteStore may be offline. If empty, the
         * RemoteStore may start its network connections.
         */
        this.ao = new Set, 
        /**
         * Event handlers that get called when the network is disabled or enabled.
         *
         * PORTING NOTE: These functions are used on the Web client to create the
         * underlying streams (to support tree-shakeable streams). On Android and iOS,
         * the streams are created during construction of RemoteStore.
         */
        this.ho = [], this.lo = i, this.lo._o((t => {
            e.Be((async () => {
                // Porting Note: Unlike iOS, `restartNetwork()` is called even when the
                // network becomes unreachable as we don't have any other way to tear
                // down our streams.
                si(this) && (I("RemoteStore", "Restarting streams for network reachability change."), 
                await async function(t) {
                    const n = g(t);
                    n.ao.add(4 /* ConnectivityChange */), await Ys(n), n.fo.set("Unknown" /* Unknown */), 
                    n.ao.delete(4 /* ConnectivityChange */), await Hs(n);
                }(this));
            }));
        })), this.fo = new Gs(e, s);
    }
}

async function Hs(t) {
    if (si(t)) for (const n of t.ho) await n(/* enabled= */ !0);
}

/**
 * Temporarily disables the network. The network can be re-enabled using
 * enableNetwork().
 */ async function Ys(t) {
    for (const n of t.ho) await n(/* enabled= */ !1);
}

/**
 * Starts new listen for the given target. Uses resume token if provided. It
 * is a no-op if the target of given `TargetData` is already being listened to.
 */
function Js(t, n) {
    const e = g(t);
    e.co.has(n.targetId) || (
    // Mark this as something the client is currently listening for.
    e.co.set(n.targetId, n), ei(e) ? 
    // The listen will be sent in onWatchStreamOpen
    ni(e) : Ii(e).Ir() && Zs(e, n));
}

/**
 * Removes the listen from server. It is a no-op if the given target id is
 * not being listened to.
 */ function Xs(t, n) {
    const e = g(t), s = Ii(e);
    e.co.delete(n), s.Ir() && ti(e, n), 0 === e.co.size && (s.Ir() ? s.Pr() : si(e) && 
    // Revert to OnlineState.Unknown if the watch stream is not open and we
    // have no listeners, since without any listens to send we cannot
    // confirm if the stream is healthy and upgrade to OnlineState.Online.
    e.fo.set("Unknown" /* Unknown */));
}

/**
 * We need to increment the the expected number of pending responses we're due
 * from watch so we wait for the ack to process any messages from this target.
 */ function Zs(t, n) {
    t.wo.In(n.targetId), Ii(t).kr(n);
}

/**
 * We need to increment the expected number of pending responses we're due
 * from watch so we wait for the removal on the server before we process any
 * messages from this target.
 */ function ti(t, n) {
    t.wo.In(n), Ii(t).Lr(n);
}

function ni(t) {
    t.wo = new bt({
        qn: n => t.oo.qn(n),
        Bn: n => t.co.get(n) || null
    }), Ii(t).start(), t.fo.Xr();
}

/**
 * Returns whether the watch stream should be started because it's necessary
 * and has not yet been started.
 */ function ei(t) {
    return si(t) && !Ii(t).mr() && t.co.size > 0;
}

function si(t) {
    return 0 === g(t).ao.size;
}

function ii(t) {
    t.wo = void 0;
}

async function ri(t) {
    t.co.forEach(((n, e) => {
        Zs(t, n);
    }));
}

async function oi(t, n) {
    ii(t), 
    // If we still need the watch stream, retry the connection.
    ei(t) ? (t.fo.eo(n), ni(t)) : 
    // No need to restart watch stream because there are no active targets.
    // The online state is set to unknown because there is no active attempt
    // at establishing a connection
    t.fo.set("Unknown" /* Unknown */);
}

async function ui(t, n, e) {
    if (
    // Mark the client as online since we got a message from the server
    t.fo.set("Online" /* Online */), n instanceof pt && 2 /* Removed */ === n.state && n.cause) 
    // There was an error on a target, don't wait for a consistent snapshot
    // to raise events
    try {
        await 
        /** Handles an error on a target */
        async function(t, n) {
            const e = n.cause;
            for (const s of n.targetIds) 
            // A watched target might have been removed already.
            t.co.has(s) && (await t.oo.Eo(s, e), t.co.delete(s), t.wo.removeTarget(s));
        }
        /**
 * Attempts to fill our write pipeline with writes from the LocalStore.
 *
 * Called internally to bootstrap or refill the write pipeline and by
 * SyncEngine whenever there are new mutations to process.
 *
 * Starts the write stream if necessary.
 */ (t, n);
    } catch (e) {
        I("RemoteStore", "Failed to remove targets %s: %s ", n.targetIds.join(","), e), 
        await ci(t, e);
    } else if (n instanceof yt ? t.wo.vn(n) : n instanceof gt ? t.wo.On(n) : t.wo.Sn(n), 
    !e.isEqual(L.min())) try {
        const n = await Ls(t.io);
        e.A(n) >= 0 && 
        // We have received a target change with a global snapshot if the snapshot
        // version is not equal to SnapshotVersion.min().
        await 
        /**
 * Takes a batch of changes from the Datastore, repackages them as a
 * RemoteEvent, and passes that on to the listener, which is typically the
 * SyncEngine.
 */
        function(t, n) {
            const e = t.wo.$n(n);
            // Update in-memory resume tokens. LocalStore will update the
            // persistent view of these when applying the completed RemoteEvent.
                        return e.Gt.forEach(((e, s) => {
                if (e.resumeToken.X() > 0) {
                    const i = t.co.get(s);
                    // A watched target might have been removed already.
                                        i && t.co.set(s, i.st(e.resumeToken, n));
                }
            })), 
            // Re-establish listens for the targets that have been invalidated by
            // existence filter mismatches.
            e.zt.forEach((n => {
                const e = t.co.get(n);
                if (!e) 
                // A watched target might have been removed already.
                return;
                // Clear the resume token for the target, since we're in a known mismatch
                // state.
                                t.co.set(n, e.st(J.Z, e.nt)), 
                // Cause a hard reset by unwatching and rewatching immediately, but
                // deliberately don't send a resume token so that we get a full update.
                ti(t, n);
                // Mark the target we send as being on behalf of an existence filter
                // mismatch, but don't actually retain that in listenTargets. This ensures
                // that we flag the first re-listen this way without impacting future
                // listens of this target (that might happen e.g. on reconnect).
                const s = new X(e.target, n, 1 /* ExistenceFilterMismatch */ , e.sequenceNumber);
                Zs(t, s);
            })), t.oo.To(e);
        }(t, e);
    } catch (n) {
        I("RemoteStore", "Failed to raise snapshot:", n), await ci(t, n);
    }
}

/**
 * Recovery logic for IndexedDB errors that takes the network offline until
 * `op` succeeds. Retries are scheduled with backoff using
 * `enqueueRetryable()`. If `op()` is not provided, IndexedDB access is
 * validated via a generic operation.
 *
 * The returned Promise is resolved once the network is disabled and before
 * any retry attempt.
 */ async function ci(t, n, e) {
    if (!ss(n)) throw n;
    t.ao.add(1 /* IndexedDbFailed */), 
    // Disable network and raise offline snapshots
    await Ys(t), t.fo.set("Offline" /* Offline */), e || (
    // Use a simple read operation to determine if IndexedDB recovered.
    // Ideally, we would expose a health check directly on SimpleDb, but
    // RemoteStore only has access to persistence through LocalStore.
    e = () => Ls(t.io)), 
    // Probe IndexedDB periodically and re-enable network
    t.Oe.ss((async () => {
        I("RemoteStore", "Retrying IndexedDB access"), await e(), t.ao.delete(1 /* IndexedDbFailed */), 
        await Hs(t);
    }));
}

/**
 * Executes `op`. If `op` fails, takes the network offline until `op`
 * succeeds. Returns after the first attempt.
 */ function ai(t, n) {
    return n().catch((e => ci(t, e, n)));
}

async function hi(t) {
    const n = g(t), e = Ai(n);
    let s = n.uo.length > 0 ? n.uo[n.uo.length - 1].batchId : -1;
    for (;li(n); ) try {
        const t = await Ms(n.io, s);
        if (null === t) {
            0 === n.uo.length && e.Pr();
            break;
        }
        s = t.batchId, _i(n, t);
    } catch (t) {
        await ci(n, t);
    }
    fi(n) && di(n);
}

/**
 * Returns true if we can add to the write pipeline (i.e. the network is
 * enabled and the write pipeline is not full).
 */ function li(t) {
    return si(t) && t.uo.length < 10;
}

/**
 * Queues additional writes to be sent to the write stream, sending them
 * immediately if the write stream is established.
 */ function _i(t, n) {
    t.uo.push(n);
    const e = Ai(t);
    e.Ir() && e.Mr && e.Ur(n.mutations);
}

function fi(t) {
    return si(t) && !Ai(t).mr() && t.uo.length > 0;
}

function di(t) {
    Ai(t).start();
}

async function wi(t) {
    Ai(t).Qr();
}

async function Ei(t) {
    const n = Ai(t);
    // Send the write pipeline now that the stream is established.
        for (const e of t.uo) n.Ur(e.mutations);
}

async function Ti(t, n, e) {
    const s = t.uo.shift(), i = Vs.from(s, n, e);
    await ai(t, (() => t.oo.mo(i))), 
    // It's possible that with the completion of this mutation another
    // slot has freed up.
    await hi(t);
}

async function mi(t, n) {
    // If the write stream closed after the write handshake completes, a write
    // operation failed and we fail the pending operation.
    n && Ai(t).Mr && 
    // This error affects the actual write.
    await async function(t, n) {
        // Only handle permanent errors here. If it's transient, just let the retry
        // logic kick in.
        if (e = n.code, et(e) && e !== x.ABORTED) {
            // This was a permanent error, the request itself was the problem
            // so it's not going to succeed if we resend it.
            const e = t.uo.shift();
            // In this case it's also unlikely that the server itself is melting
            // down -- this was just a bad request so inhibit backoff on the next
            // restart.
                        Ai(t).Rr(), await ai(t, (() => t.oo.Io(e.batchId, n))), 
            // It's possible that with the completion of this mutation
            // another slot has freed up.
            await hi(t);
        }
        var e;
        /**
 * Maps an error Code from a GRPC status identifier like 'NOT_FOUND'.
 *
 * @returns The Code equivalent to the given status string or undefined if
 *     there is no match.
 */    }(t, n), 
    // The write stream might have been started by refilling the write
    // pipeline for failed writes
    fi(t) && di(t);
}

/**
 * If not yet initialized, registers the WatchStream and its network state
 * callback with `remoteStoreImpl`. Returns the existing stream if one is
 * already available.
 *
 * PORTING NOTE: On iOS and Android, the WatchStream gets registered on startup.
 * This is not done on Web to allow it to be tree-shaken.
 */
function Ii(t) {
    return t.Ao || (
    // Create stream (but note that it is not started yet).
    t.Ao = function(t, n, e) {
        const s = g(t);
        return s.Wr(), new js(n, s.dr, s.credentials, s.serializer, e);
    }
    /**
 * @license
 * Copyright 2018 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */ (t.ro, t.Oe, {
        Nr: ri.bind(null, t),
        vr: oi.bind(null, t),
        Or: ui.bind(null, t)
    }), t.ho.push((async n => {
        n ? (t.Ao.Rr(), ei(t) ? ni(t) : t.fo.set("Unknown" /* Unknown */)) : (await t.Ao.stop(), 
        ii(t));
    }))), t.Ao;
}

/**
 * If not yet initialized, registers the WriteStream and its network state
 * callback with `remoteStoreImpl`. Returns the existing stream if one is
 * already available.
 *
 * PORTING NOTE: On iOS and Android, the WriteStream gets registered on startup.
 * This is not done on Web to allow it to be tree-shaken.
 */ function Ai(t) {
    return t.Ro || (
    // Create stream (but note that it is not started yet).
    t.Ro = function(t, n, e) {
        const s = g(t);
        return s.Wr(), new Ws(n, s.dr, s.credentials, s.serializer, e);
    }(t.ro, t.Oe, {
        Nr: wi.bind(null, t),
        vr: mi.bind(null, t),
        Br: Ei.bind(null, t),
        qr: Ti.bind(null, t)
    }), t.ho.push((async n => {
        n ? (t.Ro.Rr(), 
        // This will start the write stream if necessary.
        await hi(t)) : (await t.Ro.stop(), t.uo.length > 0 && (I("RemoteStore", `Stopping write stream with ${t.uo.length} pending writes`), 
        t.uo = []));
    }))), t.Ro;
}

/**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/**
 * A collection of references to a document from some kind of numbered entity
 * (either a target ID or batch ID). As references are added to or removed from
 * the set corresponding events are emitted to a registered garbage collector.
 *
 * Each reference is represented by a DocumentReference object. Each of them
 * contains enough information to uniquely identify the reference. They are all
 * stored primarily in a set sorted by key. A document is considered garbage if
 * there's no references in that set (this can be efficiently checked thanks to
 * sorting by key).
 *
 * ReferenceSet also keeps a secondary set that contains references sorted by
 * IDs. This one is used to efficiently implement removal of all references by
 * some target ID.
 */ class Ri {
    constructor() {
        // A set of outstanding references to a document sorted by key.
        this.Po = new ut(Pi.Vo), 
        // A set of outstanding references to a document sorted by target id.
        this.yo = new ut(Pi.po);
    }
    /** Returns true if the reference set contains no references. */    T() {
        return this.Po.T();
    }
    /** Adds a reference to the given document key for the given ID. */    vo(t, n) {
        const e = new Pi(t, n);
        this.Po = this.Po.add(e), this.yo = this.yo.add(e);
    }
    /** Add references to the given document keys for the given ID. */    bo(t, n) {
        t.forEach((t => this.vo(t, n)));
    }
    /**
     * Removes a reference to the given document key for the given
     * ID.
     */    Co(t, n) {
        this.So(new Pi(t, n));
    }
    Do(t, n) {
        t.forEach((t => this.Co(t, n)));
    }
    /**
     * Clears all references with a given ID. Calls removeRef() for each key
     * removed.
     */    No(t) {
        const n = new B(new M([])), e = new Pi(n, t), s = new Pi(n, t + 1), i = [];
        return this.yo.Nt([ e, s ], (t => {
            this.So(t), i.push(t.key);
        })), i;
    }
    Fo() {
        this.Po.forEach((t => this.So(t)));
    }
    So(t) {
        this.Po = this.Po.delete(t), this.yo = this.yo.delete(t);
    }
    xo(t) {
        const n = new B(new M([])), e = new Pi(n, t), s = new Pi(n, t + 1);
        let i = Et();
        return this.yo.Nt([ e, s ], (t => {
            i = i.add(t.key);
        })), i;
    }
    Oo(t) {
        const n = new Pi(t, 0), e = this.Po.xt(n);
        return null !== e && t.isEqual(e.key);
    }
}

class Pi {
    constructor(t, n) {
        this.key = t, this.ko = n;
    }
    /** Compare by key then by ID */    static Vo(t, n) {
        return B.g(t.key, n.key) || b(t.ko, n.ko);
    }
    /** Compare by ID then by key */    static po(t, n) {
        return b(t.ko, n.ko) || B.g(t.key, n.key);
    }
}

/**
 * @license
 * Copyright 2018 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/**
 * Metadata state of the local client. Unlike `RemoteClientState`, this class is
 * mutable and keeps track of all pending mutations, which allows us to
 * update the range of pending mutation batch IDs as new mutations are added or
 * removed.
 *
 * The data in `LocalClientState` is not read from WebStorage and instead
 * updated via its instance methods. The updated state can be serialized via
 * `toWebStorageJSON()`.
 */
// Visible for testing.
class Vi {
    constructor() {
        this.activeTargetIds = mt();
    }
    Lo(t) {
        this.activeTargetIds = this.activeTargetIds.add(t);
    }
    $o(t) {
        this.activeTargetIds = this.activeTargetIds.delete(t);
    }
    /**
     * Converts this entry into a JSON-encoded format we can use for WebStorage.
     * Does not encode `clientId` as it is part of the key in WebStorage.
     */    Mo() {
        const t = {
            activeTargetIds: this.activeTargetIds.O(),
            updateTimeMs: Date.now()
        };
        return JSON.stringify(t);
    }
}

/**
 * `MemorySharedClientState` is a simple implementation of SharedClientState for
 * clients using memory persistence. The state in this class remains fully
 * isolated and no synchronization is performed.
 */ class yi {
    constructor() {
        this.Uo = new Vi, this.qo = {}, this.zr = null, this.Ei = null;
    }
    Bo(t) {
        // No op.
    }
    Qo(t, n, e) {
        // No op.
    }
    jo(t) {
        return this.Uo.Lo(t), this.qo[t] || "not-current";
    }
    Wo(t, n, e) {
        this.qo[t] = n;
    }
    Ko(t) {
        this.Uo.$o(t);
    }
    Go(t) {
        return this.Uo.activeTargetIds.has(t);
    }
    zo(t) {
        delete this.qo[t];
    }
    Ho() {
        return this.Uo.activeTargetIds;
    }
    Yo(t) {
        return this.Uo.activeTargetIds.has(t);
    }
    start() {
        return this.Uo = new Vi, Promise.resolve();
    }
    Jo(t, n, e) {
        // No op.
    }
    Xo(t) {
        // No op.
    }
    Zo() {}
    Ii(t) {}
    tu() {
        // No op.
    }
}

/**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */ class gi {
    constructor(t) {
        this.key = t;
    }
}

class pi {
    constructor(t) {
        this.key = t;
    }
}

/**
 * View is responsible for computing the final merged truth of what docs are in
 * a query. It gets notified of local and remote changes to docs, and applies
 * the query filters and limits to determine the most correct possible results.
 */ class vi {
    constructor(t, 
    /** Documents included in the remote target */
    n) {
        this.query = t, this.nu = n, this.eu = null, 
        /**
         * A flag whether the view is current with the backend. A view is considered
         * current after it has seen the current flag from the backend and did not
         * lose consistency within the watch stream (e.g. because of an existence
         * filter mismatch).
         */
        this.Zt = !1, 
        /** Documents in the view but not in the remote target */
        this.su = Et(), 
        /** Document Keys that have local changes */
        this.Qt = Et(), this.iu = ke(t), this.ru = new It(this.iu);
    }
    /**
     * The set of remote documents that the server has told us belongs to the target associated with
     * this view.
     */    get ou() {
        return this.nu;
    }
    /**
     * Iterates over a set of doc changes, applies the query limit, and computes
     * what the new results should be, what the changes were, and whether we may
     * need to go back to the local cache for more results. Does not make any
     * changes to the view.
     * @param docChanges The doc changes to apply to this view.
     * @param previousChanges If this is being called with a refill, then start
     *        with this set of docs and changes instead of the current view.
     * @return a new set of docs, changes, and refill flag.
     */    uu(t, n) {
        const e = n ? n.cu : new At, s = n ? n.ru : this.ru;
        let i = n ? n.Qt : this.Qt, r = s, o = !1;
        // Track the last doc in a (full) limit. This is necessary, because some
        // update (a delete, or an update moving a doc past the old limit) might
        // mean there is some other document in the local cache that either should
        // come (1) between the old last limit doc and the new last document, in the
        // case of updates, or (2) after the new last document, in the case of
        // deletes. So we keep this doc at the old limit to compare the updates to.
        // Note that this should never get used in a refill (when previousChanges is
        // set), because there will only be adds -- no deletes or updates.
        const u = Pe(this.query) && s.size === this.query.limit ? s.last() : null, c = Ve(this.query) && s.size === this.query.limit ? s.first() : null;
        // Drop documents out to meet limit/limitToLast requirement.
        if (t.at(((t, n) => {
            const a = s.get(t);
            let h = n instanceof Te ? n : null;
            h && (h = Oe(this.query, h) ? h : null);
            const l = !!a && this.Qt.has(a.key), _ = !!h && (h.Jn || 
            // We only consider committed mutations for documents that were
            // mutated during the lifetime of the view.
            this.Qt.has(h.key) && h.hasCommittedMutations);
            let f = !1;
            // Calculate change
                        if (a && h) {
                a.data().isEqual(h.data()) ? l !== _ && (e.track({
                    type: 3 /* Metadata */ ,
                    doc: h
                }), f = !0) : this.au(a, h) || (e.track({
                    type: 2 /* Modified */ ,
                    doc: h
                }), f = !0, (u && this.iu(h, u) > 0 || c && this.iu(h, c) < 0) && (
                // This doc moved from inside the limit to outside the limit.
                // That means there may be some other doc in the local cache
                // that should be included instead.
                o = !0));
            } else !a && h ? (e.track({
                type: 0 /* Added */ ,
                doc: h
            }), f = !0) : a && !h && (e.track({
                type: 1 /* Removed */ ,
                doc: a
            }), f = !0, (u || c) && (
            // A doc was removed from a full limit query. We'll need to
            // requery from the local cache to see if we know about some other
            // doc that should be in the results.
            o = !0));
            f && (h ? (r = r.add(h), i = _ ? i.add(t) : i.delete(t)) : (r = r.delete(t), i = i.delete(t)));
        })), Pe(this.query) || Ve(this.query)) for (;r.size > this.query.limit; ) {
            const t = Pe(this.query) ? r.last() : r.first();
            r = r.delete(t.key), i = i.delete(t.key), e.track({
                type: 1 /* Removed */ ,
                doc: t
            });
        }
        return {
            ru: r,
            cu: e,
            hu: o,
            Qt: i
        };
    }
    au(t, n) {
        // We suppress the initial change event for documents that were modified as
        // part of a write acknowledgment (e.g. when the value of a server transform
        // is applied) as Watch will send us the same document again.
        // By suppressing the event, we only raise two user visible events (one with
        // `hasPendingWrites` and the final state of the document) instead of three
        // (one with `hasPendingWrites`, the modified document with
        // `hasPendingWrites` and the final state of the document).
        return t.Jn && n.hasCommittedMutations && !n.Jn;
    }
    /**
     * Updates the view with the given ViewDocumentChanges and optionally updates
     * limbo docs and sync state from the provided target change.
     * @param docChanges The set of changes to make to the view's docs.
     * @param updateLimboDocuments Whether to update limbo documents based on this
     *        change.
     * @param targetChange A target change to apply for computing limbo docs and
     *        sync state.
     * @return A new ViewChange with the given docs, changes, and sync state.
     */
    // PORTING NOTE: The iOS/Android clients always compute limbo document changes.
    lu(t, n, e) {
        const s = this.ru;
        this.ru = t.ru, this.Qt = t.Qt;
        // Sort changes based on type and query comparator
        const i = t.cu.qt();
        i.sort(((t, n) => function(t, n) {
            const e = t => {
                switch (t) {
                  case 0 /* Added */ :
                    return 1;

                  case 2 /* Modified */ :
                  case 3 /* Metadata */ :
                    // A metadata change is converted to a modified change at the public
                    // api layer.  Since we sort by document key and then change type,
                    // metadata and modified changes must be sorted equivalently.
                    return 2;

                  case 1 /* Removed */ :
                    return 0;

                  default:
                    return V();
                }
            };
            return e(t) - e(n);
        }
        /**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */ (t.type, n.type) || this.iu(t.doc, n.doc))), this._u(e);
        const r = n ? this.fu() : [], o = 0 === this.su.size && this.Zt ? 1 /* Synced */ : 0 /* Local */ , u = o !== this.eu;
        if (this.eu = o, 0 !== i.length || u) {
            return {
                snapshot: new Rt(this.query, t.ru, s, i, t.Qt, 0 /* Local */ === o, u, 
                /* excludesMetadataChanges= */ !1),
                du: r
            };
        }
        // no changes
        return {
            du: r
        };
    }
    /**
     * Applies an OnlineState change to the view, potentially generating a
     * ViewChange if the view's syncState changes as a result.
     */    Es(t) {
        return this.Zt && "Offline" /* Offline */ === t ? (
        // If we're offline, set `current` to false and then call applyChanges()
        // to refresh our syncState and generate a ViewChange as appropriate. We
        // are guaranteed to get a new TargetChange that sets `current` back to
        // true once the client is back online.
        this.Zt = !1, this.lu({
            ru: this.ru,
            cu: new At,
            Qt: this.Qt,
            hu: !1
        }, 
        /* updateLimboDocuments= */ !1)) : {
            du: []
        };
    }
    /**
     * Returns whether the doc for the given key should be in limbo.
     */    wu(t) {
        // If the remote end says it's part of this query, it's not in limbo.
        return !this.nu.has(t) && (
        // The local store doesn't think it's a result, so it shouldn't be in limbo.
        !!this.ru.has(t) && !this.ru.get(t).Jn);
    }
    /**
     * Updates syncedDocuments, current, and limbo docs based on the given change.
     * Returns the list of changes to which docs are in limbo.
     */    _u(t) {
        t && (t.tn.forEach((t => this.nu = this.nu.add(t))), t.nn.forEach((t => {})), t.en.forEach((t => this.nu = this.nu.delete(t))), 
        this.Zt = t.Zt);
    }
    fu() {
        // We can only determine limbo documents when we're in-sync with the server.
        if (!this.Zt) return [];
        // TODO(klimt): Do this incrementally so that it's not quadratic when
        // updating many documents.
                const t = this.su;
        this.su = Et(), this.ru.forEach((t => {
            this.wu(t.key) && (this.su = this.su.add(t.key));
        }));
        // Diff the new limbo docs with the old limbo docs.
        const n = [];
        return t.forEach((t => {
            this.su.has(t) || n.push(new pi(t));
        })), this.su.forEach((e => {
            t.has(e) || n.push(new gi(e));
        })), n;
    }
    /**
     * Update the in-memory state of the current view with the state read from
     * persistence.
     *
     * We update the query view whenever a client's primary status changes:
     * - When a client transitions from primary to secondary, it can miss
     *   LocalStorage updates and its query views may temporarily not be
     *   synchronized with the state on disk.
     * - For secondary to primary transitions, the client needs to update the list
     *   of `syncedDocuments` since secondary clients update their query views
     *   based purely on synthesized RemoteEvents.
     *
     * @param queryResult.documents - The documents that match the query according
     * to the LocalStore.
     * @param queryResult.remoteKeys - The keys of the documents that match the
     * query according to the backend.
     *
     * @return The ViewChange that resulted from this synchronization.
     */
    // PORTING NOTE: Multi-tab only.
    Eu(t) {
        this.nu = t.lr, this.su = Et();
        const n = this.uu(t.documents);
        return this.lu(n, /*updateLimboDocuments=*/ !0);
    }
    /**
     * Returns a view snapshot as if this query was just listened to. Contains
     * a document add for every existing document and the `fromCache` and
     * `hasPendingWrites` status of the already established view.
     */
    // PORTING NOTE: Multi-tab only.
    Tu() {
        return Rt.Kt(this.query, this.ru, this.Qt, 0 /* Local */ === this.eu);
    }
}

/**
 * QueryView contains all of the data that SyncEngine needs to keep track of for
 * a particular query.
 */
class bi {
    constructor(
    /**
     * The query itself.
     */
    t, 
    /**
     * The target number created by the client that is used in the watch
     * stream to identify this query.
     */
    n, 
    /**
     * The view is responsible for computing the final merged truth of what
     * docs are in the query. It gets notified of local and remote changes,
     * and applies the query filters and limits to determine the most correct
     * possible results.
     */
    e) {
        this.query = t, this.targetId = n, this.view = e;
    }
}

/** Tracks a limbo resolution. */ class Ci {
    constructor(t) {
        this.key = t, 
        /**
         * Set to true once we've received a document. This is used in
         * getRemoteKeysForTarget() and ultimately used by WatchChangeAggregator to
         * decide whether it needs to manufacture a delete event for the target once
         * the target is CURRENT.
         */
        this.mu = !1;
    }
}

/**
 * An implementation of `SyncEngine` coordinating with other parts of SDK.
 *
 * The parts of SyncEngine that act as a callback to RemoteStore need to be
 * registered individually. This is done in `syncEngineWrite()` and
 * `syncEngineListen()` (as well as `applyPrimaryState()`) as these methods
 * serve as entry points to RemoteStore's functionality.
 *
 * Note: some field defined in this class might have public access level, but
 * the class is not exported so they are only accessible from this module.
 * This is useful to implement optional features (like bundles) in free
 * functions, such that they are tree-shakeable.
 */ class Si {
    constructor(t, n, e, 
    // PORTING NOTE: Manages state synchronization in multi-tab environments.
    s, i, r) {
        this.io = t, this.Iu = n, this.Au = e, this.Ru = s, this.currentUser = i, this.Pu = r, 
        this.Vu = {}, this.yu = new F((t => Fe(t)), Ne), this.gu = new Map, 
        /**
         * The keys of documents that are in limbo for which we haven't yet started a
         * limbo resolution query.
         */
        this.pu = [], 
        /**
         * Keeps track of the target ID for each document that is in limbo with an
         * active target.
         */
        this.vu = new it(B.g), 
        /**
         * Keeps track of the information about an active limbo resolution for each
         * active target ID that was started for the purpose of limbo resolution.
         */
        this.bu = new Map, this.Cu = new Ri, 
        /** Stores user completion handlers, indexed by User and BatchId. */
        this.Su = {}, 
        /** Stores user callbacks waiting for all pending writes to be acknowledged. */
        this.Du = new Map, this.Nu = Fs.pi(), this.onlineState = "Unknown" /* Unknown */ , 
        // The primary state is set to `true` or `false` immediately after Firestore
        // startup. In the interim, a client should only be considered primary if
        // `isPrimary` is true.
        this.Fu = void 0;
    }
    get xu() {
        return !0 === this.Fu;
    }
}

/**
 * Initiates the new listen, resolves promise when listen enqueued to the
 * server. All the subsequent view snapshots or errors are sent to the
 * subscribed handlers. Returns the initial snapshot.
 */
async function Di(t, n) {
    const e = function(t) {
        const n = g(t);
        return n.Iu.oo.To = xi.bind(null, n), n.Iu.oo.qn = Hi.bind(null, n), n.Iu.oo.Eo = ki.bind(null, n), 
        n.Vu.Or = _s.bind(null, n.Au), n.Vu.Ou = fs.bind(null, n.Au), n;
    }(t);
    let s, i;
    const r = e.yu.get(n);
    if (r) 
    // PORTING NOTE: With Multi-Tab Web, it is possible that a query view
    // already exists when EventManager calls us for the first time. This
    // happens when the primary tab is already listening to this query on
    // behalf of another tab and the user of the primary also starts listening
    // to the query. EventManager will not have an assigned target ID in this
    // case and calls `listen` to obtain this ID.
    s = r.targetId, e.Ru.jo(s), i = r.view.Tu(); else {
        const t = await 
        /**
 * Assigns the given target an internal ID so that its results can be pinned so
 * they don't get GC'd. A target must be allocated in the local store before
 * the store can be used to manage its view.
 *
 * Allocating an already allocated `Target` will return the existing `TargetData`
 * for that `Target`.
 */
        function(t, n) {
            const e = g(t);
            return e.persistence.runTransaction("Allocate target", "readwrite", (t => {
                let s;
                return e.xi.ar(t, n).next((i => i ? (
                // This target has been listened to previously, so reuse the
                // previous targetID.
                // TODO(mcg): freshen last accessed date?
                s = i, es.resolve(s)) : e.xi.ku(t).next((i => (s = new X(n, i, 0 /* Listen */ , t.nr), 
                e.xi.Lu(t, s).next((() => s)))))));
            })).then((t => {
                // If Multi-Tab is enabled, the existing target data may be newer than
                // the in-memory data
                const s = e.bi.get(t.targetId);
                return (null === s || t.nt.A(s.nt) > 0) && (e.bi = e.bi.rt(t.targetId, t), e.Ci.set(n, t.targetId)), 
                t;
            }));
        }(e.io, be(n)), r = e.Ru.jo(t.targetId);
        s = t.targetId, i = await 
        /**
 * Registers a view for a previously unknown query and computes its initial
 * snapshot.
 */
        async function(t, n, e, s) {
            // PORTING NOTE: On Web only, we inject the code that registers new Limbo
            // targets based on view changes. This allows us to only depend on Limbo
            // changes when user code includes queries.
            t.$u = (n, e, s) => async function(t, n, e, s) {
                let i = n.view.uu(e);
                i.hu && (
                // The query has a limit and some docs were removed, so we need
                // to re-run the query against the local store to make sure we
                // didn't lose any good docs that had been past the limit.
                i = await qs(t.io, n.query, 
                /* usePreviousResults= */ !1).then((({documents: t}) => n.view.uu(t, i))));
                const r = s && s.Gt.get(n.targetId), o = n.view.lu(i, 
                /* updateLimboDocuments= */ t.xu, r);
                return ji(t, n.targetId, o.du), o.snapshot;
            }(t, n, e, s);
            const i = await qs(t.io, n, 
            /* usePreviousResults= */ !0), r = new vi(n, i.lr), o = r.uu(i.documents), u = Vt.Xt(e, s && "Offline" /* Offline */ !== t.onlineState), c = r.lu(o, 
            /* updateLimboDocuments= */ t.xu, u);
            ji(t, e, c.du);
            const a = new bi(n, e, r);
            t.yu.set(n, a), t.gu.has(e) ? t.gu.get(e).push(n) : t.gu.set(e, [ n ]);
            return c.snapshot;
        }
        /** Stops listening to the query. */ (e, n, s, "current" === r), e.xu && Js(e.Iu, t);
    }
    return i;
}

async function Ni(t, n) {
    const e = g(t), s = e.yu.get(n), i = e.gu.get(s.targetId);
    if (i.length > 1) return e.gu.set(s.targetId, i.filter((t => !Ne(t, n)))), void e.yu.delete(n);
    // No other queries are mapped to the target, clean up the query and the target.
        if (e.xu) {
        // We need to remove the local query target first to allow us to verify
        // whether any other client is still interested in this target.
        e.Ru.Ko(s.targetId);
        e.Ru.Yo(s.targetId) || await Us(e.io, s.targetId, 
        /*keepPersistedTargetData=*/ !1).then((() => {
            e.Ru.zo(s.targetId), Xs(e.Iu, s.targetId), Bi(e, s.targetId);
        })).catch(Bs);
    } else Bi(e, s.targetId), await Us(e.io, s.targetId, 
    /*keepPersistedTargetData=*/ !0);
}

/**
 * Initiates the write of local mutation batch which involves adding the
 * writes to the mutation queue, notifying the remote store about new
 * mutations and raising events for any changes this write caused.
 *
 * The promise returned by this call is resolved when the above steps
 * have completed, *not* when the write was acked by the backend. The
 * userCallback is resolved once the write was acked/rejected by the
 * backend (or failed locally for any other reason).
 */ async function Fi(t, n, e) {
    const s = function(t) {
        const n = g(t);
        return n.Iu.oo.mo = Li.bind(null, n), n.Iu.oo.Io = $i.bind(null, n), n;
    }
    /**
 * @license
 * Copyright 2019 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
    // TOOD(b/140938512): Drop SimpleQueryEngine and rename IndexFreeQueryEngine.
    /**
 * A query engine that takes advantage of the target document mapping in the
 * QueryCache. The IndexFreeQueryEngine optimizes query execution by only
 * reading the documents that previously matched a query plus any documents that were
 * edited after the query was last listened to.
 *
 * There are some cases where Index-Free queries are not guaranteed to produce
 * the same results as full collection scans. In these cases, the
 * IndexFreeQueryEngine falls back to full query processing. These cases are:
 *
 * - Limit queries where a document that matched the query previously no longer
 *   matches the query.
 *
 * - Limit queries where a document edit may cause the document to sort below
 *   another document that is in the local cache.
 *
 * - Queries that have never been CURRENT or free of Limbo documents.
 */ (t);
    try {
        const t = await function(t, n) {
            const e = g(t), s = k.now(), i = n.reduce(((t, n) => t.add(n.key)), Et());
            let r;
            return e.persistence.runTransaction("Locally write mutations", "readwrite", (t => e.ki.si(t, i).next((i => {
                r = i;
                // For non-idempotent mutations (such as `FieldValue.increment()`),
                // we record the base state in a separate patch mutation. This is
                // later used to guarantee consistent values and prevents flicker
                // even if the backend sends us an update that already includes our
                // transform.
                const o = [];
                for (const t of n) {
                    const n = ee(t, r.get(t.key));
                    null != n && 
                    // NOTE: The base state should only be applied if there's some
                    // existing document to override, so use a Precondition of
                    // exists=true
                    o.push(new oe(t.key, n, we(n.proto.mapValue), Jn.exists(!0)));
                }
                return e.Ys.Mu(t, s, o, n);
            })))).then((t => {
                const n = t.Ks(r);
                return {
                    batchId: t.batchId,
                    Uu: n
                };
            }));
        }(s.io, n);
        s.Ru.Bo(t.batchId), function(t, n, e) {
            let s = t.Su[t.currentUser.bs()];
            s || (s = new it(b));
            s = s.rt(n, e), t.Su[t.currentUser.bs()] = s;
        }
        /**
 * Resolves or rejects the user callback for the given batch and then discards
 * it.
 */ (s, t.batchId, e), await Gi(s, t.Uu), await hi(s.Iu);
    } catch (t) {
        // If we can't persist the mutation, we reject the user callback and
        // don't send the mutation. The user can then retry the write.
        const n = us(t, "Failed to persist write");
        e.reject(n);
    }
}

/**
 * Applies one remote event to the sync engine, notifying any views of the
 * changes, and releasing any pending mutation batches that would become
 * visible because of the snapshot version the remote event contains.
 */ async function xi(t, n) {
    const e = g(t);
    try {
        const t = await $s(e.io, n);
        // Update `receivedDocument` as appropriate for any limbo targets.
                n.Gt.forEach(((t, n) => {
            const s = e.bu.get(n);
            s && (
            // Since this is a limbo resolution lookup, it's for a single document
            // and it could be added, modified, or removed, but not a combination.
            y(t.tn.size + t.nn.size + t.en.size <= 1), t.tn.size > 0 ? s.mu = !0 : t.nn.size > 0 ? y(s.mu) : t.en.size > 0 && (y(s.mu), 
            s.mu = !1));
        })), await Gi(e, t, n);
    } catch (t) {
        await Bs(t);
    }
}

/**
 * Applies an OnlineState change to the sync engine and notifies any views of
 * the change.
 */ function Oi(t, n, e) {
    const s = g(t);
    // If we are the secondary client, we explicitly ignore the remote store's
    // online state (the local client may go offline, even though the primary
    // tab remains online) and only apply the primary tab's online state from
    // SharedClientState.
        if (s.xu && 0 /* RemoteStore */ === e || !s.xu && 1 /* SharedClientState */ === e) {
        const t = [];
        s.yu.forEach(((e, s) => {
            const i = s.view.Es(n);
            i.snapshot && t.push(i.snapshot);
        })), function(t, n) {
            const e = g(t);
            e.onlineState = n;
            let s = !1;
            e.queries.forEach(((t, e) => {
                for (const t of e.listeners) 
                // Run global snapshot listeners if a consistent snapshot has been emitted.
                t.Es(n) && (s = !0);
            })), s && ds(e);
        }(s.Au, n), t.length && s.Vu.Or(t), s.onlineState = n, s.xu && s.Ru.Xo(n);
    }
}

/**
 * Rejects the listen for the given targetID. This can be triggered by the
 * backend for any active target.
 *
 * @param syncEngine The sync engine implementation.
 * @param targetId The targetID corresponds to one previously initiated by the
 * user as part of TargetData passed to listen() on RemoteStore.
 * @param err A description of the condition that has forced the rejection.
 * Nearly always this will be an indication that the user is no longer
 * authorized to see the data matching the target.
 */ async function ki(t, n, e) {
    const s = g(t);
    // PORTING NOTE: Multi-tab only.
        s.Ru.Wo(n, "rejected", e);
    const i = s.bu.get(n), r = i && i.key;
    if (r) {
        // TODO(klimt): We really only should do the following on permission
        // denied errors, but we don't have the cause code here.
        // It's a limbo doc. Create a synthetic event saying it was deleted.
        // This is kind of a hack. Ideally, we would have a method in the local
        // store to purge a document. However, it would be tricky to keep all of
        // the local store's invariants with another method.
        let t = new it(B.g);
        t = t.rt(r, new me(r, L.min()));
        const e = Et().add(r), i = new Pt(L.min(), 
        /* targetChanges= */ new Map, 
        /* targetMismatches= */ new ut(b), t, e);
        await xi(s, i), 
        // Since this query failed, we won't want to manually unlisten to it.
        // We only remove it from bookkeeping after we successfully applied the
        // RemoteEvent. If `applyRemoteEvent()` throws, we want to re-listen to
        // this query when the RemoteStore restarts the Watch stream, which should
        // re-trigger the target failure.
        s.vu = s.vu.remove(r), s.bu.delete(n), Ki(s);
    } else await Us(s.io, n, 
    /* keepPersistedTargetData */ !1).then((() => Bi(s, n, e))).catch(Bs);
}

async function Li(t, n) {
    const e = g(t), s = n.batch.batchId;
    try {
        const t = await ks(e.io, n);
        // The local store may or may not be able to apply the write result and
        // raise events immediately (depending on whether the watcher is caught
        // up), so we raise user callbacks first so that they consistently happen
        // before listen events.
                qi(e, s, /*error=*/ null), Ui(e, s), e.Ru.Qo(s, "acknowledged"), await Gi(e, t);
    } catch (t) {
        await Bs(t);
    }
}

async function $i(t, n, e) {
    const s = g(t);
    try {
        const t = await function(t, n) {
            const e = g(t);
            return e.persistence.runTransaction("Reject batch", "readwrite-primary", (t => {
                let s;
                return e.Ys.qu(t, n).next((n => (y(null !== n), s = n.keys(), e.Ys.Yi(t, n)))).next((() => e.Ys.Ji(t))).next((() => e.ki.si(t, s)));
            }));
        }
        /**
 * Returns the largest (latest) batch id in mutation queue that is pending
 * server response.
 *
 * Returns `BATCHID_UNKNOWN` if the queue is empty.
 */ (s.io, n);
        // The local store may or may not be able to apply the write result and
        // raise events immediately (depending on whether the watcher is caught up),
        // so we raise user callbacks first so that they consistently happen before
        // listen events.
                qi(s, n, e), Ui(s, n), s.Ru.Qo(n, "rejected", e), await Gi(s, t);
    } catch (e) {
        await Bs(e);
    }
}

/**
 * Registers a user callback that resolves when all pending mutations at the moment of calling
 * are acknowledged .
 */ async function Mi(t, n) {
    const e = g(t);
    si(e.Iu) || I("SyncEngine", "The network is disabled. The task returned by 'awaitPendingWrites()' will not complete until the network is enabled.");
    try {
        const t = await function(t) {
            const n = g(t);
            return n.persistence.runTransaction("Get highest unacknowledged batch id", "readonly", (t => n.Ys.Bu(t)));
        }(e.io);
        if (-1 === t) 
        // Trigger the callback right away if there is no pending writes at the moment.
        return void n.resolve();
        const s = e.Du.get(t) || [];
        s.push(n), e.Du.set(t, s);
    } catch (t) {
        const e = us(t, "Initialization of waitForPendingWrites() operation failed");
        n.reject(e);
    }
}

/**
 * Triggers the callbacks that are waiting for this batch id to get acknowledged by server,
 * if there are any.
 */ function Ui(t, n) {
    (t.Du.get(n) || []).forEach((t => {
        t.resolve();
    })), t.Du.delete(n);
}

/** Reject all outstanding callbacks waiting for pending writes to complete. */ function qi(t, n, e) {
    const s = g(t);
    let i = s.Su[s.currentUser.bs()];
    // NOTE: Mutations restored from persistence won't have callbacks, so it's
    // okay for there to be no callback for this ID.
        if (i) {
        const t = i.get(n);
        t && (e ? t.reject(e) : t.resolve(), i = i.remove(n)), s.Su[s.currentUser.bs()] = i;
    }
}

function Bi(t, n, e = null) {
    t.Ru.Ko(n);
    for (const s of t.gu.get(n)) t.yu.delete(s), e && t.Vu.Ou(s, e);
    if (t.gu.delete(n), t.xu) {
        t.Cu.No(n).forEach((n => {
            t.Cu.Oo(n) || 
            // We removed the last reference for this key
            Qi(t, n);
        }));
    }
}

function Qi(t, n) {
    // It's possible that the target already got removed because the query failed. In that case,
    // the key won't exist in `limboTargetsByKey`. Only do the cleanup if we still have the target.
    const e = t.vu.get(n);
    null !== e && (Xs(t.Iu, e), t.vu = t.vu.remove(n), t.bu.delete(e), Ki(t));
}

function ji(t, n, e) {
    for (const s of e) if (s instanceof gi) t.Cu.vo(s.key, n), Wi(t, s); else if (s instanceof pi) {
        I("SyncEngine", "Document no longer in limbo: " + s.key), t.Cu.Co(s.key, n);
        t.Cu.Oo(s.key) || 
        // We removed the last reference for this key
        Qi(t, s.key);
    } else V();
}

function Wi(t, n) {
    const e = n.key;
    t.vu.get(e) || (I("SyncEngine", "New document in limbo: " + e), t.pu.push(e), Ki(t));
}

/**
 * Starts listens for documents in limbo that are enqueued for resolution,
 * subject to a maximum number of concurrent resolutions.
 *
 * Without bounding the number of concurrent resolutions, the server can fail
 * with "resource exhausted" errors which can lead to pathological client
 * behavior as seen in https://github.com/firebase/firebase-js-sdk/issues/2683.
 */ function Ki(t) {
    for (;t.pu.length > 0 && t.vu.size < t.Pu; ) {
        const n = t.pu.shift(), e = t.Nu.next();
        t.bu.set(e, new Ci(n)), t.vu = t.vu.rt(n, e), Js(t.Iu, new X(be(Re(n.path)), e, 2 /* LimboResolution */ , bs.Ai));
    }
}

async function Gi(t, n, e) {
    const s = g(t), i = [], r = [], o = [];
    s.yu.T() || (s.yu.forEach(((t, u) => {
        o.push(s.$u(u, n, e).then((t => {
            if (t) {
                s.xu && s.Ru.Wo(u.targetId, t.fromCache ? "not-current" : "current"), i.push(t);
                const n = vs.wi(u.targetId, t);
                r.push(n);
            }
        })));
    })), await Promise.all(o), s.Vu.Or(i), await async function(t, n) {
        const e = g(t);
        try {
            await e.persistence.runTransaction("notifyLocalViewChanges", "readwrite", (t => es.forEach(n, (n => es.forEach(n.fi, (s => e.persistence.ir.vo(t, n.targetId, s))).next((() => es.forEach(n.di, (s => e.persistence.ir.Co(t, n.targetId, s)))))))));
        } catch (t) {
            if (!ss(t)) throw t;
            // If `notifyLocalViewChanges` fails, we did not advance the sequence
            // number for the documents that were included in this transaction.
            // This might trigger them to be deleted earlier than they otherwise
            // would have, but it should not invalidate the integrity of the data.
            I("LocalStore", "Failed to update sequence numbers: " + t);
        }
        for (const t of n) {
            const n = t.targetId;
            if (!t.fromCache) {
                const t = e.bi.get(n), s = t.nt, i = t.it(s);
                // Advance the last limbo free snapshot version
                                e.bi = e.bi.rt(n, i);
            }
        }
    }(s.io, r));
}

async function zi(t, n) {
    const e = g(t);
    if (!e.currentUser.isEqual(n)) {
        I("SyncEngine", "User change. New user:", n.bs());
        const t = await Os(e.io, n);
        e.currentUser = n, 
        // Fails tasks waiting for pending writes requested by previous user.
        function(t, n) {
            t.Du.forEach((t => {
                t.forEach((t => {
                    t.reject(new O(x.CANCELLED, n));
                }));
            })), t.Du.clear();
        }(e, "'waitForPendingWrites' promise is rejected due to a user change."), 
        // TODO(b/114226417): Consider calling this only in the primary tab.
        e.Ru.Jo(n, t.Wi, t.Ki), await Gi(e, t.ji);
    }
}

function Hi(t, n) {
    const e = g(t), s = e.bu.get(n);
    if (s && s.mu) return Et().add(s.key);
    {
        let t = Et();
        const s = e.gu.get(n);
        if (!s) return t;
        for (const n of s) {
            const s = e.yu.get(n);
            t = t.Ot(s.view.ou);
        }
        return t;
    }
}

class Yi {
    Ui(t) {
        this.Qu = t;
    }
    oi(t, n, s, i) {
        // Queries that match all documents don't benefit from using
        // IndexFreeQueries. It is more efficient to scan all documents in a
        // collection, rather than to perform individual lookups.
        return function(t) {
            return 0 === t.filters.length && null === t.limit && null == t.startAt && null == t.endAt && (0 === t.re.length || 1 === t.re.length && t.re[0].field.U());
        }(n) || s.isEqual(L.min()) ? this.ju(t, n) : this.Qu.si(t, i).next((r => {
            const o = this.Wu(n, r);
            return (Pe(n) || Ve(n)) && this.hu(n.limitType, o, i, s) ? this.ju(t, n) : (m() <= e.DEBUG && I("IndexFreeQueryEngine", "Re-using previous result from %s to execute query: %s", s.toString(), xe(n)), 
            this.Qu.oi(t, n, s).next((t => (
            // We merge `previousResults` into `updateResults`, since
            // `updateResults` is already a DocumentMap. If a document is
            // contained in both lists, then its contents are the same.
            o.forEach((n => {
                t = t.rt(n.key, n);
            })), t))));
        }));
        // Queries that have never seen a snapshot without limbo free documents
        // should also be run as a full collection scan.
        }
    /** Applies the query filter and sorting to the provided documents.  */    Wu(t, n) {
        // Sort the documents and re-apply the query filter since previously
        // matching documents do not necessarily still match the query.
        let e = new ut(ke(t));
        return n.forEach(((n, s) => {
            s instanceof Te && Oe(t, s) && (e = e.add(s));
        })), e;
    }
    /**
     * Determines if a limit query needs to be refilled from cache, making it
     * ineligible for index-free execution.
     *
     * @param sortedPreviousResults The documents that matched the query when it
     * was last synchronized, sorted by the query's comparator.
     * @param remoteKeys The document keys that matched the query at the last
     * snapshot.
     * @param limboFreeSnapshotVersion The version of the snapshot when the query
     * was last synchronized.
     */    hu(t, n, e, s) {
        // The query needs to be refilled if a previously matching document no
        // longer matches.
        if (e.size !== n.size) return !0;
        // Limit queries are not eligible for index-free query execution if there is
        // a potential that an older document from cache now sorts before a document
        // that was previously part of the limit. This, however, can only happen if
        // the document at the edge of the limit goes out of limit.
        // If a document that is not the limit boundary sorts differently,
        // the boundary of the limit itself did not change and documents from cache
        // will continue to be "rejected" by this boundary. Therefore, we can ignore
        // any modifications that don't affect the last document.
                const i = "F" /* First */ === t ? n.last() : n.first();
        return !!i && (i.hasPendingWrites || i.version.A(s) > 0);
    }
    ju(t, n) {
        return m() <= e.DEBUG && I("IndexFreeQueryEngine", "Using full collection scan to execute query:", xe(n)), 
        this.Qu.oi(t, n, L.min());
    }
}

/**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */ class Ji {
    constructor(t, n) {
        this.Js = t, this.ir = n, 
        /**
         * The set of all mutations that have been sent but not yet been applied to
         * the backend.
         */
        this.Ys = [], 
        /** Next value to use when assigning sequential IDs to each mutation batch. */
        this.Ku = 1, 
        /** An ordered mapping between documents and the mutations batch IDs. */
        this.Gu = new ut(Pi.Vo);
    }
    zu(t) {
        return es.resolve(0 === this.Ys.length);
    }
    Mu(t, n, e, s) {
        const i = this.Ku;
        if (this.Ku++, this.Ys.length > 0) {
            this.Ys[this.Ys.length - 1];
        }
        const r = new Ps(i, n, e, s);
        this.Ys.push(r);
        // Track references by document key and index collection parents.
        for (const n of s) this.Gu = this.Gu.add(new Pi(n.key, i)), this.Js.Vi(t, n.key.path.C());
        return es.resolve(r);
    }
    qu(t, n) {
        return es.resolve(this.Hu(n));
    }
    cr(t, n) {
        const e = n + 1, s = this.Yu(e), i = s < 0 ? 0 : s;
        // The requested batchId may still be out of range so normalize it to the
        // start of the queue.
                return es.resolve(this.Ys.length > i ? this.Ys[i] : null);
    }
    Bu() {
        return es.resolve(0 === this.Ys.length ? -1 : this.Ku - 1);
    }
    Qi(t) {
        return es.resolve(this.Ys.slice());
    }
    Zs(t, n) {
        const e = new Pi(n, 0), s = new Pi(n, Number.POSITIVE_INFINITY), i = [];
        return this.Gu.Nt([ e, s ], (t => {
            const n = this.Hu(t.ko);
            i.push(n);
        })), es.resolve(i);
    }
    ri(t, n) {
        let e = new ut(b);
        return n.forEach((t => {
            const n = new Pi(t, 0), s = new Pi(t, Number.POSITIVE_INFINITY);
            this.Gu.Nt([ n, s ], (t => {
                e = e.add(t.ko);
            }));
        })), es.resolve(this.Ju(e));
    }
    li(t, n) {
        // Use the query path as a prefix for testing if a document matches the
        // query.
        const e = n.path, s = e.length + 1;
        // Construct a document reference for actually scanning the index. Unlike
        // the prefix the document key in this reference must have an even number of
        // segments. The empty segment can be used a suffix of the query path
        // because it precedes all other segments in an ordered traversal.
        let i = e;
        B.G(i) || (i = i.child(""));
        const r = new Pi(new B(i), 0);
        // Find unique batchIDs referenced by all documents potentially matching the
        // query.
                let o = new ut(b);
        return this.Gu.Ft((t => {
            const n = t.key.path;
            return !!e.N(n) && (
            // Rows with document keys more than one segment longer than the query
            // path can't be matches. For example, a query on 'rooms' can't match
            // the document /rooms/abc/messages/xyx.
            // TODO(mcg): we'll need a different scanner when we implement
            // ancestor queries.
            n.length === s && (o = o.add(t.ko)), !0);
        }), r), es.resolve(this.Ju(o));
    }
    Ju(t) {
        // Construct an array of matching batches, sorted by batchID to ensure that
        // multiple mutations affecting the same document key are applied in order.
        const n = [];
        return t.forEach((t => {
            const e = this.Hu(t);
            null !== e && n.push(e);
        })), n;
    }
    Yi(t, n) {
        y(0 === this.Xu(n.batchId, "removed")), this.Ys.shift();
        let e = this.Gu;
        return es.forEach(n.mutations, (s => {
            const i = new Pi(s.key, n.batchId);
            return e = e.delete(i), this.ir.Zu(t, s.key);
        })).next((() => {
            this.Gu = e;
        }));
    }
    tc(t) {
        // No-op since the memory mutation queue does not maintain a separate cache.
    }
    Oo(t, n) {
        const e = new Pi(n, 0), s = this.Gu.xt(e);
        return es.resolve(n.isEqual(s && s.key));
    }
    Ji(t) {
        return this.Ys.length, es.resolve();
    }
    /**
     * Finds the index of the given batchId in the mutation queue and asserts that
     * the resulting index is within the bounds of the queue.
     *
     * @param batchId The batchId to search for
     * @param action A description of what the caller is doing, phrased in passive
     * form (e.g. "acknowledged" in a routine that acknowledges batches).
     */    Xu(t, n) {
        return this.Yu(t);
    }
    /**
     * Finds the index of the given batchId in the mutation queue. This operation
     * is O(1).
     *
     * @return The computed index of the batch with the given batchId, based on
     * the state of the queue. Note this index can be negative if the requested
     * batchId has already been remvoed from the queue or past the end of the
     * queue if the batchId is larger than the last added batch.
     */    Yu(t) {
        if (0 === this.Ys.length) 
        // As an index this is past the end of the queue
        return 0;
        // Examine the front of the queue to figure out the difference between the
        // batchId and indexes in the array. Note that since the queue is ordered
        // by batchId, if the first batch has a larger batchId then the requested
        // batchId doesn't exist in the queue.
                return t - this.Ys[0].batchId;
    }
    /**
     * A version of lookupMutationBatch that doesn't return a promise, this makes
     * other functions that uses this code easier to read and more efficent.
     */    Hu(t) {
        const n = this.Yu(t);
        if (n < 0 || n >= this.Ys.length) return null;
        return this.Ys[n];
    }
}

/**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/**
 * The memory-only RemoteDocumentCache for IndexedDb. To construct, invoke
 * `newMemoryRemoteDocumentCache()`.
 */
class Xi {
    /**
     * @param sizer Used to assess the size of a document. For eager GC, this is expected to just
     * return 0 to avoid unnecessarily doing the work of calculating the size.
     */
    constructor(t, n) {
        this.Js = t, this.nc = n, 
        /** Underlying cache of documents and their read times. */
        this.docs = new it(B.g), 
        /** Size of all cached documents. */
        this.size = 0;
    }
    /**
     * Adds the supplied entry to the cache and updates the cache size as appropriate.
     *
     * All calls of `addEntry`  are required to go through the RemoteDocumentChangeBuffer
     * returned by `newChangeBuffer()`.
     */    Hi(t, n, e) {
        const s = n.key, i = this.docs.get(s), r = i ? i.size : 0, o = this.nc(n);
        return this.docs = this.docs.rt(s, {
            ec: n,
            size: o,
            readTime: e
        }), this.size += o - r, this.Js.Vi(t, s.path.C());
    }
    /**
     * Removes the specified entry from the cache and updates the cache size as appropriate.
     *
     * All calls of `removeEntry` are required to go through the RemoteDocumentChangeBuffer
     * returned by `newChangeBuffer()`.
     */    rr(t) {
        const n = this.docs.get(t);
        n && (this.docs = this.docs.remove(t), this.size -= n.size);
    }
    ni(t, n) {
        const e = this.docs.get(n);
        return es.resolve(e ? e.ec : null);
    }
    getEntries(t, n) {
        let e = lt();
        return n.forEach((t => {
            const n = this.docs.get(t);
            e = e.rt(t, n ? n.ec : null);
        })), es.resolve(e);
    }
    oi(t, n, e) {
        let s = ft();
        // Documents are ordered by key, so we can use a prefix scan to narrow down
        // the documents we need to match the query against.
                const i = new B(n.path.child("")), r = this.docs._t(i);
        for (;r.It(); ) {
            const {key: t, value: {ec: i, readTime: o}} = r.Tt();
            if (!n.path.N(t.path)) break;
            o.A(e) <= 0 || i instanceof Te && Oe(n, i) && (s = s.rt(i.key, i));
        }
        return es.resolve(s);
    }
    sc(t, n) {
        return es.forEach(this.docs, (t => n(t)));
    }
    Gi(t) {
        // `trackRemovals` is ignores since the MemoryRemoteDocumentCache keeps
        // a separate changelog and does not need special handling for removals.
        return new Zi(this);
    }
    ic(t) {
        return es.resolve(this.size);
    }
}

/**
 * Creates a new memory-only RemoteDocumentCache.
 *
 * @param indexManager A class that manages collection group indices.
 * @param sizer Used to assess the size of a document. For eager GC, this is expected to just
 * return 0 to avoid unnecessarily doing the work of calculating the size.
 */
/**
 * Handles the details of adding and updating documents in the MemoryRemoteDocumentCache.
 */
class Zi extends class {
    constructor() {
        // A mapping of document key to the new cache entry that should be written (or null if any
        // existing cache entry should be removed).
        this.Uu = new F((t => t.toString()), ((t, n) => t.isEqual(n))), this.rc = !1;
    }
    oc(t) {
        const n = this.Uu.get(t);
        return n ? n.readTime : L.min();
    }
    /**
     * Buffers a `RemoteDocumentCache.addEntry()` call.
     *
     * You can only modify documents that have already been retrieved via
     * `getEntry()/getEntries()` (enforced via IndexedDbs `apply()`).
     */    Hi(t, n) {
        this.uc(), this.Uu.set(t.key, {
            ec: t,
            readTime: n
        });
    }
    /**
     * Buffers a `RemoteDocumentCache.removeEntry()` call.
     *
     * You can only remove documents that have already been retrieved via
     * `getEntry()/getEntries()` (enforced via IndexedDbs `apply()`).
     */    rr(t, n = null) {
        this.uc(), this.Uu.set(t, {
            ec: null,
            readTime: n
        });
    }
    /**
     * Looks up an entry in the cache. The buffered changes will first be checked,
     * and if no buffered change applies, this will forward to
     * `RemoteDocumentCache.getEntry()`.
     *
     * @param transaction The transaction in which to perform any persistence
     *     operations.
     * @param documentKey The key of the entry to look up.
     * @return The cached Document or NoDocument entry, or null if we have nothing
     * cached.
     */    ni(t, n) {
        this.uc();
        const e = this.Uu.get(n);
        return void 0 !== e ? es.resolve(e.ec) : this.cc(t, n);
    }
    /**
     * Looks up several entries in the cache, forwarding to
     * `RemoteDocumentCache.getEntry()`.
     *
     * @param transaction The transaction in which to perform any persistence
     *     operations.
     * @param documentKeys The keys of the entries to look up.
     * @return A map of cached `Document`s or `NoDocument`s, indexed by key. If an
     *     entry cannot be found, the corresponding key will be mapped to a null
     *     value.
     */    getEntries(t, n) {
        return this.ac(t, n);
    }
    /**
     * Applies buffered changes to the underlying RemoteDocumentCache, using
     * the provided transaction.
     */    apply(t) {
        return this.uc(), this.rc = !0, this.lu(t);
    }
    /** Helper to assert this.changes is not null  */    uc() {}
} {
    constructor(t) {
        super(), this.hc = t;
    }
    lu(t) {
        const n = [];
        return this.Uu.forEach(((e, s) => {
            s && s.ec ? n.push(this.hc.Hi(t, s.ec, this.oc(e))) : this.hc.rr(e);
        })), es.Fe(n);
    }
    cc(t, n) {
        return this.hc.ni(t, n);
    }
    ac(t, n) {
        return this.hc.getEntries(t, n);
    }
}

/**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */ class tr {
    constructor(t) {
        this.persistence = t, 
        /**
         * Maps a target to the data about that target
         */
        this.lc = new F((t => G(t)), H), 
        /** The last received snapshot version. */
        this.lastRemoteSnapshotVersion = L.min(), 
        /** The highest numbered target ID encountered. */
        this.highestTargetId = 0, 
        /** The highest sequence number encountered. */
        this._c = 0, 
        /**
         * A ordered bidirectional mapping between documents and the remote target
         * IDs.
         */
        this.fc = new Ri, this.targetCount = 0, this.dc = Fs.gi();
    }
    Dn(t, n) {
        return this.lc.forEach(((t, e) => n(e))), es.resolve();
    }
    Xi(t) {
        return es.resolve(this.lastRemoteSnapshotVersion);
    }
    wc(t) {
        return es.resolve(this._c);
    }
    ku(t) {
        return this.highestTargetId = this.dc.next(), es.resolve(this.highestTargetId);
    }
    ur(t, n, e) {
        return e && (this.lastRemoteSnapshotVersion = e), n > this._c && (this._c = n), 
        es.resolve();
    }
    Ec(t) {
        this.lc.set(t.target, t);
        const n = t.targetId;
        n > this.highestTargetId && (this.dc = new Fs(n), this.highestTargetId = n), t.sequenceNumber > this._c && (this._c = t.sequenceNumber);
    }
    Lu(t, n) {
        return this.Ec(n), this.targetCount += 1, es.resolve();
    }
    er(t, n) {
        return this.Ec(n), es.resolve();
    }
    Tc(t, n) {
        return this.lc.delete(n.target), this.fc.No(n.targetId), this.targetCount -= 1, 
        es.resolve();
    }
    mc(t, n, e) {
        let s = 0;
        const i = [];
        return this.lc.forEach(((r, o) => {
            o.sequenceNumber <= n && null === e.get(o.targetId) && (this.lc.delete(r), i.push(this.Ic(t, o.targetId)), 
            s++);
        })), es.Fe(i).next((() => s));
    }
    Ac(t) {
        return es.resolve(this.targetCount);
    }
    ar(t, n) {
        const e = this.lc.get(n) || null;
        return es.resolve(e);
    }
    tr(t, n, e) {
        return this.fc.bo(n, e), es.resolve();
    }
    Zi(t, n, e) {
        this.fc.Do(n, e);
        const s = this.persistence.ir, i = [];
        return s && n.forEach((n => {
            i.push(s.Zu(t, n));
        })), es.Fe(i);
    }
    Ic(t, n) {
        return this.fc.No(n), es.resolve();
    }
    hr(t, n) {
        const e = this.fc.xo(n);
        return es.resolve(e);
    }
    Oo(t, n) {
        return es.resolve(this.fc.Oo(n));
    }
}

/**
 * @license
 * Copyright 2020 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */ class nr {
    constructor(t) {
        this.serializer = t, this.Rc = new Map, this.Pc = new Map;
    }
    Vc(t, n) {
        return es.resolve(this.Rc.get(n));
    }
    yc(t, n) {
        /** Encodes a BundleMetadata proto object to a Bundle model object. */
        var e;
        return this.Rc.set(n.id, {
            id: (e = n).id,
            version: e.version,
            createTime: un(e.createTime)
        }), es.resolve();
    }
    gc(t, n) {
        return es.resolve(this.Pc.get(n));
    }
    vc(t, n) {
        var e;
        return this.Pc.set(n.name, {
            name: (e = n).name,
            query: Ss(e.bundledQuery),
            readTime: un(e.readTime)
        }), es.resolve();
    }
}

/**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/**
 * A memory-backed instance of Persistence. Data is stored only in RAM and
 * not persisted across sessions.
 */
class er {
    /**
     * The constructor accepts a factory for creating a reference delegate. This
     * allows both the delegate and this instance to have strong references to
     * each other without having nullable fields that would then need to be
     * checked or asserted on every access.
     */
    constructor(t, n) {
        this.bc = {}, this.Cc = new bs(0), this.Sc = !1, this.Sc = !0, this.ir = t(this), 
        this.xi = new tr(this);
        this.Js = new Ds, this.Hs = function(t, n) {
            return new Xi(t, n);
        }(this.Js, (t => this.ir.Dc(t))), this.serializer = new Cs(n), this.$i = new nr(this.serializer);
    }
    start() {
        return Promise.resolve();
    }
    Zo() {
        // No durable state to ensure is closed on shutdown.
        return this.Sc = !1, Promise.resolve();
    }
    get Nc() {
        return this.Sc;
    }
    Fc() {
        // No op.
    }
    xc() {
        // No op.
    }
    Li() {
        return this.Js;
    }
    Di(t) {
        let n = this.bc[t.bs()];
        return n || (n = new Ji(this.Js, this.ir), this.bc[t.bs()] = n), n;
    }
    Oi() {
        return this.xi;
    }
    Fi() {
        return this.Hs;
    }
    Mi() {
        return this.$i;
    }
    runTransaction(t, n, e) {
        I("MemoryPersistence", "Starting transaction:", t);
        const s = new sr(this.Cc.next());
        return this.ir.Oc(), e(s).next((t => this.ir.kc(s).next((() => t)))).De().then((t => (s.Lc(), 
        t)));
    }
    $c(t, n) {
        return es.xe(Object.values(this.bc).map((e => () => e.Oo(t, n))));
    }
}

/**
 * Memory persistence is not actually transactional, but future implementations
 * may have transaction-scoped state.
 */ class sr extends 
/**
 * A base class representing a persistence transaction, encapsulating both the
 * transaction's sequence numbers as well as a list of onCommitted listeners.
 *
 * When you call Persistence.runTransaction(), it will create a transaction and
 * pass it to your callback. You then pass it to any method that operates
 * on persistence.
 */
class {
    constructor() {
        this.Mc = [];
    }
    Uc(t) {
        this.Mc.push(t);
    }
    Lc() {
        this.Mc.forEach((t => t()));
    }
} {
    constructor(t) {
        super(), this.nr = t;
    }
}

class ir {
    constructor(t) {
        this.persistence = t, 
        /** Tracks all documents that are active in Query views. */
        this.qc = new Ri, 
        /** The list of documents that are potentially GCed after each transaction. */
        this.Bc = null;
    }
    static Qc(t) {
        return new ir(t);
    }
    get jc() {
        if (this.Bc) return this.Bc;
        throw V();
    }
    vo(t, n, e) {
        return this.qc.vo(e, n), this.jc.delete(e.toString()), es.resolve();
    }
    Co(t, n, e) {
        return this.qc.Co(e, n), this.jc.add(e.toString()), es.resolve();
    }
    Zu(t, n) {
        return this.jc.add(n.toString()), es.resolve();
    }
    removeTarget(t, n) {
        this.qc.No(n.targetId).forEach((t => this.jc.add(t.toString())));
        const e = this.persistence.Oi();
        return e.hr(t, n.targetId).next((t => {
            t.forEach((t => this.jc.add(t.toString())));
        })).next((() => e.Tc(t, n)));
    }
    Oc() {
        this.Bc = new Set;
    }
    kc(t) {
        // Remove newly orphaned documents.
        const n = this.persistence.Fi().Gi();
        return es.forEach(this.jc, (e => {
            const s = B.j(e);
            return this.Wc(t, s).next((t => {
                t || n.rr(s);
            }));
        })).next((() => (this.Bc = null, n.apply(t))));
    }
    sr(t, n) {
        return this.Wc(t, n).next((t => {
            t ? this.jc.delete(n.toString()) : this.jc.add(n.toString());
        }));
    }
    Dc(t) {
        // For eager GC, we don't care about the document size, there are no size thresholds.
        return 0;
    }
    Wc(t, n) {
        return es.xe([ () => es.resolve(this.qc.Oo(n)), () => this.persistence.Oi().Oo(t, n), () => this.persistence.$c(t, n) ]);
    }
}

/**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/**
 * Provides a simple helper class that implements the Stream interface to
 * bridge to other implementations that are streams but do not implement the
 * interface. The stream callbacks are invoked with the callOn... methods.
 */ class rr {
    constructor(t) {
        this.Kc = t.Kc, this.Gc = t.Gc;
    }
    Nr(t) {
        this.zc = t;
    }
    vr(t) {
        this.Hc = t;
    }
    onMessage(t) {
        this.Yc = t;
    }
    close() {
        this.Gc();
    }
    send(t) {
        this.Kc(t);
    }
    Jc() {
        this.zc();
    }
    Xc(t) {
        this.Hc(t);
    }
    Zc(t) {
        this.Yc(t);
    }
}

/**
 * @license
 * Copyright 2020 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */ const or = {
    BatchGetDocuments: "batchGet",
    Commit: "commit",
    RunQuery: "runQuery"
};

/**
 * Maps RPC names to the corresponding REST endpoint name.
 *
 * We use array notation to avoid mangling.
 */ class ur extends 
/**
 * Base class for all Rest-based connections to the backend (WebChannel and
 * HTTP).
 */
class {
    constructor(t) {
        this.ta = t, this.t = t.t;
        const n = t.ssl ? "https" : "http";
        this.na = n + "://" + t.host, this.ea = "projects/" + this.t.projectId + "/databases/" + this.t.database + "/documents";
    }
    Kr(t, n, e, s) {
        const i = this.sa(t, n);
        I("RestConnection", "Sending: ", i, e);
        const r = {};
        return this.ia(r, s), this.ra(t, i, r, e).then((t => (I("RestConnection", "Received: ", t), 
        t)), (n => {
            throw R("RestConnection", t + " failed with error: ", n, "url: ", i, "request:", e), 
            n;
        }));
    }
    Gr(t, n, e, s) {
        // The REST API automatically aggregates all of the streamed results, so we
        // can just use the normal invoke() method.
        return this.Kr(t, n, e, s);
    }
    /**
     * Modifies the headers for a request, adding any authorization token if
     * present and any additional headers for the request.
     */    ia(t, n) {
        if (t["X-Goog-Api-Client"] = "gl-js/ fire/8.0.2", 
        // Content-Type: text/plain will avoid preflight requests which might
        // mess with CORS and redirects by proxies. If we add custom headers
        // we will need to change this code to potentially use the $httpOverwrite
        // parameter supported by ESF to avoid triggering preflight requests.
        t["Content-Type"] = "text/plain", n) for (const e in n.Ds) n.Ds.hasOwnProperty(e) && (t[e] = n.Ds[e]);
    }
    sa(t, n) {
        const e = or[t];
        return `${this.na}/v1/${n}:${e}`;
    }
}
/**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */ {
    constructor(t) {
        super(t), this.forceLongPolling = t.forceLongPolling, this.i = t.i;
    }
    ra(t, n, e, s) {
        return new Promise(((i, r) => {
            const o = new a;
            o.listenOnce(h.COMPLETE, (() => {
                try {
                    switch (o.getLastErrorCode()) {
                      case l.NO_ERROR:
                        const n = o.getResponseJson();
                        I("Connection", "XHR received:", JSON.stringify(n)), i(n);
                        break;

                      case l.TIMEOUT:
                        I("Connection", 'RPC "' + t + '" timed out'), r(new O(x.DEADLINE_EXCEEDED, "Request time out"));
                        break;

                      case l.HTTP_ERROR:
                        const e = o.getStatus();
                        if (I("Connection", 'RPC "' + t + '" failed with status:', e, "response text:", o.getResponseText()), 
                        e > 0) {
                            const t = o.getResponseJson().error;
                            if (t && t.status && t.message) {
                                const n = function(t) {
                                    const n = t.toLowerCase().replace("_", "-");
                                    return Object.values(x).indexOf(n) >= 0 ? n : x.UNKNOWN;
                                }(t.status);
                                r(new O(n, t.message));
                            } else r(new O(x.UNKNOWN, "Server responded with status " + o.getStatus()));
                        } else 
                        // If we received an HTTP_ERROR but there's no status code,
                        // it's most probably a connection issue
                        r(new O(x.UNAVAILABLE, "Connection failed."));
                        break;

                      default:
                        V();
                    }
                } finally {
                    I("Connection", 'RPC "' + t + '" completed.');
                }
            }));
            const u = JSON.stringify(s);
            o.send(n, "POST", u, e, 15);
        }));
    }
    Fr(t, n) {
        const e = [ this.na, "/", "google.firestore.v1.Firestore", "/", t, "/channel" ], a = _(), h = {
            // Required for backend stickiness, routing behavior is based on this
            // parameter.
            httpSessionIdParam: "gsessionid",
            initMessageHeaders: {},
            messageUrlParams: {
                // This param is used to improve routing and project isolation by the
                // backend and must be included in every request.
                database: `projects/${this.t.projectId}/databases/${this.t.database}`
            },
            sendRawJson: !0,
            supportsCrossDomainXhr: !0,
            internalChannelParams: {
                // Override the default timeout (randomized between 10-20 seconds) since
                // a large write batch on a slow internet connection may take a long
                // time to send to the backend. Rather than have WebChannel impose a
                // tight timeout which could lead to infinite timeouts and retries, we
                // set it very large (5-10 minutes) and rely on the browser's builtin
                // timeouts to kick in if the request isn't working.
                forwardChannelRequestTimeoutMs: 6e5
            },
            forceLongPolling: this.forceLongPolling,
            detectBufferingProxy: this.i
        };
        this.ia(h.initMessageHeaders, n), 
        // Sending the custom headers we just added to request.initMessageHeaders
        // (Authorization, etc.) will trigger the browser to make a CORS preflight
        // request because the XHR will no longer meet the criteria for a "simple"
        // CORS request:
        // https://developer.mozilla.org/en-US/docs/Web/HTTP/CORS#Simple_requests
        // Therefore to avoid the CORS preflight request (an extra network
        // roundtrip), we use the httpHeadersOverwriteParam option to specify that
        // the headers should instead be encoded into a special "$httpHeaders" query
        // parameter, which is recognized by the webchannel backend. This is
        // formally defined here:
        // https://github.com/google/closure-library/blob/b0e1815b13fb92a46d7c9b3c30de5d6a396a3245/closure/goog/net/rpc/httpcors.js#L32
        // TODO(b/145624756): There is a backend bug where $httpHeaders isn't respected if the request
        // doesn't have an Origin header. So we have to exclude a few browser environments that are
        // known to (sometimes) not include an Origin. See
        // https://github.com/firebase/firebase-js-sdk/issues/1491.
        s() || i() || r() || o() || u() || c() || (h.httpHeadersOverwriteParam = "$httpHeaders");
        const l = e.join("");
        I("Connection", "Creating WebChannel: " + l, h);
        const d = a.createWebChannel(l, h);
        // WebChannel supports sending the first message with the handshake - saving
        // a network round trip. However, it will have to call send in the same
        // JS event loop as open. In order to enforce this, we delay actually
        // opening the WebChannel until send is called. Whether we have called
        // open is tracked with this variable.
                let w = !1, E = !1;
        // A flag to determine whether the stream was closed (by us or through an
        // error/close event) to avoid delivering multiple close events or sending
        // on a closed stream
                const T = new rr({
            Kc: t => {
                E ? I("Connection", "Not sending because WebChannel is closed:", t) : (w || (I("Connection", "Opening WebChannel transport."), 
                d.open(), w = !0), I("Connection", "WebChannel sending:", t), d.send(t));
            },
            Gc: () => d.close()
        }), m = (t, n) => {
            // TODO(dimond): closure typing seems broken because WebChannel does
            // not implement goog.events.Listenable
            d.listen(t, (t => {
                try {
                    n(t);
                } catch (t) {
                    setTimeout((() => {
                        throw t;
                    }), 0);
                }
            }));
        };
        // Closure events are guarded and exceptions are swallowed, so catch any
        // exception and rethrow using a setTimeout so they become visible again.
        // Note that eventually this function could go away if we are confident
        // enough the code is exception free.
                return m(f.EventType.OPEN, (() => {
            E || I("Connection", "WebChannel transport opened.");
        })), m(f.EventType.CLOSE, (() => {
            E || (E = !0, I("Connection", "WebChannel transport closed"), T.Xc());
        })), m(f.EventType.ERROR, (t => {
            E || (E = !0, R("Connection", "WebChannel transport errored:", t), T.Xc(new O(x.UNAVAILABLE, "The operation could not be completed")));
        })), m(f.EventType.MESSAGE, (t => {
            var n;
            if (!E) {
                const e = t.data[0];
                y(!!e);
                // TODO(b/35143891): There is a bug in One Platform that caused errors
                // (and only errors) to be wrapped in an extra array. To be forward
                // compatible with the bug we need to check either condition. The latter
                // can be removed once the fix has been rolled out.
                // Use any because msgData.error is not typed.
                const s = e, i = s.error || (null === (n = s[0]) || void 0 === n ? void 0 : n.error);
                if (i) {
                    I("Connection", "WebChannel received error:", i);
                    // error.status will be a string like 'OK' or 'NOT_FOUND'.
                    const t = i.status;
                    let n = function(t) {
                        // lookup by string
                        // eslint-disable-next-line @typescript-eslint/no-explicit-any
                        const n = tt[t];
                        if (void 0 !== n) return st(n);
                    }(t), e = i.message;
                    void 0 === n && (n = x.INTERNAL, e = "Unknown error status: " + t + " with message " + i.message), 
                    // Mark closed so no further events are propagated
                    E = !0, T.Xc(new O(n, e)), d.close();
                } else I("Connection", "WebChannel received:", e), T.Zc(e);
            }
        })), setTimeout((() => {
            // Technically we could/should wait for the WebChannel opened event,
            // but because we want to send the first message with the WebChannel
            // handshake we pretend the channel opened here (asynchronously), and
            // then delay the actual open until the first message is sent.
            T.Jc();
        }), 0), T;
    }
}

/**
 * @license
 * Copyright 2019 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
// References to `window` are guarded by BrowserConnectivityMonitor.isAvailable()
/* eslint-disable no-restricted-globals */
/**
 * Browser implementation of ConnectivityMonitor.
 */
class cr {
    constructor() {
        this.oa = () => this.ua(), this.ca = () => this.aa(), this.ha = [], this.la();
    }
    _o(t) {
        this.ha.push(t);
    }
    Zo() {
        window.removeEventListener("online", this.oa), window.removeEventListener("offline", this.ca);
    }
    la() {
        window.addEventListener("online", this.oa), window.addEventListener("offline", this.ca);
    }
    ua() {
        I("ConnectivityMonitor", "Network connectivity changed: AVAILABLE");
        for (const t of this.ha) t(0 /* AVAILABLE */);
    }
    aa() {
        I("ConnectivityMonitor", "Network connectivity changed: UNAVAILABLE");
        for (const t of this.ha) t(1 /* UNAVAILABLE */);
    }
    // TODO(chenbrian): Consider passing in window either into this component or
    // here for testing via FakeWindow.
    /** Checks that all used attributes of window are available. */
    static _a() {
        return "undefined" != typeof window && void 0 !== window.addEventListener && void 0 !== window.removeEventListener;
    }
}

/**
 * @license
 * Copyright 2019 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */ class ar {
    _o(t) {
        // No-op.
    }
    Zo() {
        // No-op.
    }
}

/**
 * @license
 * Copyright 2020 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/** Initializes the WebChannelConnection for the browser. */
/**
 * @license
 * Copyright 2020 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/**
 * Provides all components needed for Firestore with in-memory persistence.
 * Uses EagerGC garbage collection.
 */
class hr {
    constructor() {
        this.synchronizeTabs = !1;
    }
    async initialize(t) {
        this.serializer = ys(t.ta.t), this.Ru = this.fa(t), this.persistence = this.da(t), 
        await this.persistence.start(), this.wa = this.Ea(t), this.io = this.Ta(t);
    }
    Ea(t) {
        return null;
    }
    Ta(t) {
        /** Manages our in-memory or durable persistence. */
        return n = this.persistence, e = new Yi, s = t.ma, i = this.serializer, new xs(n, e, s, i);
        var n, e, s, i;
    }
    da(t) {
        return new er(ir.Qc, this.serializer);
    }
    fa(t) {
        return new yi;
    }
    async terminate() {
        this.wa && this.wa.stop(), await this.Ru.Zo(), await this.persistence.Zo();
    }
}

/**
 * Initializes and wires the components that are needed to interface with the
 * network.
 */ class lr {
    async initialize(t, n) {
        this.io || (this.io = t.io, this.Ru = t.Ru, this.ro = this.Ia(n), this.Iu = this.Aa(n), 
        this.Au = this.Ra(n), this.Pa = this.Va(n, 
        /* startAsPrimary=*/ !t.synchronizeTabs), this.Ru.zr = t => Oi(this.Pa, t, 1 /* SharedClientState */), 
        this.Iu.oo.ya = zi.bind(null, this.Pa), await 
        /**
 * Toggles the network state when the client gains or loses its primary lease.
 */
        async function(t, n) {
            const e = g(t);
            n ? (e.ao.delete(2 /* IsSecondary */), await Hs(e)) : n || (e.ao.add(2 /* IsSecondary */), 
            await Ys(e), e.fo.set("Unknown" /* Unknown */));
        }(this.Iu, this.Pa.xu));
    }
    Ra(t) {
        return new as;
    }
    Ia(t) {
        const n = ys(t.ta.t), e = (s = t.ta, new ur(s));
        var s;
        /** Return the Platform-specific connectivity monitor. */        return function(t, n, e) {
            return new Ks(t, n, e);
        }(t.credentials, e, n);
    }
    Aa(t) {
        return n = this.io, e = this.ro, s = t.Oe, i = t => Oi(this.Pa, t, 0 /* RemoteStore */), 
        r = cr._a() ? new cr : new ar, new zs(n, e, s, i, r);
        var n, e, s, i, r;
        /** Re-enables the network. Idempotent. */    }
    Va(t, n) {
        return function(t, n, e, 
        // PORTING NOTE: Manages state synchronization in multi-tab environments.
        s, i, r, o) {
            const u = new Si(t, n, e, s, i, r);
            return o && (u.Fu = !0), u;
        }(this.io, this.Iu, this.Au, this.Ru, t.ma, t.Pu, n);
    }
    terminate() {
        return async function(t) {
            const n = g(t);
            I("RemoteStore", "RemoteStore shutting down."), n.ao.add(5 /* Shutdown */), await Ys(n), 
            n.lo.Zo(), 
            // Set the OnlineState to Unknown (rather than Offline) to avoid potentially
            // triggering spurious listener events with cached data, etc.
            n.fo.set("Unknown" /* Unknown */);
        }(this.Iu);
    }
}

/**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/*
 * A wrapper implementation of Observer<T> that will dispatch events
 * asynchronously. To allow immediate silencing, a mute call is added which
 * causes events scheduled to no longer be raised.
 */ class _r {
    constructor(t) {
        this.observer = t, 
        /**
         * When set to true, will not raise future events. Necessary to deal with
         * async detachment of listener.
         */
        this.muted = !1;
    }
    next(t) {
        this.observer.next && this.ga(this.observer.next, t);
    }
    error(t) {
        this.observer.error ? this.ga(this.observer.error, t) : console.error("Uncaught Error in snapshot listener:", t);
    }
    pa() {
        this.muted = !0;
    }
    ga(t, n) {
        this.muted || setTimeout((() => {
            this.muted || t(n);
        }), 0);
    }
}

/**
 * @license
 * Copyright 2020 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/**
 * A class implemented by all API types of the legacy Firestore API which
 * contains a reference to the API type in the firestore-exp API. All internal
 * code unwraps these references, which allows us to only use firestore-exp
 * types in the SDK.
 */ class fr {
    constructor(t) {
        this.va = t;
    }
}

/**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */ function dr(t, n, e) {
    if (!e) throw new O(x.INVALID_ARGUMENT, `Function ${t}() cannot be called with an empty ${n}.`);
}

function wr(t, n) {
    if (void 0 === n) return {
        merge: !1
    };
    if (void 0 !== n.mergeFields && void 0 !== n.merge) throw new O(x.INVALID_ARGUMENT, `Invalid options passed to function ${t}(): You cannot specify both "merge" and "mergeFields".`);
    return n;
}

/**
 * Validates that two boolean options are not set at the same time.
 */ function Er(t, n, e, s) {
    if (!0 === n && !0 === s) throw new O(x.INVALID_ARGUMENT, `${t} and ${e} cannot be used together.`);
}

/**
 * Returns true if it's a non-null object without a custom prototype
 * (i.e. excludes Array, Date, etc.).
 */
/** Returns a string describing the type / value of the provided input. */
function Tr(t) {
    if (void 0 === t) return "undefined";
    if (null === t) return "null";
    if ("string" == typeof t) return t.length > 20 && (t = t.substring(0, 20) + "..."), 
    JSON.stringify(t);
    if ("number" == typeof t || "boolean" == typeof t) return "" + t;
    if ("object" == typeof t) {
        if (t instanceof Array) return "an array";
        {
            const n = 
            /** Hacky method to try to get the constructor name for an object. */
            function(t) {
                if (t.constructor) {
                    const n = /function\s+([^\s(]+)\s*\(/.exec(t.constructor.toString());
                    if (n && n.length > 1) return n[1];
                }
                return null;
            }
            /**
 * Casts `obj` to `T`. Throws if  `obj` is not an instance of `T`.
 *
 * This cast is used in the Lite and Full SDK to verify instance types for
 * arguments passed to the public API.
 */ (t);
            return n ? `a custom ${n} object` : "an object";
        }
    }
    return "function" == typeof t ? "a function" : V();
}

function mr(t, 
// eslint-disable-next-line @typescript-eslint/no-explicit-any
n) {
    if (t instanceof fr && (t = t.va), !(t instanceof n)) {
        if (n.name === t.constructor.name) throw new O(x.INVALID_ARGUMENT, "Type does not match the expected instance. Did you pass a reference from a different Firestore SDK?");
        {
            const e = Tr(t);
            throw new O(x.INVALID_ARGUMENT, `Expected type '${n.name}', but it was: ${e}`);
        }
    }
    return t;
}

function Ir(t, n) {
    if (n <= 0) throw new O(x.INVALID_ARGUMENT, `Function ${t}() requires a positive number, but it was: ${n}.`);
}

/**
 * @license
 * Copyright 2020 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/**
 * Sentinel values that can be used when writing document fields with `set()`
 * or `update()`.
 */ class Ar {
    /**
     * @param _methodName The public API endpoint that returns this class.
     */
    constructor(t) {
        this.ba = t;
    }
}

/**
 * Returns a sentinel for use with {@link updateDoc()} or
 * {@link setDoc `setDoc({}, { merge: true })`} to mark a field for deletion.
 */
/**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
class Rr extends Ar {
    Ca(t) {
        if (2 /* MergeSet */ !== t.Sa) throw 1 /* Update */ === t.Sa ? t.Da(this.ba + "() can only appear at the top level of your update data") : t.Da(this.ba + "() cannot be used with set() unless you pass {merge:true}");
        // No transform to add for a delete, but we need to add it to our
        // fieldMask so it gets deleted.
        return t.jn.push(t.path), null;
    }
    isEqual(t) {
        return t instanceof Rr;
    }
}

/**
 * Creates a child context for parsing SerializableFieldValues.
 *
 * This is different than calling `ParseContext.contextWith` because it keeps
 * the fieldTransforms and fieldMask separate.
 *
 * The created context has its `dataSource` set to `UserDataSource.Argument`.
 * Although these values are used with writes, any elements in these FieldValues
 * are not considered writes since they cannot contain any FieldValue sentinels,
 * etc.
 *
 * @param fieldValue The sentinel FieldValue for which to create a child
 *     context.
 * @param context The parent context.
 * @param arrayElement Whether or not the FieldValue has an array.
 */ function Pr(t, n, e) {
    return new zr({
        Sa: 3 /* Argument */ ,
        Na: n.settings.Na,
        methodName: t.ba,
        Fa: e
    }, n.t, n.serializer, n.ignoreUndefinedProperties);
}

class Vr extends Ar {
    Ca(t) {
        return new zn(t.path, new Mn);
    }
    isEqual(t) {
        return t instanceof Vr;
    }
}

class yr extends Ar {
    constructor(t, n) {
        super(t), this.xa = n;
    }
    Ca(t) {
        const n = Pr(this, t, 
        /*array=*/ !0), e = this.xa.map((t => to(t, n))), s = new Un(e);
        return new zn(t.path, s);
    }
    isEqual(t) {
        // TODO(mrschmidt): Implement isEquals
        return this === t;
    }
}

class gr extends Ar {
    constructor(t, n) {
        super(t), this.xa = n;
    }
    Ca(t) {
        const n = Pr(this, t, 
        /*array=*/ !0), e = this.xa.map((t => to(t, n))), s = new Bn(e);
        return new zn(t.path, s);
    }
    isEqual(t) {
        // TODO(mrschmidt): Implement isEquals
        return this === t;
    }
}

class pr extends Ar {
    constructor(t, n) {
        super(t), this.Oa = n;
    }
    Ca(t) {
        const n = new jn(t.serializer, en(t.serializer, this.Oa));
        return new zn(t.path, n);
    }
    isEqual(t) {
        // TODO(mrschmidt): Implement isEquals
        return this === t;
    }
}

/**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/**
 * An immutable object representing a geographic location in Firestore. The
 * location is represented as latitude/longitude pair.
 *
 * Latitude values are in the range of [-90, 90].
 * Longitude values are in the range of [-180, 180].
 */ class vr {
    /**
     * Creates a new immutable `GeoPoint` object with the provided latitude and
     * longitude values.
     * @param latitude The latitude as number between -90 and 90.
     * @param longitude The longitude as number between -180 and 180.
     */
    constructor(t, n) {
        if (!isFinite(t) || t < -90 || t > 90) throw new O(x.INVALID_ARGUMENT, "Latitude must be a number between -90 and 90, but was: " + t);
        if (!isFinite(n) || n < -180 || n > 180) throw new O(x.INVALID_ARGUMENT, "Longitude must be a number between -180 and 180, but was: " + n);
        this.ka = t, this.La = n;
    }
    /**
     * The latitude of this `GeoPoint` instance.
     */    get latitude() {
        return this.ka;
    }
    /**
     * The longitude of this `GeoPoint` instance.
     */    get longitude() {
        return this.La;
    }
    /**
     * Returns true if this `GeoPoint` is equal to the provided one.
     *
     * @param other The `GeoPoint` to compare against.
     * @return true if this `GeoPoint` is equal to the provided one.
     */    isEqual(t) {
        return this.ka === t.ka && this.La === t.La;
    }
    toJSON() {
        return {
            latitude: this.ka,
            longitude: this.La
        };
    }
    /**
     * Actually private to JS consumers of our API, so this function is prefixed
     * with an underscore.
     */    m(t) {
        return b(this.ka, t.ka) || b(this.La, t.La);
    }
}

/**
 * @license
 * Copyright 2020 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/**
 * An immutable object representing an array of bytes.
 */ class br {
    constructor(t) {
        this.$a = t;
    }
    /**
     * Creates a new `Bytes` object from the given Base64 string, converting it to
     * bytes.
     *
     * @param base64 The Base64 string used to create the `Bytes` object.
     */    static fromBase64String(t) {
        try {
            return new br(J.fromBase64String(t));
        } catch (t) {
            throw new O(x.INVALID_ARGUMENT, "Failed to construct Bytes from Base64 string: " + t);
        }
    }
    /**
     * Creates a new `Bytes` object from the given Uint8Array.
     *
     * @param array The Uint8Array used to create the `Bytes` object.
     */    static fromUint8Array(t) {
        return new br(J.fromUint8Array(t));
    }
    /**
     * Returns the underlying bytes as a Base64-encoded string.
     *
     * @return The Base64-encoded string created from the `Bytes` object.
     */    toBase64() {
        return this.$a.toBase64();
    }
    /**
     * Returns the underlying bytes in a new `Uint8Array`.
     *
     * @return The Uint8Array created from the `Bytes` object.
     */    toUint8Array() {
        return this.$a.toUint8Array();
    }
    /**
     * Returns a string representation of the `Bytes` object.
     *
     * @return A string representation of the `Bytes` object.
     */    toString() {
        return "Bytes(base64: " + this.toBase64() + ")";
    }
    /**
     * Returns true if this `Bytes` object is equal to the provided one.
     *
     * @param other The `Bytes` object to compare against.
     * @return true if this `Bytes` object is equal to the provided one.
     */    isEqual(t) {
        return this.$a.isEqual(t.$a);
    }
}

/**
 * @license
 * Copyright 2020 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */ const Cr = new Map;

// settings() defaults:
/**
 * A concrete type describing all the values that can be applied via a
 * user-supplied firestore.Settings object. This is a separate type so that
 * defaults can be supplied and the value can be checked for equality.
 */
class Sr {
    constructor(t) {
        var n;
        if (void 0 === t.host) {
            if (void 0 !== t.ssl) throw new O(x.INVALID_ARGUMENT, "Can't provide ssl option if host option is not set");
            this.host = "firestore.googleapis.com", this.ssl = true;
        } else this.host = t.host, this.ssl = null === (n = t.ssl) || void 0 === n || n;
        if (this.credentials = t.credentials, this.ignoreUndefinedProperties = !!t.ignoreUndefinedProperties, 
        void 0 === t.cacheSizeBytes) this.cacheSizeBytes = 41943040; else {
            if (-1 !== t.cacheSizeBytes && t.cacheSizeBytes < 1048576) throw new O(x.INVALID_ARGUMENT, "cacheSizeBytes must be at least 1048576");
            this.cacheSizeBytes = t.cacheSizeBytes;
        }
        this.experimentalForceLongPolling = !!t.experimentalForceLongPolling, this.experimentalAutoDetectLongPolling = !!t.experimentalAutoDetectLongPolling, 
        Er("experimentalForceLongPolling", t.experimentalForceLongPolling, "experimentalAutoDetectLongPolling", t.experimentalAutoDetectLongPolling);
    }
    isEqual(t) {
        return this.host === t.host && this.ssl === t.ssl && this.credentials === t.credentials && this.cacheSizeBytes === t.cacheSizeBytes && this.experimentalForceLongPolling === t.experimentalForceLongPolling && this.experimentalAutoDetectLongPolling === t.experimentalAutoDetectLongPolling && this.ignoreUndefinedProperties === t.ignoreUndefinedProperties;
    }
}

/**
 * The Cloud Firestore service interface.
 *
 * Do not call this constructor directly. Instead, use {@link getFirestore()}.
 */
/**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/** Helper function to assert Uint8Array is available at runtime. */
function Dr() {
    if ("undefined" == typeof Uint8Array) throw new O(x.UNIMPLEMENTED, "Uint8Arrays are not available in this environment.");
}

/** Helper function to assert Base64 functions are available at runtime. */ function Nr() {
    if ("undefined" == typeof atob) throw new O(x.UNIMPLEMENTED, "Blobs are unavailable in Firestore in this environment.");
}

/**
 * Immutable class holding a blob (binary data).
 *
 * This class is directly exposed in the public API. It extends the Bytes class
 * of the firestore-exp API to support `instanceof Bytes` checks during user
 * data conversion.
 *
 * Note that while you can't hide the constructor in JavaScript code, we are
 * using the hack above to make sure no-one outside this module can call it.
 */ class Fr extends br {
    static fromBase64String(t) {
        Nr();
        try {
            return new Fr(J.fromBase64String(t));
        } catch (t) {
            throw new O(x.INVALID_ARGUMENT, "Failed to construct Blob from Base64 string: " + t);
        }
    }
    static fromUint8Array(t) {
        return Dr(), new Fr(J.fromUint8Array(t));
    }
    toBase64() {
        return Nr(), super.toBase64();
    }
    toUint8Array() {
        return Dr(), super.toUint8Array();
    }
    toString() {
        return "Blob(base64: " + this.toBase64() + ")";
    }
}

/**
 * @license
 * Copyright 2020 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/**
 * Converts Firestore's internal types to the JavaScript types that we expose
 * to the user.
 */ class xr {
    Ma(t, n = "none") {
        switch (Ot(t)) {
          case 0 /* NullValue */ :
            return null;

          case 1 /* BooleanValue */ :
            return t.booleanValue;

          case 2 /* NumberValue */ :
            return Qt(t.integerValue || t.doubleValue);

          case 3 /* TimestampValue */ :
            return this.Ua(t.timestampValue);

          case 4 /* ServerTimestampValue */ :
            return this.qa(t, n);

          case 5 /* StringValue */ :
            return t.stringValue;

          case 6 /* BlobValue */ :
            return this.Ba(jt(t.bytesValue));

          case 7 /* RefValue */ :
            return this.Qa(t.referenceValue);

          case 8 /* GeoPointValue */ :
            return this.ja(t.geoPointValue);

          case 9 /* ArrayValue */ :
            return this.Wa(t.arrayValue, n);

          case 10 /* ObjectValue */ :
            return this.Ka(t.mapValue, n);

          default:
            throw V();
        }
    }
    Ka(t, n) {
        const e = {};
        return D(t.fields || {}, ((t, s) => {
            e[t] = this.Ma(s, n);
        })), e;
    }
    ja(t) {
        return new vr(Qt(t.latitude), Qt(t.longitude));
    }
    Wa(t, n) {
        return (t.values || []).map((t => this.Ma(t, n)));
    }
    qa(t, n) {
        switch (n) {
          case "previous":
            const e = Nt(t);
            return null == e ? null : this.Ma(e, n);

          case "estimate":
            return this.Ua(Ft(t));

          default:
            return null;
        }
    }
    Ua(t) {
        const n = Bt(t);
        return new k(n.seconds, n.nanos);
    }
    Ga(t, n) {
        const e = M.L(t);
        y(xn(e));
        const s = new E(e.get(1), e.get(3)), i = new B(e.v(5));
        return s.isEqual(n) || 
        // TODO(b/64130202): Somehow support foreign references.
        A(`Document ${i} contains a document reference within a different database (${s.projectId}/${s.database}) which is not supported. It will be treated as a reference in the current database (${n.projectId}/${n.database}) instead.`), 
        i;
    }
}

class Or extends xr {
    constructor(t) {
        super(), this.firestore = t;
    }
    Ba(t) {
        return new Fr(t);
    }
    Qa(t) {
        const n = this.Ga(t, this.firestore.za);
        return zo.Ha(n, this.firestore, /* converter= */ null);
    }
}

/**
 * @license
 * Copyright 2020 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/**
 * A `DocumentSnapshot` contains data read from a document in your Firestore
 * database. The data can be extracted with `.data()` or `.get(<field>)` to
 * get a specific field.
 *
 * For a `DocumentSnapshot` that points to a non-existing document, any data
 * access will return 'undefined'. You can use the `exists()` method to
 * explicitly verify a document's existence.
 */ class kr {
    // Note: This class is stripped down version of the DocumentSnapshot in
    // the legacy SDK. The changes are:
    // - No support for SnapshotMetadata.
    // - No support for SnapshotOptions.
    constructor(t, n, e, s, i) {
        this.Ya = t, this.Ja = n, this.Xa = e, this.Za = s, this.th = i;
    }
    /** Property of the `DocumentSnapshot` that provides the document's ID. */    get id() {
        return this.Xa.path.D();
    }
    /**
     * The `DocumentReference` for the document included in the `DocumentSnapshot`.
     */    get ref() {
        return new Ur(this.Ya, this.th, this.Xa);
    }
    /**
     * Signals whether or not the document at the snapshot's location exists.
     *
     * @return true if the document exists.
     */    exists() {
        return null !== this.Za;
    }
    /**
     * Retrieves all fields in the document as an `Object`. Returns `undefined` if
     * the document doesn't exist.
     *
     * @return An `Object` containing all fields in the document or `undefined`
     * if the document doesn't exist.
     */    data() {
        if (this.Za) {
            if (this.th) {
                // We only want to use the converter and create a new DocumentSnapshot
                // if a converter has been provided.
                const t = new Lr(this.Ya, this.Ja, this.Xa, this.Za, 
                /* converter= */ null);
                return this.th.fromFirestore(t);
            }
            return this.Ja.Ma(this.Za.ie());
        }
    }
    /**
     * Retrieves the field specified by `fieldPath`. Returns `undefined` if the
     * document or field doesn't exist.
     *
     * @param fieldPath The path (for example 'foo' or 'foo.bar') to a specific
     * field.
     * @return The data at the specified field location or undefined if no such
     * field exists in the document.
     */
    // We are using `any` here to avoid an explicit cast by our users.
    // eslint-disable-next-line @typescript-eslint/no-explicit-any
    get(t) {
        if (this.Za) {
            const n = this.Za.data().field($r("DocumentSnapshot.get", t));
            if (null !== n) return this.Ja.Ma(n);
        }
    }
}

/**
 * A `QueryDocumentSnapshot` contains data read from a document in your
 * Firestore database as part of a query. The document is guaranteed to exist
 * and its data can be extracted with `.data()` or `.get(<field>)` to get a
 * specific field.
 *
 * A `QueryDocumentSnapshot` offers the same API surface as a
 * `DocumentSnapshot`. Since query results contain only existing documents, the
 * `exists` property will always be true and `data()` will never return
 * 'undefined'.
 */ class Lr extends kr {
    /**
     * Retrieves all fields in the document as an `Object`.
     *
     * @override
     * @return An `Object` containing all fields in the document.
     */
    data() {
        return super.data();
    }
}

/**
 * Helper that calls fromDotSeparatedString() but wraps any error thrown.
 */ function $r(t, n) {
    return "string" == typeof n ? oo(t, n) : n instanceof fr ? n.va.nh : n.nh;
}

/**
 * @license
 * Copyright 2020 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/**
 * A `FieldPath` refers to a field in a document. The path may consist of a
 * single field name (referring to a top-level field in the document), or a
 * list of field names (referring to a nested field in the document).
 *
 * Create a `FieldPath` by providing field names. If more than one field
 * name is provided, the path will point to a nested field in a document.
 */ class Mr {
    /**
     * Creates a FieldPath from the provided field names. If more than one field
     * name is provided, the path will point to a nested field in a document.
     *
     * @param fieldNames A list of field names.
     */
    constructor(...t) {
        for (let n = 0; n < t.length; ++n) if (0 === t[n].length) throw new O(x.INVALID_ARGUMENT, "Invalid field name at argument $(i + 1). Field names must not be empty.");
        this.nh = new q(t);
    }
    /**
     * Returns true if this `FieldPath` is equal to the provided one.
     *
     * @param other The `FieldPath` to compare against.
     * @return true if this `FieldPath` is equal to the provided one.
     */    isEqual(t) {
        return this.nh.isEqual(t.nh);
    }
}

/**
 * @license
 * Copyright 2020 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/**
 * A `DocumentReference` refers to a document location in a Firestore database
 * and can be used to write, read, or listen to the location. The document at
 * the referenced location may or may not exist.
 */ class Ur {
    constructor(t, n, e) {
        this.th = n, this.Xa = e, 
        /** The type of this Firestore reference. */
        this.type = "document", this.firestore = t;
    }
    get eh() {
        return this.Xa.path;
    }
    /**
     * The document's identifier within its collection.
     */    get id() {
        return this.Xa.path.D();
    }
    /**
     * A string representing the path of the referenced document (relative
     * to the root of the database).
     */    get path() {
        return this.Xa.path.k();
    }
    /**
     * The collection this `DocumentReference` belongs to.
     */    get parent() {
        return new Br(this.firestore, this.th, this.Xa.path.C());
    }
    /**
     * Applies a custom data converter to this `DocumentReference`, allowing you
     * to use your own custom model objects with Firestore. When you call {@link
     * setDoc()}, {@link getDoc()}, etc. with the returned `DocumentReference`
     * instance, the provided converter will convert between Firestore data and
     * your custom type `U`.
     *
     * @param converter Converts objects to and from Firestore.
     * @return A `DocumentReference<U>` that uses the provided converter.
     */    withConverter(t) {
        return new Ur(this.firestore, t, this.Xa);
    }
}

/**
 * A `Query` refers to a Query which you can read or listen to. You can also
 * construct refined `Query` objects by adding filters and ordering.
 */ class qr {
    // This is the lite version of the Query class in the main SDK.
    constructor(t, n, e) {
        this.th = n, this.sh = e, 
        /** The type of this Firestore reference. */
        this.type = "query", this.firestore = t;
    }
    /**
     * Applies a custom data converter to this query, allowing you to use your own
     * custom model objects with Firestore. When you call {@link getDocs()} with
     * the returned query, the provided converter will convert between Firestore
     * data and your custom type `U`.
     *
     * @param converter Converts objects to and from Firestore.
     * @return A `Query<U>` that uses the provided converter.
     */    withConverter(t) {
        return new qr(this.firestore, t, this.sh);
    }
}

/**
 * A `CollectionReference` object can be used for adding documents, getting
 * document references, and querying for documents (using {@link query()}`).
 */ class Br extends qr {
    constructor(t, n, e) {
        super(t, n, Re(e)), this.firestore = t, this.eh = e, this.type = "collection";
    }
    /** The collection's identifier. */    get id() {
        return this.sh.path.D();
    }
    /**
     * A string representing the path of the referenced collection (relative
     * to the root of the database).
     */    get path() {
        return this.sh.path.k();
    }
    /**
     * A reference to the containing `DocumentReference` if this is a
     * subcollection. If this isn't a subcollection, the reference is null.
     */    get parent() {
        const t = this.eh.C();
        return t.T() ? null : new Ur(this.firestore, 
        /* converter= */ null, new B(t));
    }
    /**
     * Applies a custom data converter to this CollectionReference, allowing you
     * to use your own custom model objects with Firestore. When you call {@link
     * addDoc()} with the returned `CollectionReference` instance, the provided
     * converter will convert between Firestore data and your custom type `U`.
     *
     * @param converter Converts objects to and from Firestore.
     * @return A `CollectionReference<U>` that uses the provided converter.
     */    withConverter(t) {
        return new Br(this.firestore, t, this.eh);
    }
}

/**
 * Returns true if the provided references are equal.
 *
 * @param left A reference to compare.
 * @param right A reference to compare.
 * @return true if the references point to the same location in the same
 * Firestore database.
 */ function Qr(t) {
    const n = t.ih(), e = ys(t.za);
    return new Hr(t.za, !!n.ignoreUndefinedProperties, e);
}

/**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */ const jr = /^__.*__$/;

/** The result of parsing document data (e.g. for a setData call). */ class Wr {
    constructor(t, n, e) {
        this.data = t, this.jn = n, this.fieldTransforms = e;
    }
    rh(t, n) {
        const e = [];
        return null !== this.jn ? e.push(new oe(t, this.data, this.jn, n)) : e.push(new re(t, this.data, n)), 
        this.fieldTransforms.length > 0 && e.push(new ce(t, this.fieldTransforms)), e;
    }
}

/** The result of parsing "update" data (i.e. for an updateData call). */ class Kr {
    constructor(t, n, e) {
        this.data = t, this.jn = n, this.fieldTransforms = e;
    }
    rh(t, n) {
        const e = [ new oe(t, this.data, this.jn, n) ];
        return this.fieldTransforms.length > 0 && e.push(new ce(t, this.fieldTransforms)), 
        e;
    }
}

function Gr(t) {
    switch (t) {
      case 0 /* Set */ :
 // fall through
              case 2 /* MergeSet */ :
 // fall through
              case 1 /* Update */ :
        return !0;

      case 3 /* Argument */ :
      case 4 /* ArrayArgument */ :
        return !1;

      default:
        throw V();
    }
}

/** A "context" object passed around while parsing user data. */ class zr {
    /**
     * Initializes a ParseContext with the given source and path.
     *
     * @param settings The settings for the parser.
     * @param databaseId The database ID of the Firestore instance.
     * @param serializer The serializer to use to generate the Value proto.
     * @param ignoreUndefinedProperties Whether to ignore undefined properties
     * rather than throw.
     * @param fieldTransforms A mutable list of field transforms encountered while
     *     parsing the data.
     * @param fieldMask A mutable list of field paths encountered while parsing
     *     the data.
     *
     * TODO(b/34871131): We don't support array paths right now, so path can be
     * null to indicate the context represents any location within an array (in
     * which case certain features will not work and errors will be somewhat
     * compromised).
     */
    constructor(t, n, e, s, i, r) {
        this.settings = t, this.t = n, this.serializer = e, this.ignoreUndefinedProperties = s, 
        // Minor hack: If fieldTransforms is undefined, we assume this is an
        // external call and we need to validate the entire path.
        void 0 === i && this.oh(), this.fieldTransforms = i || [], this.jn = r || [];
    }
    get path() {
        return this.settings.path;
    }
    get Sa() {
        return this.settings.Sa;
    }
    /** Returns a new context with the specified settings overwritten. */    uh(t) {
        return new zr(Object.assign(Object.assign({}, this.settings), t), this.t, this.serializer, this.ignoreUndefinedProperties, this.fieldTransforms, this.jn);
    }
    ah(t) {
        var n;
        const e = null === (n = this.path) || void 0 === n ? void 0 : n.child(t), s = this.uh({
            path: e,
            Fa: !1
        });
        return s.hh(t), s;
    }
    lh(t) {
        var n;
        const e = null === (n = this.path) || void 0 === n ? void 0 : n.child(t), s = this.uh({
            path: e,
            Fa: !1
        });
        return s.oh(), s;
    }
    _h(t) {
        // TODO(b/34871131): We don't support array paths right now; so make path
        // undefined.
        return this.uh({
            path: void 0,
            Fa: !0
        });
    }
    Da(t) {
        return uo(t, this.settings.methodName, this.settings.fh || !1, this.path, this.settings.Na);
    }
    /** Returns 'true' if 'fieldPath' was traversed when creating this context. */    contains(t) {
        return void 0 !== this.jn.find((n => t.N(n))) || void 0 !== this.fieldTransforms.find((n => t.N(n.field)));
    }
    oh() {
        // TODO(b/34871131): Remove null check once we have proper paths for fields
        // within arrays.
        if (this.path) for (let t = 0; t < this.path.length; t++) this.hh(this.path.get(t));
    }
    hh(t) {
        if (0 === t.length) throw this.Da("Document fields must not be empty");
        if (Gr(this.Sa) && jr.test(t)) throw this.Da('Document fields cannot begin and end with "__"');
    }
}

/**
 * Helper for parsing raw user input (provided via the API) into internal model
 * classes.
 */ class Hr {
    constructor(t, n, e) {
        this.t = t, this.ignoreUndefinedProperties = n, this.serializer = e || ys(t);
    }
    /** Creates a new top-level parse context. */    dh(t, n, e, s = !1) {
        return new zr({
            Sa: t,
            methodName: n,
            Na: e,
            path: q.$(),
            Fa: !1,
            fh: s
        }, this.t, this.serializer, this.ignoreUndefinedProperties);
    }
}

/** Parse document data from a set() call. */ function Yr(t, n, e, s, i, r = {}) {
    const o = t.dh(r.merge || r.mergeFields ? 2 /* MergeSet */ : 0 /* Set */ , n, e, i);
    so("Data must be an object, but it was:", o, s);
    const u = no(s, o);
    let c, a;
    if (r.merge) c = new Gn(o.jn), a = o.fieldTransforms; else if (r.mergeFields) {
        const t = [];
        for (const s of r.mergeFields) {
            const i = io(n, s, e);
            if (!o.contains(i)) throw new O(x.INVALID_ARGUMENT, `Field '${i}' is specified in your field mask but missing from your input data.`);
            co(t, i) || t.push(i);
        }
        c = new Gn(t), a = o.fieldTransforms.filter((t => c.Hn(t.field)));
    } else c = null, a = o.fieldTransforms;
    return new Wr(new fe(u), c, a);
}

/** Parse update data from an update() call. */ function Jr(t, n, e, s) {
    const i = t.dh(1 /* Update */ , n, e);
    so("Data must be an object, but it was:", i, s);
    const r = [], o = new de;
    D(s, ((t, s) => {
        const u = oo(n, t, e);
        // For Compat types, we have to "extract" the underlying types before
        // performing validation.
                s instanceof fr && (s = s.va);
        const c = i.lh(u);
        if (s instanceof Rr) 
        // Add it to the field mask, but don't add anything to updateData.
        r.push(u); else {
            const t = to(s, c);
            null != t && (r.push(u), o.set(u, t));
        }
    }));
    const u = new Gn(r);
    return new Kr(o.Xn(), u, i.fieldTransforms);
}

/** Parse update data from a list of field/value arguments. */ function Xr(t, n, e, s, i, r) {
    const o = t.dh(1 /* Update */ , n, e), u = [ io(n, s, e) ], c = [ i ];
    if (r.length % 2 != 0) throw new O(x.INVALID_ARGUMENT, `Function ${n}() needs to be called with an even number of arguments that alternate between field names and values.`);
    for (let t = 0; t < r.length; t += 2) u.push(io(n, r[t])), c.push(r[t + 1]);
    const a = [], h = new de;
    // We iterate in reverse order to pick the last value for a field if the
    // user specified the field multiple times.
    for (let t = u.length - 1; t >= 0; --t) if (!co(a, u[t])) {
        const n = u[t];
        let e = c[t];
        // For Compat types, we have to "extract" the underlying types before
        // performing validation.
                e instanceof fr && (e = e.va);
        const s = o.lh(n);
        if (e instanceof Rr) 
        // Add it to the field mask, but don't add anything to updateData.
        a.push(n); else {
            const t = to(e, s);
            null != t && (a.push(n), h.set(n, t));
        }
    }
    const l = new Gn(a);
    return new Kr(h.Xn(), l, o.fieldTransforms);
}

/**
 * Parse a "query value" (e.g. value in a where filter or a value in a cursor
 * bound).
 *
 * @param allowArrays Whether the query value is an array that may directly
 * contain additional arrays (e.g. the operand of an `in` query).
 */ function Zr(t, n, e, s = !1) {
    return to(e, t.dh(s ? 4 /* ArrayArgument */ : 3 /* Argument */ , n));
}

/**
 * Parses user data to Protobuf Values.
 *
 * @param input Data to be parsed.
 * @param context A context object representing the current path being parsed,
 * the source of the data being parsed, etc.
 * @return The parsed value, or null if the value was a FieldValue sentinel
 * that should not be included in the resulting parsed data.
 */ function to(t, n) {
    if (
    // Unwrap the API type from the Compat SDK. This will return the API type
    // from firestore-exp.
    t instanceof fr && (t = t.va), eo(t)) return so("Unsupported field value:", n, t), 
    no(t, n);
    if (t instanceof Ar) 
    // FieldValues usually parse into transforms (except FieldValue.delete())
    // in which case we do not want to include this field in our parsed data
    // (as doing so will overwrite the field directly prior to the transform
    // trying to transform it). So we don't add this location to
    // context.fieldMask and we return null as our parsing result.
    /**
 * "Parses" the provided FieldValueImpl, adding any necessary transforms to
 * context.fieldTransforms.
 */
    return function(t, n) {
        // Sentinels are only supported with writes, and not within arrays.
        if (!Gr(n.Sa)) throw n.Da(t.ba + "() can only be used with update() and set()");
        if (!n.path) throw n.Da(t.ba + "() is not currently supported inside arrays");
        const e = t.Ca(n);
        e && n.fieldTransforms.push(e);
    }
    /**
 * Helper to parse a scalar value (i.e. not an Object, Array, or FieldValue)
 *
 * @return The parsed value
 */ (t, n), null;
    if (
    // If context.path is null we are inside an array and we don't support
    // field mask paths more granular than the top-level array.
    n.path && n.jn.push(n.path), t instanceof Array) {
        // TODO(b/34871131): Include the path containing the array in the error
        // message.
        // In the case of IN queries, the parsed data is an array (representing
        // the set of values to be included for the IN query) that may directly
        // contain additional arrays (each representing an individual field
        // value), so we disable this validation.
        if (n.settings.Fa && 4 /* ArrayArgument */ !== n.Sa) throw n.Da("Nested arrays are not supported");
        return function(t, n) {
            const e = [];
            let s = 0;
            for (const i of t) {
                let t = to(i, n._h(s));
                null == t && (
                // Just include nulls in the array for fields being replaced with a
                // sentinel.
                t = {
                    nullValue: "NULL_VALUE"
                }), e.push(t), s++;
            }
            return {
                arrayValue: {
                    values: e
                }
            };
        }(t, n);
    }
    return function(t, n) {
        t instanceof fr && (t = t.va);
        if (null === t) return {
            nullValue: "NULL_VALUE"
        };
        if ("number" == typeof t) return en(n.serializer, t);
        if ("boolean" == typeof t) return {
            booleanValue: t
        };
        if ("string" == typeof t) return {
            stringValue: t
        };
        if (t instanceof Date) {
            const e = k.fromDate(t);
            return {
                timestampValue: sn(n.serializer, e)
            };
        }
        if (t instanceof k) {
            // Firestore backend truncates precision down to microseconds. To ensure
            // offline mode works the same with regards to truncation, perform the
            // truncation immediately without waiting for the backend to do that.
            const e = new k(t.seconds, 1e3 * Math.floor(t.nanoseconds / 1e3));
            return {
                timestampValue: sn(n.serializer, e)
            };
        }
        if (t instanceof vr) return {
            geoPointValue: {
                latitude: t.latitude,
                longitude: t.longitude
            }
        };
        if (t instanceof br) return {
            bytesValue: rn(n.serializer, t.$a)
        };
        if (t instanceof Ur) {
            const e = n.t, s = t.firestore.za;
            if (!s.isEqual(e)) throw n.Da(`Document reference is for database ${s.projectId}/${s.database} but should be for database ${e.projectId}/${e.database}`);
            return {
                referenceValue: cn(t.firestore.za || n.t, t.Xa.path)
            };
        }
        if (void 0 === t && n.ignoreUndefinedProperties) return null;
        throw n.Da("Unsupported field value: " + Tr(t));
    }
    /**
 * Checks whether an object looks like a JSON object that should be converted
 * into a struct. Normal class/prototype instances are considered to look like
 * JSON objects since they should be converted to a struct value. Arrays, Dates,
 * GeoPoints, etc. are not considered to look like JSON objects since they map
 * to specific FieldValue types other than ObjectValue.
 */ (t, n);
}

function no(t, n) {
    const e = {};
    return N(t) ? 
    // If we encounter an empty object, we explicitly add it to the update
    // mask to ensure that the server creates a map entry.
    n.path && n.path.length > 0 && n.jn.push(n.path) : D(t, ((t, s) => {
        const i = to(s, n.ah(t));
        null != i && (e[t] = i);
    })), {
        mapValue: {
            fields: e
        }
    };
}

function eo(t) {
    return !("object" != typeof t || null === t || t instanceof Array || t instanceof Date || t instanceof k || t instanceof vr || t instanceof br || t instanceof Ur || t instanceof Ar);
}

function so(t, n, e) {
    if (!eo(e) || !function(t) {
        return "object" == typeof t && null !== t && (Object.getPrototypeOf(t) === Object.prototype || null === Object.getPrototypeOf(t));
    }(e)) {
        const s = Tr(e);
        throw "an object" === s ? n.Da(t + " a custom object") : n.Da(t + " " + s);
    }
}

/**
 * Helper that calls fromDotSeparatedString() but wraps any error thrown.
 */ function io(t, n, e) {
    if (
    // If required, replace the FieldPath Compat class with with the firestore-exp
    // FieldPath.
    n instanceof fr && (n = n.va), n instanceof Mr) return n.nh;
    if ("string" == typeof n) return oo(t, n);
    throw uo("Field path arguments must be of type string or FieldPath.", t, 
    /* hasConverter= */ !1, 
    /* path= */ void 0, e);
}

/**
 * Matches any characters in a field path string that are reserved.
 */ const ro = new RegExp("[~\\*/\\[\\]]");

/**
 * Wraps fromDotSeparatedString with an error message about the method that
 * was thrown.
 * @param methodName The publicly visible method name
 * @param path The dot-separated string form of a field path which will be split
 * on dots.
 * @param targetDoc The document against which the field path will be evaluated.
 */ function oo(t, n, e) {
    if (n.search(ro) >= 0) throw uo(`Invalid field path (${n}). Paths must not contain '~', '*', '/', '[', or ']'`, t, 
    /* hasConverter= */ !1, 
    /* path= */ void 0, e);
    try {
        return new Mr(...n.split(".")).nh;
    } catch (s) {
        throw uo(`Invalid field path (${n}). Paths must not be empty, begin with '.', end with '.', or contain '..'`, t, 
        /* hasConverter= */ !1, 
        /* path= */ void 0, e);
    }
}

function uo(t, n, e, s, i) {
    const r = s && !s.T(), o = void 0 !== i;
    let u = `Function ${n}() called with invalid data`;
    e && (u += " (via `toFirestore()`)"), u += ". ";
    let c = "";
    return (r || o) && (c += " (found", r && (c += " in field " + s), o && (c += " in document " + i), 
    c += ")"), new O(x.INVALID_ARGUMENT, u + t + c);
}

/** Checks `haystack` if FieldPath `needle` is present. Runs in O(n). */ function co(t, n) {
    return t.some((t => t.isEqual(n)));
}

/**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/**
 * Internal transaction object responsible for accumulating the mutations to
 * perform and the base versions for any documents read.
 */ class ao {
    constructor(t) {
        this.ro = t, 
        // The version of each document that was read during this transaction.
        this.wh = new Map, this.mutations = [], this.Eh = !1, 
        /**
         * A deferred usage error that occurred previously in this transaction that
         * will cause the transaction to fail once it actually commits.
         */
        this.Th = null, 
        /**
         * Set of documents that have been written in the transaction.
         *
         * When there's more than one write to the same key in a transaction, any
         * writes after the first are handled differently.
         */
        this.mh = new Set;
    }
    async Ih(t) {
        if (this.Ah(), this.mutations.length > 0) throw new O(x.INVALID_ARGUMENT, "Firestore transactions require all reads to be executed before all writes.");
        const n = await async function(t, n) {
            const e = g(t), s = fn(e.serializer) + "/documents", i = {
                documents: n.map((t => hn(e.serializer, t)))
            }, r = await e.Gr("BatchGetDocuments", s, i), o = new Map;
            r.forEach((t => {
                const n = En(e.serializer, t);
                o.set(n.key.toString(), n);
            }));
            const u = [];
            return n.forEach((t => {
                const n = o.get(t.toString());
                y(!!n), u.push(n);
            })), u;
        }(this.ro, t);
        return n.forEach((t => {
            t instanceof me || t instanceof Te ? this.Rh(t) : V();
        })), n;
    }
    set(t, n) {
        this.write(n.rh(t, this.Gn(t))), this.mh.add(t.toString());
    }
    update(t, n) {
        try {
            this.write(n.rh(t, this.Ph(t)));
        } catch (t) {
            this.Th = t;
        }
        this.mh.add(t.toString());
    }
    delete(t) {
        this.write([ new le(t, this.Gn(t)) ]), this.mh.add(t.toString());
    }
    async commit() {
        if (this.Ah(), this.Th) throw this.Th;
        const t = this.wh;
        // For each mutation, note that the doc was written.
                this.mutations.forEach((n => {
            t.delete(n.key.toString());
        })), 
        // For each document that was read but not written to, we want to perform
        // a `verify` operation.
        t.forEach(((t, n) => {
            const e = B.j(n);
            this.mutations.push(new _e(e, this.Gn(e)));
        })), await async function(t, n) {
            const e = g(t), s = fn(e.serializer) + "/documents", i = {
                writes: n.map((t => mn(e.serializer, t)))
            };
            await e.Kr("Commit", s, i);
        }(this.ro, this.mutations), this.Eh = !0;
    }
    Rh(t) {
        let n;
        if (t instanceof Te) n = t.version; else {
            if (!(t instanceof me)) throw V();
            // For deleted docs, we must use baseVersion 0 when we overwrite them.
            n = L.min();
        }
        const e = this.wh.get(t.key.toString());
        if (e) {
            if (!n.isEqual(e)) 
            // This transaction will fail no matter what.
            throw new O(x.ABORTED, "Document version changed between two reads.");
        } else this.wh.set(t.key.toString(), n);
    }
    /**
     * Returns the version of this document when it was read in this transaction,
     * as a precondition, or no precondition if it was not read.
     */    Gn(t) {
        const n = this.wh.get(t.toString());
        return !this.mh.has(t.toString()) && n ? Jn.updateTime(n) : Jn.Yn();
    }
    /**
     * Returns the precondition for a document if the operation is an update.
     */    Ph(t) {
        const n = this.wh.get(t.toString());
        // The first time a document is written, we want to take into account the
        // read time and existence
                if (!this.mh.has(t.toString()) && n) {
            if (n.isEqual(L.min())) 
            // The document doesn't exist, so fail the transaction.
            // This has to be validated locally because you can't send a
            // precondition that a document does not exist without changing the
            // semantics of the backend write to be an insert. This is the reverse
            // of what we want, since we want to assert that the document doesn't
            // exist but then send the update and have it fail. Since we can't
            // express that to the backend, we have to validate locally.
            // Note: this can change once we can send separate verify writes in the
            // transaction.
            throw new O(x.INVALID_ARGUMENT, "Can't update a document that doesn't exist.");
            // Document exists, base precondition on document update time.
                        return Jn.updateTime(n);
        }
        // Document was not read, so we just use the preconditions for a blind
        // update.
        return Jn.exists(!0);
    }
    write(t) {
        this.Ah(), this.mutations = this.mutations.concat(t);
    }
    Ah() {}
}

/**
 * @license
 * Copyright 2019 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/**
 * TransactionRunner encapsulates the logic needed to run and retry transactions
 * with backoff.
 */
class ho {
    constructor(t, n, e, s) {
        this.Oe = t, this.ro = n, this.updateFunction = e, this.$e = s, this.Vh = 5, this.Ye = new ns(this.Oe, "transaction_retry" /* TransactionRetry */);
    }
    /** Runs the transaction and sets the result on deferred. */    run() {
        this.yh();
    }
    yh() {
        this.Ye.Ae((async () => {
            const t = new ao(this.ro), n = this.gh(t);
            n && n.then((n => {
                this.Oe.Be((() => t.commit().then((() => {
                    this.$e.resolve(n);
                })).catch((t => {
                    this.ph(t);
                }))));
            })).catch((t => {
                this.ph(t);
            }));
        }));
    }
    gh(t) {
        try {
            const n = this.updateFunction(t);
            return !Q(n) && n.catch && n.then ? n : (this.$e.reject(Error("Transaction callback must return a Promise")), 
            null);
        } catch (t) {
            // Do not retry errors thrown by user provided updateFunction.
            return this.$e.reject(t), null;
        }
    }
    ph(t) {
        this.Vh > 0 && this.bh(t) ? (this.Vh -= 1, this.Oe.Be((() => (this.yh(), Promise.resolve())))) : this.$e.reject(t);
    }
    bh(t) {
        if ("FirebaseError" === t.name) {
            // In transactions, the backend will fail outdated reads with FAILED_PRECONDITION and
            // non-matching document versions with ABORTED. These errors should be retried.
            const n = t.code;
            return "aborted" === n || "failed-precondition" === n || !et(n);
        }
        return !1;
    }
}

/**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/**
 * FirestoreClient is a top-level class that constructs and owns all of the
 * pieces of the client SDK architecture. It is responsible for creating the
 * async queue that is shared by all of the other components in the system.
 */
class lo {
    constructor(t, 
    /**
     * Asynchronous queue responsible for all of our internal processing. When
     * we get incoming work from the user (via public API) or the network
     * (incoming GRPC messages), we should always schedule onto this queue.
     * This ensures all of our work is properly serialized (e.g. we don't
     * start processing a new operation while the previous one is waiting for
     * an async I/O to complete).
     */
    n, e) {
        this.credentials = t, this.Oe = n, this.ta = e, this.user = Es.UNAUTHENTICATED, 
        this.clientId = v.u(), this.Ch = () => {}, this.Ls = new ts, this.credentials.xs((t => {
            I("FirestoreClient", "Received user=", t.uid), this.user.isEqual(t) || (this.user = t, 
            this.Ch(t)), this.Ls.resolve();
        }));
    }
    async getConfiguration() {
        return await this.Ls.promise, {
            Oe: this.Oe,
            ta: this.ta,
            clientId: this.clientId,
            credentials: this.credentials,
            ma: this.user,
            Pu: 100
        };
    }
    Sh(t) {
        this.Ch = t, 
        // eslint-disable-next-line @typescript-eslint/no-floating-promises
        this.Ls.promise.then((() => this.Ch(this.user)));
    }
    /**
     * Checks that the client has not been terminated. Ensures that other methods on
     * this class cannot be called after the client is terminated.
     */    Dh() {
        if (this.Oe.Xe) throw new O(x.FAILED_PRECONDITION, "The client has already been terminated.");
    }
    terminate() {
        this.Oe.es();
        const t = new ts;
        return this.Oe.Ze((async () => {
            try {
                this.Nh && await this.Nh.terminate(), this.Fh && await this.Fh.terminate(), 
                // `removeChangeListener` must be called after shutting down the
                // RemoteStore as it will prevent the RemoteStore from retrieving
                // auth tokens.
                this.credentials.Os(), t.resolve();
            } catch (n) {
                const e = us(n, "Failed to shutdown persistence");
                t.reject(e);
            }
        })), t.promise;
    }
}

async function _o(t, n) {
    t.Oe.us();
    const e = await fo(t);
    I("FirestoreClient", "Initializing OnlineComponentProvider");
    const s = await t.getConfiguration();
    await n.initialize(e, s), 
    // The CredentialChangeListener of the online component provider takes
    // precedence over the offline component provider.
    t.Sh((e => t.Oe.ss((() => async function(t, n) {
        const e = g(t);
        e.Oe.us(), I("RemoteStore", "RemoteStore received new credentials");
        const s = si(e);
        // Tear down and re-create our network streams. This will ensure we get a
        // fresh auth token for the new user and re-fill the write pipeline with
        // new mutations from the LocalStore (since mutations are per-user).
                e.ao.add(3 /* CredentialChange */), await Ys(e), s && 
        // Don't set the network status to Unknown if we are offline.
        e.fo.set("Unknown" /* Unknown */), await e.oo.ya(n), e.ao.delete(3 /* CredentialChange */), 
        await Hs(e);
    }(n.Iu, e))))), t.Nh = n;
}

async function fo(t) {
    return t.Fh || (I("FirestoreClient", "Using default OfflineComponentProvider"), 
    await async function(t, n) {
        t.Oe.us(), I("FirestoreClient", "Initializing OfflineComponentProvider");
        const e = await t.getConfiguration();
        await n.initialize(e), t.Sh((e => t.Oe.ss((async () => {
            await Os(n.io, e);
        })))), 
        // When a user calls clearPersistence() in one client, all other clients
        // need to be terminated to allow the delete to succeed.
        n.persistence.Fc((() => t.terminate())), t.Fh = n;
    }(t, new hr)), t.Fh;
}

async function wo(t) {
    return t.Nh || (I("FirestoreClient", "Using default OnlineComponentProvider"), await _o(t, new lr)), 
    t.Nh;
}

function Eo(t) {
    return fo(t).then((t => t.persistence));
}

function To(t) {
    return fo(t).then((t => t.io));
}

function mo(t) {
    return wo(t).then((t => t.Iu));
}

function Io(t) {
    return wo(t).then((t => t.Pa));
}

async function Ao(t) {
    const n = await wo(t), e = n.Au;
    return e.ws = Di.bind(null, n.Pa), e.Is = Ni.bind(null, n.Pa), e;
}

/** Enables the network connection and re-enqueues all pending operations. */ function Ro(t) {
    return t.Oe.enqueue((async () => {
        const n = await Eo(t), e = await mo(t);
        return n.xc(!0), function(t) {
            const n = g(t);
            return n.ao.delete(0 /* UserDisabled */), Hs(n);
        }(e);
    }));
}

/** Disables the network connection. Pending operations will not complete. */ function Po(t) {
    return t.Oe.enqueue((async () => {
        const n = await Eo(t), e = await mo(t);
        return n.xc(!1), async function(t) {
            const n = g(t);
            n.ao.add(0 /* UserDisabled */), await Ys(n), 
            // Set the OnlineState to Offline so get()s return from cache, etc.
            n.fo.set("Offline" /* Offline */);
        }(e);
    }));
}

/**
 * Returns a Promise that resolves when all writes that were pending at the time
 * this method was called received server acknowledgement. An acknowledgement
 * can be either acceptance or rejection.
 */ function Vo(t, n) {
    const e = new ts;
    return t.Oe.Be((async () => async function(t, n, e) {
        try {
            const s = await qs(t, n, 
            /* usePreviousResults= */ !0), i = new vi(n, s.lr), r = i.uu(s.documents), o = i.lu(r, 
            /* updateLimboDocuments= */ !1);
            e.resolve(o.snapshot);
        } catch (t) {
            const s = us(t, `Failed to execute query '${n} against cache`);
            e.reject(s);
        }
    }
    /**
 * Retrieves a latency-compensated query snapshot from the backend via a
 * SnapshotListener.
 */ (await To(t), n, e))), e.promise;
}

function yo(t, n, e = {}) {
    const s = new ts;
    return t.Oe.Be((async () => function(t, n, e, s, i) {
        const r = new _r({
            next: e => {
                // Remove query first before passing event to user to avoid
                // user actions affecting the now stale query.
                n.Be((() => ls(t, o))), e.fromCache && "server" === s.source ? i.reject(new O(x.UNAVAILABLE, 'Failed to get documents from server. (However, these documents may exist in the local cache. Run again without setting source to "server" to retrieve the cached documents.)')) : i.resolve(e);
            },
            error: t => i.reject(t)
        }), o = new ws(e, r, {
            includeMetadataChanges: !0,
            ps: !0
        });
        return hs(t, o);
    }
    /**
 * @license
 * Copyright 2020 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
    /**
 * The Cloud Firestore service interface.
 *
 * Do not call this constructor directly. Instead, use {@link getFirestore()}.
 */ (await Ao(t), t.Oe, n, e, s))), s.promise;
}

function go(t, n) {
    const e = new ts;
    return t.Oe.Be((async () => Fi(await Io(t), n, e))), e.promise;
}

/**
 * Takes an updateFunction in which a set of reads and writes can be performed
 * atomically. In the updateFunction, the client can read and write values
 * using the supplied transaction object. After the updateFunction, all
 * changes will be committed. If a retryable error occurs (ex: some other
 * client has changed any of the data referenced), then the updateFunction
 * will be called again after a backoff. If the updateFunction still fails
 * after all retries, then the transaction will be rejected.
 *
 * The transaction object passed to the updateFunction contains methods for
 * accessing documents and collections. Unlike other datastore access, data
 * accessed with the transaction will not reflect local changes that have not
 * been committed. For this reason, it is required that all reads are
 * performed before any writes. Transactions must be performed while online.
 */ function po(t, n) {
    const e = new ts;
    return t.Oe.Be((async () => {
        const s = await function(t) {
            return wo(t).then((t => t.ro));
        }(t);
        new ho(t.Oe, s, n, e).run();
    })), e.promise;
}

async function vo(t, n, e) {
    try {
        const s = await function(t, n) {
            const e = g(t);
            return e.persistence.runTransaction("read document", "readonly", (t => e.ki.Xs(t, n)));
        }(t, n);
        s instanceof Te ? e.resolve(s) : s instanceof me ? e.resolve(null) : e.reject(new O(x.UNAVAILABLE, "Failed to get document from cache. (However, this document may exist on the server. Run again without setting 'source' in the GetOptions to attempt to retrieve the document from the server.)"));
    } catch (t) {
        const s = us(t, `Failed to get document '${n} from cache`);
        e.reject(s);
    }
}

/**
 * Retrieves a latency-compensated document from the backend via a
 * SnapshotListener.
 */ function bo(t, n, e, s, i) {
    const r = new _r({
        next: r => {
            // Remove query first before passing event to user to avoid
            // user actions affecting the now stale query.
            n.Be((() => ls(t, o)));
            const u = r.docs.has(e);
            !u && r.fromCache ? 
            // TODO(dimond): If we're online and the document doesn't
            // exist then we resolve with a doc.exists set to false. If
            // we're offline however, we reject the Promise in this
            // case. Two options: 1) Cache the negative response from
            // the server so we can deliver that even when you're
            // offline 2) Actually reject the Promise in the online case
            // if the document doesn't exist.
            i.reject(new O(x.UNAVAILABLE, "Failed to get document because the client is offline.")) : u && r.fromCache && s && "server" === s.source ? i.reject(new O(x.UNAVAILABLE, 'Failed to get document from server. (However, this document does exist in the local cache. Run again without setting source to "server" to retrieve the cached document.)')) : i.resolve(r);
        },
        error: t => i.reject(t)
    }), o = new ws(Re(e.path), r, {
        includeMetadataChanges: !0,
        ps: !0
    });
    return hs(t, o);
}

class Co extends class {
    constructor(t, n) {
        this.xh = "(lite)", this.Oh = new Sr({}), this.kh = !1, t instanceof E ? (this.za = t, 
        this.Lh = new ms) : (this.$h = t, this.za = function(t) {
            if (!Object.prototype.hasOwnProperty.apply(t.options, [ "projectId" ])) throw new O(x.INVALID_ARGUMENT, '"projectId" not provided in firebase.initializeApp.');
            return new E(t.options.projectId);
        }(t), this.Lh = new Is(n));
    }
    /**
     * The {@link FirebaseApp app} associated with this `Firestore` service
     * instance.
     */    get app() {
        if (!this.$h) throw new O(x.FAILED_PRECONDITION, "Firestore was not initialized using the Firebase SDK. 'app' is not available");
        return this.$h;
    }
    get Mh() {
        return this.kh;
    }
    get Uh() {
        return void 0 !== this.qh;
    }
    Bh(t) {
        if (this.kh) throw new O(x.FAILED_PRECONDITION, "Firestore has already been started and its settings can no longer be changed. You can only modify settings before calling any other methods on a Firestore object.");
        this.Oh = new Sr(t), void 0 !== t.credentials && (this.Lh = function(t) {
            if (!t) return new ms;
            switch (t.type) {
              case "gapi":
                const n = t.client;
                // Make sure this really is a Gapi client.
                                return y(!("object" != typeof n || null === n || !n.auth || !n.auth.getAuthHeaderValueForFirstParty)), 
                new Rs(n, t.sessionIndex || "0");

              case "provider":
                return t.client;

              default:
                throw new O(x.INVALID_ARGUMENT, "makeCredentialsProvider failed due to invalid credential type");
            }
        }
        /**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */ (t.credentials));
    }
    Qh() {
        return this.Oh;
    }
    ih() {
        return this.kh = !0, this.Oh;
    }
    _delete() {
        return this.qh || (this.qh = this.jh()), this.qh;
    }
    /**
     * Terminates all components used by this client. Subclasses can override
     * this method to clean up their own dependencies, but must also call this
     * method.
     *
     * Only ever called once.
     */    jh() {
        /**
 * Removes all components associated with the provided instance. Must be called
 * when the `Firestore` instance is terminated.
 */
        return function(t) {
            const n = Cr.get(t);
            n && (I("ComponentProvider", "Removing Datastore"), Cr.delete(t), n.terminate());
        }
        /**
 * @license
 * Copyright 2020 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
        // settings() defaults:
        (this), Promise.resolve();
    }
} {
    constructor(t, n) {
        super(t, n), this.Wh = new os, this.xh = "name" in t ? t.name : "[DEFAULT]";
    }
    jh() {
        return this.Kh || 
        // The client must be initialized to ensure that all subsequent API
        // usage throws an exception.
        Wo(this), this.Kh.terminate();
    }
}

/**
 * Waits until all currently pending writes for the active user have been
 * acknowledged by the backend.
 *
 * The returned Promise resolves immediately if there are no outstanding writes.
 * Otherwise, the Promise waits for all previously issued writes (including
 * those written in a previous app session), but it does not wait for writes
 * that were added after the function is called. If you want to wait for
 * additional writes, call `waitForPendingWrites()` again.
 *
 * Any outstanding `waitForPendingWrites()` Promises are rejected during user
 * changes.
 *
 * @return A Promise which resolves when all currently pending writes have been
 * acknowledged by the backend.
 */ function So(t) {
    return function(t) {
        const n = new ts;
        return t.Oe.Be((async () => Mi(await Io(t), n))), n.promise;
    }(jo(t));
}

/**
 * Re-enables use of the network for this Firestore instance after a prior
 * call to {@link disableNetwork()}.
 *
 * @return A promise that is resolved once the network has been enabled.
 */
/**
 * @license
 * Copyright 2020 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/**
 * A `DocumentSnapshot` contains data read from a document in your Firestore
 * database. The data can be extracted with `.data()` or `.get(<field>)` to
 * get a specific field.
 *
 * For a `DocumentSnapshot` that points to a non-existing document, any data
 * access will return 'undefined'. You can use the `exists()` method to
 * explicitly verify a document's existence.
 */
class Do extends kr {
    constructor(t, n, e, s, i, r) {
        super(t, n, e, s, r), this.Ya = t, this.Gh = t, this.metadata = i;
    }
    /**
     * Property of the `DocumentSnapshot` that signals whether or not the data
     * exists. True if the document exists.
     */    exists() {
        return super.exists();
    }
    /**
     * Retrieves all fields in the document as an `Object`. Returns `undefined` if
     * the document doesn't exist.
     *
     * By default, `FieldValue.serverTimestamp()` values that have not yet been
     * set to their final value will be returned as `null`. You can override
     * this by passing an options object.
     *
     * @param options An options object to configure how data is retrieved from
     * the snapshot (for example the desired behavior for server timestamps that
     * have not yet been set to their final value).
     * @return An `Object` containing all fields in the document or `undefined` if
     * the document doesn't exist.
     */    data(t = {}) {
        if (this.Za) {
            if (this.th) {
                // We only want to use the converter and create a new DocumentSnapshot
                // if a converter has been provided.
                const n = new No(this.Ya, this.Ja, this.Xa, this.Za, this.metadata, 
                /* converter= */ null);
                return this.th.fromFirestore(n, t);
            }
            return this.Ja.Ma(this.Za.ie(), t.serverTimestamps);
        }
    }
    /**
     * Retrieves the field specified by `fieldPath`. Returns `undefined` if the
     * document or field doesn't exist.
     *
     * By default, a `FieldValue.serverTimestamp()` that has not yet been set to
     * its final value will be returned as `null`. You can override this by
     * passing an options object.
     *
     * @param fieldPath The path (for example 'foo' or 'foo.bar') to a specific
     * field.
     * @param options An options object to configure how the field is retrieved
     * from the snapshot (for example the desired behavior for server timestamps
     * that have not yet been set to their final value).
     * @return The data at the specified field location or undefined if no such
     * field exists in the document.
     */
    // We are using `any` here to avoid an explicit cast by our users.
    // eslint-disable-next-line @typescript-eslint/no-explicit-any
    get(t, n = {}) {
        if (this.Za) {
            const e = this.Za.data().field($r("DocumentSnapshot.get", t));
            if (null !== e) return this.Ja.Ma(e, n.serverTimestamps);
        }
    }
}

/**
 * A `QueryDocumentSnapshot` contains data read from a document in your
 * Firestore database as part of a query. The document is guaranteed to exist
 * and its data can be extracted with `.data()` or `.get(<field>)` to get a
 * specific field.
 *
 * A `QueryDocumentSnapshot` offers the same API surface as a
 * `DocumentSnapshot`. Since query results contain only existing documents, the
 * `exists` property will always be true and `data()` will never return
 * 'undefined'.
 */ class No extends Do {
    /**
     * Retrieves all fields in the document as an `Object`.
     *
     * By default, `FieldValue.serverTimestamp()` values that have not yet been
     * set to their final value will be returned as `null`. You can override
     * this by passing an options object.
     *
     * @override
     * @param options An options object to configure how data is retrieved from
     * the snapshot (for example the desired behavior for server timestamps that
     * have not yet been set to their final value).
     * @return An `Object` containing all fields in the document.
     */
    data(t = {}) {
        return super.data(t);
    }
}

/**
 * A `QuerySnapshot` contains zero or more `DocumentSnapshot` objects
 * representing the results of a query. The documents can be accessed as an
 * array via the `docs` property or enumerated using the `forEach` method. The
 * number of documents can be determined via the `empty` and `size`
 * properties.
 */ class Fo {
    constructor(t, n, e, s) {
        this.Ya = t, this.Ja = n, this.zh = s, this.metadata = new Yo(s.hasPendingWrites, s.fromCache), 
        this.query = e;
    }
    /** An array of all the documents in the `QuerySnapshot`. */    get docs() {
        const t = [];
        return this.forEach((n => t.push(n))), t;
    }
    /** The number of documents in the `QuerySnapshot`. */    get size() {
        return this.zh.docs.size;
    }
    /** True if there are no documents in the `QuerySnapshot`. */    get empty() {
        return 0 === this.size;
    }
    /**
     * Enumerates all of the documents in the `QuerySnapshot`.
     *
     * @param callback A callback to be called with a `QueryDocumentSnapshot` for
     * each document in the snapshot.
     * @param thisArg The `this` binding for the callback.
     */    forEach(t, n) {
        this.zh.docs.forEach((e => {
            t.call(n, new No(this.Ya, this.Ja, e.key, e, new Yo(this.zh.Qt.has(e.key), this.zh.fromCache), this.query.th));
        }));
    }
    /**
     * Returns an array of the documents changes since the last snapshot. If this
     * is the first snapshot, all documents will be in the list as 'added'
     * changes.
     *
     * @param options `SnapshotListenOptions` that control whether metadata-only
     * changes (i.e. only `DocumentSnapshot.metadata` changed) should trigger
     * snapshot events.
     */    docChanges(t = {}) {
        const n = !!t.includeMetadataChanges;
        if (n && this.zh.Wt) throw new O(x.INVALID_ARGUMENT, "To include metadata changes with your document changes, you must also pass { includeMetadataChanges:true } to onSnapshot().");
        return this.Hh && this.Yh === n || (this.Hh = 
        /** Calculates the array of DocumentChanges for a given ViewSnapshot. */
        function(t, n) {
            if (t.zh.Bt.T()) {
                // Special case the first snapshot because index calculation is easy and
                // fast
                let n, e = 0;
                return t.zh.docChanges.map((s => {
                    const i = new No(t.Ya, t.Ja, s.doc.key, s.doc, new Yo(t.zh.Qt.has(s.doc.key), t.zh.fromCache), t.query.th);
                    return n = s.doc, {
                        type: "added",
                        doc: i,
                        oldIndex: -1,
                        newIndex: e++
                    };
                }));
            }
            {
                // A DocumentSet that is updated incrementally as changes are applied to use
                // to lookup the index of a document.
                let e = t.zh.Bt;
                return t.zh.docChanges.filter((t => n || 3 /* Metadata */ !== t.type)).map((n => {
                    const s = new No(t.Ya, t.Ja, n.doc.key, n.doc, new Yo(t.zh.Qt.has(n.doc.key), t.zh.fromCache), t.query.th);
                    let i = -1, r = -1;
                    return 0 /* Added */ !== n.type && (i = e.indexOf(n.doc.key), e = e.delete(n.doc.key)), 
                    1 /* Removed */ !== n.type && (e = e.add(n.doc), r = e.indexOf(n.doc.key)), {
                        type: xo(n.type),
                        doc: s,
                        oldIndex: i,
                        newIndex: r
                    };
                }));
            }
        }(this, n), this.Yh = n), this.Hh;
    }
}

function xo(t) {
    switch (t) {
      case 0 /* Added */ :
        return "added";

      case 2 /* Modified */ :
      case 3 /* Metadata */ :
        return "modified";

      case 1 /* Removed */ :
        return "removed";

      default:
        return V();
    }
}

// TODO(firestoreexp): Add tests for snapshotEqual with different snapshot
// metadata
/**
 * Returns true if the provided snapshots are equal.
 *
 * @param left A snapshot to compare.
 * @param right A snapshot to compare.
 * @return true if the snapshots are equal.
 */ function Oo(t, n) {
    return t instanceof Do && n instanceof Do ? t.Ya === n.Ya && t.Xa.isEqual(n.Xa) && (null === t.Za ? null === n.Za : t.Za.isEqual(n.Za)) && t.th === n.th : t instanceof Fo && n instanceof Fo && (t.Ya === n.Ya && 
    /**
 * Returns true if the provided queries point to the same collection and apply
 * the same constraints.
 *
 * @param left A `Query` to compare.
 * @param right A Query` to compare.
 * @return true if the references point to the same location in the same
 * Firestore database.
 */
    function(t, n) {
        return t instanceof qr && n instanceof qr && t.firestore === n.firestore && Ne(t.sh, n.sh) && t.th === n.th;
    }(t.query, n.query) && t.metadata.isEqual(n.metadata) && t.zh.isEqual(n.zh));
}

/**
 * @license
 * Copyright 2020 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/**
 * Reads the document referred to by this `DocumentReference`.
 *
 * Note: `getDoc()` attempts to provide up-to-date data when possible by waiting
 * for data from the server, but it may return cached data or fail if you are
 * offline and the server cannot be reached. To specify this behavior, invoke
 * {@link getDocFromCache()} or {@link getDocFromServer()}.
 *
 * @param reference The reference of the document to fetch.
 * @return A Promise resolved with a `DocumentSnapshot` containing the
 * current document contents.
 */ class ko extends xr {
    constructor(t) {
        super(), this.firestore = t;
    }
    Ba(t) {
        return new br(t);
    }
    Qa(t) {
        const n = this.Ga(t, this.firestore.za);
        return new Ur(this.firestore, /* converter= */ null, n);
    }
}

/**
 * Reads the document referred to by this `DocumentReference` from cache.
 * Returns an error if the document is not currently cached.
 *
 * @return A Promise resolved with a `DocumentSnapshot` containing the
 * current document contents.
 */ function Lo(t, n, e, ...s) {
    const i = mr(t.firestore, Co), r = Qr(i);
    let o;
    // For Compat types, we have to "extract" the underlying types before
    // performing validation.
    n instanceof fr && (n = n.va), o = "string" == typeof n || n instanceof Mr ? Xr(r, "updateDoc", t.Xa, n, e, s) : Jr(r, "updateDoc", t.Xa, n);
    return Mo(i, o.rh(t.Xa, Jn.exists(!0)));
}

/**
 * Deletes the document referred to by the specified `DocumentReference`.
 *
 * @param reference A reference to the document to delete.
 * @return A Promise resolved once the document has been successfully
 * deleted from the backend (note that it won't resolve while you're offline).
 */ function $o(t, n) {
    const e = jo(t), s = gs(n) ? n : {
        next: n
    }, i = new _r(s);
    return t.Wh.Be((async () => {
        !function(t, n) {
            g(t).ds.add(n), 
            // Immediately fire an initial event, indicating all existing listeners
            // are in-sync.
            n.next();
        }(await Ao(e), i);
    })), () => {
        i.pa(), t.Wh.Be((async () => {
            !function(t, n) {
                g(t).ds.delete(n);
            }(await Ao(e), i);
        }));
    };
}

/** Locally writes `mutations` on the async queue. */ function Mo(t, n) {
    return go(jo(t), n);
}

/**
 * Converts a ViewSnapshot that contains the single document specified by `ref`
 * to a DocumentSnapshot.
 */ function Uo(t, n, e) {
    const s = e.docs.get(n.Xa), i = new ko(t);
    return new Do(t, i, n.Xa, s, new Yo(e.hasPendingWrites, e.fromCache), n.th);
}

/**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/**
 * Constant used to indicate the LRU garbage collection should be disabled.
 * Set this value as the `cacheSizeBytes` on the settings passed to the
 * `Firestore` instance.
 */ const qo = "You are using the memory-only build of Firestore. Persistence support is only available via the @firebase/firestore bundle or the firebase-firestore.js build.";

/**
 * The persistence provider included with the memory-only SDK. This provider
 * errors for all attempts to access persistence.
 */
class Bo {
    enableIndexedDbPersistence(t, n) {
        throw new O(x.FAILED_PRECONDITION, qo);
    }
    enableMultiTabIndexedDbPersistence(t) {
        throw new O(x.FAILED_PRECONDITION, qo);
    }
    clearIndexedDbPersistence(t) {
        throw new O(x.FAILED_PRECONDITION, qo);
    }
}

/**
 * Compat class for Firestore. Exposes Firestore Legacy API, but delegates
 * to the functional API of firestore-exp.
 */ class Qo extends fr {
    constructor(t, n, e) {
        super(n), this.Jh = e, this.INTERNAL = {
            delete: () => this.terminate()
        }, t instanceof E || (this.Xh = t);
    }
    get za() {
        return this.va.za;
    }
    settings(t) {
        t.merge && 
        // Remove the property from the settings once the merge is completed
        delete (t = Object.assign(Object.assign({}, this.va.Qh()), t)).merge, this.va.Bh(t);
    }
    useEmulator(t, n) {
        "firestore.googleapis.com" !== this.va.Qh().host && R("Host has been set in both settings() and useEmulator(), emulator host will be used"), 
        this.settings({
            host: `${t}:${n}`,
            ssl: !1,
            merge: !0
        });
    }
    enableNetwork() {
        return Ro(jo(this.va));
    }
    disableNetwork() {
        return Po(jo(this.va));
    }
    enablePersistence(t) {
        let n = !1, e = !1;
        return t && (n = !!t.synchronizeTabs, e = !!t.experimentalForceOwningTab, Er("synchronizeTabs", n, "experimentalForceOwningTab", e)), 
        n ? this.Jh.enableMultiTabIndexedDbPersistence(this) : this.Jh.enableIndexedDbPersistence(this, e);
    }
    clearPersistence() {
        return this.Jh.clearIndexedDbPersistence(this);
    }
    terminate() {
        return this.app._removeServiceInstance("firestore"), this.app._removeServiceInstance("firestore-exp"), 
        this.va._delete();
    }
    waitForPendingWrites() {
        return So(this.va);
    }
    onSnapshotsInSync(t) {
        return $o(this.va, t);
    }
    get app() {
        if (!this.Xh) throw new O(x.FAILED_PRECONDITION, "Firestore was not initialized using the Firebase SDK. 'app' is not available");
        return this.Xh;
    }
    collection(t) {
        return dr("Firestore.collection", "path", t), jo(this.va), new cu(M.L(t), this, 
        /* converter= */ null);
    }
    doc(t) {
        return dr("Firestore.doc", "path", t), jo(this.va), zo.Zh(M.L(t), this, 
        /* converter= */ null);
    }
    collectionGroup(t) {
        if (dr("Firestore.collectionGroup", "collectionId", t), t.indexOf("/") >= 0) throw new O(x.INVALID_ARGUMENT, `Invalid collection ID '${t}' passed to function Firestore.collectionGroup(). Collection IDs must not contain '/'.`);
        return jo(this.va), new ru(
        /**
 * Creates a new Query for a collection group query that matches all documents
 * within the provided collection group.
 */
        function(t) {
            return new Ae(M.$(), t);
        }(t), this, 
        /* converter= */ null);
    }
    runTransaction(t) {
        return po(jo(this.va), (n => t(new Ko(this, n))));
    }
    batch() {
        return jo(this.va), new Go(this);
    }
}

function jo(t) {
    return t.Kh || Wo(t), t.Kh.Dh(), t.Kh;
}

function Wo(t) {
    const n = t.ih(), e = function(t, n, e) {
        return new w(t, n, e.host, e.ssl, e.experimentalForceLongPolling, e.experimentalAutoDetectLongPolling);
    }(t.za, t.xh, n);
    t.Kh = new lo(t.Lh, t.Wh, e);
}

/**
 * A reference to a transaction.
 */
class Ko {
    constructor(t, n) {
        this.Ya = t, this.tl = n, this.nl = Qr(this.Ya.va);
    }
    get(t) {
        const n = au("Transaction.get", t, this.Ya), e = new Or(this.Ya);
        return this.tl.Ih([ n.Xa ]).then((t => {
            if (!t || 1 !== t.length) return V();
            const s = t[0];
            if (s instanceof me) return new Jo(this.Ya, new Do(this.Ya.va, e, n.Xa, null, new Yo(
            /*hasPendingWrites= */ !1, 
            /* fromCache= */ !1), n.th));
            if (s instanceof Te) return new Jo(this.Ya, new Do(this.Ya.va, e, n.Xa, s, new Yo(
            /*hasPendingWrites= */ !1, 
            /* fromCache= */ !1), n.th));
            throw V();
        }));
    }
    set(t, n, e) {
        const s = au("Transaction.set", t, this.Ya);
        e = wr("Transaction.set", e);
        const i = hu(s.th, n, e), r = Yr(this.nl, "Transaction.set", s.Xa, i, null !== s.th, e);
        return this.tl.set(s.Xa, r), this;
    }
    update(t, n, e, ...s) {
        const i = au("Transaction.update", t, this.Ya);
        // For Compat types, we have to "extract" the underlying types before
        // performing validation.
                let r;
        return n instanceof fr && (n = n.va), r = "string" == typeof n || n instanceof Mr ? Xr(this.nl, "Transaction.update", i.Xa, n, e, s) : Jr(this.nl, "Transaction.update", i.Xa, n), 
        this.tl.update(i.Xa, r), this;
    }
    delete(t) {
        const n = au("Transaction.delete", t, this.Ya);
        return this.tl.delete(n.Xa), this;
    }
}

class Go {
    constructor(t) {
        this.Ya = t, this.el = [], this.sl = !1, this.nl = Qr(this.Ya.va);
    }
    set(t, n, e) {
        this.il();
        const s = au("WriteBatch.set", t, this.Ya);
        e = wr("WriteBatch.set", e);
        const i = hu(s.th, n, e), r = Yr(this.nl, "WriteBatch.set", s.Xa, i, null !== s.th, e);
        return this.el = this.el.concat(r.rh(s.Xa, Jn.Yn())), this;
    }
    update(t, n, e, ...s) {
        this.il();
        const i = au("WriteBatch.update", t, this.Ya);
        // For Compat types, we have to "extract" the underlying types before
        // performing validation.
                let r;
        return n instanceof fr && (n = n.va), r = "string" == typeof n || n instanceof Mr ? Xr(this.nl, "WriteBatch.update", i.Xa, n, e, s) : Jr(this.nl, "WriteBatch.update", i.Xa, n), 
        this.el = this.el.concat(r.rh(i.Xa, Jn.exists(!0))), this;
    }
    delete(t) {
        this.il();
        const n = au("WriteBatch.delete", t, this.Ya);
        return this.el = this.el.concat(new le(n.Xa, Jn.Yn())), this;
    }
    commit() {
        if (this.il(), this.sl = !0, this.el.length > 0) {
            return go(jo(this.Ya.va), this.el);
        }
        return Promise.resolve();
    }
    il() {
        if (this.sl) throw new O(x.FAILED_PRECONDITION, "A write batch can no longer be used after commit() has been called.");
    }
}

/**
 * A reference to a particular document in a collection in the database.
 */ class zo extends fr {
    constructor(t, n) {
        super(n), this.firestore = t, this.Ja = new Or(t);
    }
    static Zh(t, n, e) {
        if (t.length % 2 != 0) throw new O(x.INVALID_ARGUMENT, `Invalid document reference. Document references must have an even number of segments, but ${t.k()} has ${t.length}`);
        return new zo(n, new Ur(n.va, e, new B(t)));
    }
    static Ha(t, n, e) {
        return new zo(n, new Ur(n.va, e, t));
    }
    get id() {
        return this.va.id;
    }
    get parent() {
        return new cu(this.va.eh.C(), this.firestore, this.va.th);
    }
    get path() {
        return this.va.path;
    }
    collection(t) {
        if (dr("DocumentReference.collection", "path", t), !t) throw new O(x.INVALID_ARGUMENT, "Must provide a non-empty collection name to collection()");
        const n = M.L(t);
        return new cu(this.va.eh.child(n), this.firestore, 
        /* converter= */ null);
    }
    isEqual(t) {
        return t instanceof fr && (t = t.va), t instanceof Ur && (n = this.va, e = t, (n instanceof Ur || n instanceof Br) && (e instanceof Ur || e instanceof Br) && n.firestore === e.firestore && n.path === e.path && n.th === e.th);
        var n, e;
    }
    set(t, n) {
        n = wr("DocumentReference.set", n);
        try {
            return function(t, n, e) {
                const s = mr(t.firestore, Co), i = hu(t.th, n, e);
                return Mo(s, Yr(Qr(s), "setDoc", t.Xa, i, null !== t.th, e).rh(t.Xa, Jn.Yn()));
            }(this.va, t, n);
        } catch (t) {
            throw Ho(t, "setDoc", "DocumentReference.set");
        }
    }
    update(t, n, ...e) {
        try {
            return 1 === arguments.length ? Lo(this.va, t) : Lo(this.va, t, n, ...e);
        } catch (t) {
            throw Ho(t, "updateDoc", "DocumentReference.update");
        }
    }
    delete() {
        return Mo(mr((t = this.va).firestore, Co), [ new le(t.Xa, Jn.Yn()) ]);
        var t;
    }
    onSnapshot(...t) {
        const n = 
        /**
 * Iterates the list of arguments from an `onSnapshot` call and returns the
 * first argument that may be an `SnapshotListenOptions` object. Returns an
 * empty object if none is found.
 */
        function(t) {
            for (const n of t) if ("object" == typeof n && !gs(n)) return n;
            return {};
        }
        /**
 * Creates an observer that can be passed to the firestore-exp SDK. The
 * observer converts all observed values into the format expected by the classic
 * SDK.
 *
 * @param args The list of arguments from an `onSnapshot` call.
 * @param wrapper The function that converts the firestore-exp type into the
 * type used by this shim.
 */ (t), e = function(t, n) {
            var e, s;
            let i;
            i = gs(t[0]) ? t[0] : gs(t[1]) ? t[1] : "function" == typeof t[0] ? {
                next: t[0],
                error: t[1],
                complete: t[2]
            } : {
                next: t[1],
                error: t[2],
                complete: t[3]
            };
            return {
                next: t => {
                    i.next && i.next(n(t));
                },
                error: null === (e = i.error) || void 0 === e ? void 0 : e.bind(i),
                complete: null === (s = i.complete) || void 0 === s ? void 0 : s.bind(i)
            };
        }
        /**
 * Metadata about a snapshot, describing the state of the snapshot.
 */ (t, (t => new Jo(this.firestore, new Do(this.firestore.va, this.Ja, t.Xa, t.Za, t.metadata, this.va.th))));
        return function(t, ...n) {
            var e, s, i;
            let r = {
                includeMetadataChanges: !1
            }, o = 0;
            "object" != typeof n[o] || gs(n[o]) || (r = n[o], o++);
            const u = {
                includeMetadataChanges: r.includeMetadataChanges
            };
            if (gs(n[o])) {
                const t = n[o];
                n[o] = null === (e = t.next) || void 0 === e ? void 0 : e.bind(t), n[o + 1] = null === (s = t.error) || void 0 === s ? void 0 : s.bind(t), 
                n[o + 2] = null === (i = t.complete) || void 0 === i ? void 0 : i.bind(t);
            }
            let c, a, h;
            if (t instanceof Ur) a = mr(t.firestore, Co), h = Re(t.Xa.path), c = {
                next: e => {
                    n[o] && n[o](Uo(a, t, e));
                },
                error: n[o + 1],
                complete: n[o + 2]
            }; else {
                a = mr(t.firestore, Co), h = t.sh;
                const e = new ko(a);
                c = {
                    next: s => {
                        n[o] && n[o](new Fo(a, e, t, s));
                    },
                    error: n[o + 1],
                    complete: n[o + 2]
                }, iu(t.sh);
            }
            const l = jo(a), _ = new _r(c), f = new ws(h, _, u);
            return a.Wh.Be((async () => hs(await Ao(l), f))), () => {
                _.pa(), a.Wh.Be((async () => ls(await Ao(l), f)));
            };
        }(this.va, n, e);
    }
    get(t) {
        let n;
        return n = "cache" === (null == t ? void 0 : t.source) ? function(t) {
            const n = mr(t.firestore, Co), e = jo(n), s = new ko(n), i = new ts;
            return n.Wh.Be((async () => {
                const n = await To(e);
                await vo(n, t.Xa, i);
            })), i.promise.then((e => new Do(n, s, t.Xa, e, new Yo(e instanceof Te && e.Jn, 
            /* fromCache= */ !0), t.th)));
        }
        /**
 * Reads the document referred to by this `DocumentReference` from the server.
 * Returns an error if the network is not available.
 *
 * @return A Promise resolved with a `DocumentSnapshot` containing the
 * current document contents.
 */ (this.va) : "server" === (null == t ? void 0 : t.source) ? function(t) {
            const n = mr(t.firestore, Co), e = jo(n), s = new ts;
            return n.Wh.Be((async () => {
                const i = await Ao(e);
                await bo(i, n.Wh, t.Xa, {
                    source: "server"
                }, s);
            })), s.promise.then((e => Uo(n, t, e)));
        }(this.va) : function(t) {
            const n = mr(t.firestore, Co), e = jo(n), s = new ts;
            return n.Wh.Be((async () => {
                const i = await Ao(e);
                await bo(i, n.Wh, t.Xa, {
                    source: "default"
                }, s);
            })), s.promise.then((e => Uo(n, t, e)));
        }(this.va), n.then((t => new Jo(this.firestore, new Do(this.firestore.va, this.Ja, t.Xa, t.Za, t.metadata, this.va.th))));
    }
    withConverter(t) {
        return new zo(this.firestore, this.va.withConverter(t));
    }
}

/**
 * Replaces the function name in an error thrown by the firestore-exp API
 * with the function names used in the classic API.
 */ function Ho(t, n, e) {
    return t.message = t.message.replace(n + "()", e + "()"), t;
}

class Yo {
    constructor(t, n) {
        this.hasPendingWrites = t, this.fromCache = n;
    }
    /**
     * Returns true if this `SnapshotMetadata` is equal to the provided one.
     *
     * @param other The `SnapshotMetadata` to compare against.
     * @return true if this `SnapshotMetadata` is equal to the provided one.
     */    isEqual(t) {
        return this.hasPendingWrites === t.hasPendingWrites && this.fromCache === t.fromCache;
    }
}

class Jo extends fr {
    constructor(t, n) {
        super(n), this.Ya = t;
    }
    get ref() {
        return new zo(this.Ya, this.va.ref);
    }
    get id() {
        return this.va.id;
    }
    get metadata() {
        return this.va.metadata;
    }
    get exists() {
        return this.va.exists();
    }
    data(t) {
        return this.va.data(t);
    }
    get(t, n) {
        return this.va.get(t, n);
    }
    isEqual(t) {
        return Oo(this.va, t.va);
    }
}

class Xo extends Jo {
    data(t) {
        return this.va.data(t);
    }
}

function Zo(t, n, e, s, i, r, o) {
    let u;
    if (i.U()) {
        if ("array-contains" /* ARRAY_CONTAINS */ === r || "array-contains-any" /* ARRAY_CONTAINS_ANY */ === r) throw new O(x.INVALID_ARGUMENT, `Invalid Query. You can't perform '${r}' queries on FieldPath.documentId().`);
        if ("in" /* IN */ === r || "not-in" /* NOT_IN */ === r) {
            eu(o, r);
            const n = [];
            for (const e of o) n.push(nu(s, t, e));
            u = {
                arrayValue: {
                    values: n
                }
            };
        } else u = nu(s, t, o);
    } else "in" /* IN */ !== r && "not-in" /* NOT_IN */ !== r && "array-contains-any" /* ARRAY_CONTAINS_ANY */ !== r || eu(o, r), 
    u = Zr(e, n, o, 
    /* allowArrays= */ "in" /* IN */ === r || "not-in" /* NOT_IN */ === r);
    const c = Le.create(i, r, u);
    return function(t, n) {
        if (n.ce()) {
            const e = ge(t);
            if (null !== e && !e.isEqual(n.field)) throw new O(x.INVALID_ARGUMENT, `Invalid query. All where filters with an inequality (<, <=, >, or >=) must be on the same field. But you have inequality filters on '${e.toString()}' and '${n.field.toString()}'`);
            const s = ye(t);
            null !== s && su(t, n.field, s);
        }
        const e = function(t, n) {
            for (const e of t.filters) if (n.indexOf(e.op) >= 0) return e.op;
            return null;
        }(t, 
        /**
 * Given an operator, returns the set of operators that cannot be used with it.
 *
 * Operators in a query must adhere to the following set of rules:
 * 1. Only one array operator is allowed.
 * 2. Only one disjunctive operator is allowed.
 * 3. NOT_EQUAL cannot be used with another NOT_EQUAL operator.
 * 4. NOT_IN cannot be used with array, disjunctive, or NOT_EQUAL operators.
 *
 * Array operators: ARRAY_CONTAINS, ARRAY_CONTAINS_ANY
 * Disjunctive operators: IN, ARRAY_CONTAINS_ANY, NOT_IN
 */
        function(t) {
            switch (t) {
              case "!=" /* NOT_EQUAL */ :
                return [ "!=" /* NOT_EQUAL */ , "not-in" /* NOT_IN */ ];

              case "array-contains" /* ARRAY_CONTAINS */ :
                return [ "array-contains" /* ARRAY_CONTAINS */ , "array-contains-any" /* ARRAY_CONTAINS_ANY */ , "not-in" /* NOT_IN */ ];

              case "in" /* IN */ :
                return [ "array-contains-any" /* ARRAY_CONTAINS_ANY */ , "in" /* IN */ , "not-in" /* NOT_IN */ ];

              case "array-contains-any" /* ARRAY_CONTAINS_ANY */ :
                return [ "array-contains" /* ARRAY_CONTAINS */ , "array-contains-any" /* ARRAY_CONTAINS_ANY */ , "in" /* IN */ , "not-in" /* NOT_IN */ ];

              case "not-in" /* NOT_IN */ :
                return [ "array-contains" /* ARRAY_CONTAINS */ , "array-contains-any" /* ARRAY_CONTAINS_ANY */ , "in" /* IN */ , "not-in" /* NOT_IN */ , "!=" /* NOT_EQUAL */ ];

              default:
                return [];
            }
        }(n.op));
        if (null !== e) 
        // Special case when it's a duplicate op to give a slightly clearer error message.
        throw e === n.op ? new O(x.INVALID_ARGUMENT, `Invalid query. You cannot use more than one '${n.op.toString()}' filter.`) : new O(x.INVALID_ARGUMENT, `Invalid query. You cannot use '${n.op.toString()}' filters with '${e.toString()}' filters.`);
    }(t, c), c;
}

function tu(t, n, e) {
    if (null !== t.startAt) throw new O(x.INVALID_ARGUMENT, "Invalid query. You must not call startAt() or startAfter() before calling orderBy().");
    if (null !== t.endAt) throw new O(x.INVALID_ARGUMENT, "Invalid query. You must not call endAt() or endBefore() before calling orderBy().");
    const s = new Je(n, e);
    return function(t, n) {
        if (null === ye(t)) {
            // This is the first order by. It must match any inequality.
            const e = ge(t);
            null !== e && su(t, e, n.field);
        }
    }(t, s), s;
}

/**
 * Create a Bound from a query and a document.
 *
 * Note that the Bound will always include the key of the document
 * and so only the provided document will compare equal to the returned
 * position.
 *
 * Will throw if the document does not contain all fields of the order by
 * of the query or if any of the fields in the order by are an uncommitted
 * server timestamp.
 */
/**
 * Parses the given documentIdValue into a ReferenceValue, throwing
 * appropriate errors if the value is anything other than a DocumentReference
 * or String, or if the string is malformed.
 */
function nu(t, n, e) {
    if (e instanceof fr && (e = e.va), "string" == typeof e) {
        if ("" === e) throw new O(x.INVALID_ARGUMENT, "Invalid query. When querying with FieldPath.documentId(), you must provide a valid document ID, but it was an empty string.");
        if (!pe(n) && -1 !== e.indexOf("/")) throw new O(x.INVALID_ARGUMENT, `Invalid query. When querying a collection by FieldPath.documentId(), you must provide a plain document ID, but '${e}' contains a '/' character.`);
        const s = n.path.child(M.L(e));
        if (!B.G(s)) throw new O(x.INVALID_ARGUMENT, `Invalid query. When querying a collection group by FieldPath.documentId(), the value provided must result in a valid document path, but '${s}' is not because it has an odd number of segments (${s.length}).`);
        return Wt(t, new B(s));
    }
    if (e instanceof Ur) return Wt(t, e.Xa);
    throw new O(x.INVALID_ARGUMENT, "Invalid query. When querying with FieldPath.documentId(), you must provide a valid string or a DocumentReference, but it was: " + Tr(e) + ".");
}

/**
 * Validates that the value passed into a disjunctive filter satisfies all
 * array requirements.
 */ function eu(t, n) {
    if (!Array.isArray(t) || 0 === t.length) throw new O(x.INVALID_ARGUMENT, `Invalid Query. A non-empty array is required for '${n.toString()}' filters.`);
    if (t.length > 10) throw new O(x.INVALID_ARGUMENT, `Invalid Query. '${n.toString()}' filters support a maximum of 10 elements in the value array.`);
}

function su(t, n, e) {
    if (!e.isEqual(n)) throw new O(x.INVALID_ARGUMENT, `Invalid query. You have a where filter with an inequality (<, <=, >, or >=) on field '${n.toString()}' and so you must also use '${n.toString()}' as your first orderBy(), but your first orderBy() is on field '${e.toString()}' instead.`);
}

function iu(t) {
    if (Ve(t) && 0 === t.re.length) throw new O(x.UNIMPLEMENTED, "limitToLast() queries require specifying at least one orderBy() clause");
}

class ru {
    constructor(t, n, e) {
        this.sh = t, this.firestore = n, this.th = e, this.rl = Qr(n.va), this.Ja = new Or(n);
    }
    where(t, n, e) {
        const s = io("Query.where", t), i = Zo(this.sh, "Query.where", this.rl, this.firestore.za, s, n, e);
        return new ru(function(t, n) {
            const e = t.filters.concat([ n ]);
            return new Ae(t.path, t.collectionGroup, t.re.slice(), e, t.limit, t.limitType, t.startAt, t.endAt);
        }(this.sh, i), this.firestore, this.th);
    }
    orderBy(t, n) {
        let e;
        if (void 0 === n || "asc" === n) e = "asc" /* ASCENDING */; else {
            if ("desc" !== n) throw new O(x.INVALID_ARGUMENT, `Function Query.orderBy() has unknown direction '${n}', expected 'asc' or 'desc'.`);
            e = "desc" /* DESCENDING */;
        }
        const s = io("Query.orderBy", t), i = tu(this.sh, s, e);
        return new ru(function(t, n) {
            // TODO(dimond): validate that orderBy does not list the same key twice.
            const e = t.re.concat([ n ]);
            return new Ae(t.path, t.collectionGroup, e, t.filters.slice(), t.limit, t.limitType, t.startAt, t.endAt);
        }(this.sh, i), this.firestore, this.th);
    }
    limit(t) {
        return Ir("Query.limit", t), new ru(Ce(this.sh, t, "F" /* First */), this.firestore, this.th);
    }
    limitToLast(t) {
        return Ir("Query.limitToLast", t), new ru(Ce(this.sh, t, "L" /* Last */), this.firestore, this.th);
    }
    startAt(t, ...n) {
        const e = this.ol("Query.startAt", t, n, 
        /*before=*/ !0);
        return new ru(Se(this.sh, e), this.firestore, this.th);
    }
    startAfter(t, ...n) {
        const e = this.ol("Query.startAfter", t, n, 
        /*before=*/ !1);
        return new ru(Se(this.sh, e), this.firestore, this.th);
    }
    endBefore(t, ...n) {
        const e = this.ol("Query.endBefore", t, n, 
        /*before=*/ !0);
        return new ru(De(this.sh, e), this.firestore, this.th);
    }
    endAt(t, ...n) {
        const e = this.ol("Query.endAt", t, n, 
        /*before=*/ !1);
        return new ru(De(this.sh, e), this.firestore, this.th);
    }
    isEqual(t) {
        return t instanceof ru && (this.firestore === t.firestore && Ne(this.sh, t.sh) && this.th === t.th);
    }
    withConverter(t) {
        return new ru(this.sh, this.firestore, t);
    }
    /** Helper function to create a bound from a document or fields */    ol(t, n, e, s) {
        if (n instanceof Jo) return function(t, n, e, s, i) {
            if (!s) throw new O(x.NOT_FOUND, "Can't use a DocumentSnapshot that doesn't exist for " + e + "().");
            const r = [];
            // Because people expect to continue/end a query at the exact document
            // provided, we need to use the implicit sort order rather than the explicit
            // sort order, because it's guaranteed to contain the document key. That way
            // the position becomes unambiguous and the query continues/ends exactly at
            // the provided document. Without the key (by using the explicit sort
            // orders), multiple documents could match the position, yielding duplicate
            // results.
                        for (const e of ve(t)) if (e.field.U()) r.push(Wt(n, s.key)); else {
                const t = s.field(e.field);
                if (Dt(t)) throw new O(x.INVALID_ARGUMENT, 'Invalid query. You are trying to start or end a query using a document for which the field "' + e.field + '" is an uncommitted server timestamp. (Since the value of this field is unknown, you cannot start/end a query with it.)');
                if (null === t) {
                    const t = e.field.k();
                    throw new O(x.INVALID_ARGUMENT, `Invalid query. You are trying to start or end a query using a document for which the field '${t}' (used as the orderBy) does not exist.`);
                }
                r.push(t);
            }
            return new Ge(r, i);
        }
        /**
 * Converts a list of field values to a Bound for the given query.
 */ (this.sh, this.firestore.za, t, n.va.Za, s);
        {
            const i = [ n ].concat(e);
            return function(t, n, e, s, i, r) {
                // Use explicit order by's because it has to match the query the user made
                const o = t.re;
                if (i.length > o.length) throw new O(x.INVALID_ARGUMENT, `Too many arguments provided to ${s}(). The number of arguments must be less than or equal to the number of orderBy() clauses`);
                const u = [];
                for (let r = 0; r < i.length; r++) {
                    const c = i[r];
                    if (o[r].field.U()) {
                        if ("string" != typeof c) throw new O(x.INVALID_ARGUMENT, `Invalid query. Expected a string for document ID in ${s}(), but got a ${typeof c}`);
                        if (!pe(t) && -1 !== c.indexOf("/")) throw new O(x.INVALID_ARGUMENT, `Invalid query. When querying a collection and ordering by FieldPath.documentId(), the value passed to ${s}() must be a plain document ID, but '${c}' contains a slash.`);
                        const e = t.path.child(M.L(c));
                        if (!B.G(e)) throw new O(x.INVALID_ARGUMENT, `Invalid query. When querying a collection group and ordering by FieldPath.documentId(), the value passed to ${s}() must result in a valid document path, but '${e}' is not because it contains an odd number of segments.`);
                        const i = new B(e);
                        u.push(Wt(n, i));
                    } else {
                        const t = Zr(e, s, c);
                        u.push(t);
                    }
                }
                return new Ge(u, r);
            }(this.sh, this.firestore.za, this.rl, t, i, s);
        }
    }
    onSnapshot(...t) {
        var n, e, s;
        let i = {}, r = 0;
        if ("object" != typeof t[r] || gs(t[r]) || (i = t[r], r++), gs(t[r])) {
            const i = t[r];
            t[r] = null === (n = i.next) || void 0 === n ? void 0 : n.bind(i), t[r + 1] = null === (e = i.error) || void 0 === e ? void 0 : e.bind(i), 
            t[r + 2] = null === (s = i.complete) || void 0 === s ? void 0 : s.bind(i);
        }
        const o = {
            next: n => {
                t[r] && t[r](new uu(this.firestore, new Fo(this.firestore.va, this.Ja, new qr(this.firestore.va, this.th, this.sh), n)));
            },
            error: t[r + 1],
            complete: t[r + 2]
        };
        iu(this.sh);
        return function(t, n, e, s) {
            const i = new _r(s), r = new ws(n, i, e);
            return t.Oe.Be((async () => hs(await Ao(t), r))), () => {
                i.pa(), t.Oe.Be((async () => ls(await Ao(t), r)));
            };
        }(jo(this.firestore.va), this.sh, i, o);
    }
    get(t) {
        iu(this.sh);
        const n = jo(this.firestore.va);
        return (t && "cache" === t.source ? Vo(n, this.sh) : yo(n, this.sh, t)).then((t => new uu(this.firestore, new Fo(this.firestore.va, this.Ja, new qr(this.firestore.va, this.th, this.sh), t))));
    }
}

class ou extends fr {
    constructor(t, n) {
        super(n), this.Ya = t;
    }
    get type() {
        return this.va.type;
    }
    get doc() {
        return new Xo(this.Ya, this.va.doc);
    }
    get oldIndex() {
        return this.va.oldIndex;
    }
    get newIndex() {
        return this.va.oldIndex;
    }
}

class uu extends fr {
    constructor(t, n) {
        super(n), this.Ya = t;
    }
    get query() {
        return new ru(this.va.query.sh, this.Ya, this.va.query.th);
    }
    get metadata() {
        return this.va.metadata;
    }
    get size() {
        return this.va.size;
    }
    get empty() {
        return this.va.empty;
    }
    get docs() {
        return this.va.docs.map((t => new Xo(this.Ya, t)));
    }
    docChanges(t) {
        return this.va.docChanges(t).map((t => new ou(this.Ya, t)));
    }
    forEach(t, n) {
        this.va.forEach((e => {
            t.call(n, new Xo(this.Ya, e));
        }));
    }
    isEqual(t) {
        return Oo(this.va, t.va);
    }
}

class cu extends ru {
    constructor(t, n, e) {
        if (super(Re(t), n, e), this.eh = t, t.length % 2 != 1) throw new O(x.INVALID_ARGUMENT, `Invalid collection reference. Collection references must have an odd number of segments, but ${t.k()} has ${t.length}`);
    }
    get id() {
        return this.sh.path.D();
    }
    get parent() {
        const t = this.sh.path.C();
        return t.T() ? null : zo.Zh(t, this.firestore, 
        /* converter= */ null);
    }
    get path() {
        return this.sh.path.k();
    }
    doc(t) {
        // We allow omission of 'pathString' but explicitly prohibit passing in both
        // 'undefined' and 'null'.
        0 === arguments.length && (t = v.u()), dr("CollectionReference.doc", "path", t);
        const n = M.L(t);
        return zo.Zh(this.sh.path.child(n), this.firestore, this.th);
    }
    add(t) {
        const n = this.th ? this.th.toFirestore(t) : t, e = this.doc();
        // Call set() with the converted value directly to avoid calling toFirestore() a second time.
        return zo.Ha(e.va.Xa, this.firestore, null).set(n).then((() => e));
    }
    withConverter(t) {
        return new cu(this.eh, this.firestore, t);
    }
}

function au(t, n, e) {
    const s = mr(n, Ur);
    if (s.firestore !== e.va) throw new O(x.INVALID_ARGUMENT, "Provided document reference is from a different Firestore instance.");
    return s;
}

/**
 * Converts custom model object of type T into DocumentData by applying the
 * converter if it exists.
 *
 * This function is used when converting user objects to DocumentData
 * because we want to provide the user with a more specific error message if
 * their set() or fails due to invalid data originating from a toFirestore()
 * call.
 */ function hu(t, n, e) {
    let s;
    // Cast to `any` in order to satisfy the union type constraint on
    // toFirestore().
    // eslint-disable-next-line @typescript-eslint/no-explicit-any
    return s = t ? e && (e.merge || e.mergeFields) ? t.toFirestore(n, e) : t.toFirestore(n) : n, 
    s;
}

/**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
// The objects that are a part of this API are exposed to third-parties as
// compiled javascript so we want to flag our private members with a leading
// underscore to discourage their use.
/**
 * A `FieldPath` refers to a field in a document. The path may consist of a
 * single field name (referring to a top-level field in the document), or a list
 * of field names (referring to a nested field in the document).
 */ class lu extends fr {
    /**
     * Creates a FieldPath from the provided field names. If more than one field
     * name is provided, the path will point to a nested field in a document.
     *
     * @param fieldNames A list of field names.
     */
    constructor(...t) {
        super(new Mr(...t));
    }
    static documentId() {
        /**
         * Internal Note: The backend doesn't technically support querying by
         * document ID. Instead it queries by the entire document name (full path
         * included), but in the cases we currently support documentId(), the net
         * effect is the same.
         */
        return new lu(q.q().k());
    }
    isEqual(t) {
        return t instanceof fr && (t = t.va), t instanceof Mr && this.va.nh.isEqual(t.nh);
    }
}

/**
 * @license
 * Copyright 2020 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
class _u extends fr {
    static serverTimestamp() {
        const t = new Vr("serverTimestamp");
        return t.ba = "FieldValue.serverTimestamp", new _u(t);
    }
    static delete() {
        const t = new Rr("deleteField");
        return t.ba = "FieldValue.delete", new _u(t);
    }
    static arrayUnion(...t) {
        const n = 
        /**
 * Returns a special value that can be used with {@link setDoc()} or {@link
 * updateDoc()} that tells the server to union the given elements with any array
 * value that already exists on the server. Each specified element that doesn't
 * already exist in the array will be added to the end. If the field being
 * modified is not already an array it will be overwritten with an array
 * containing exactly the specified elements.
 *
 * @param elements The elements to union into the array.
 * @return The `FieldValue` sentinel for use in a call to `setDoc()` or
 * `updateDoc()`.
 */
        function(...t) {
            // NOTE: We don't actually parse the data until it's used in set() or
            // update() since we'd need the Firestore instance to do this.
            return new yr("arrayUnion", t);
        }
        /**
 * Returns a special value that can be used with {@link setDoc()} or {@link
 * updateDoc()} that tells the server to remove the given elements from any
 * array value that already exists on the server. All instances of each element
 * specified will be removed from the array. If the field being modified is not
 * already an array it will be overwritten with an empty array.
 *
 * @param elements The elements to remove from the array.
 * @return The `FieldValue` sentinel for use in a call to `setDoc()` or
 * `updateDoc()`
 */ (...t);
        return n.ba = "FieldValue.arrayUnion", new _u(n);
    }
    static arrayRemove(...t) {
        const n = function(...t) {
            // NOTE: We don't actually parse the data until it's used in set() or
            // update() since we'd need the Firestore instance to do this.
            return new gr("arrayRemove", t);
        }
        /**
 * Returns a special value that can be used with {@link setDoc()} or {@link
 * updateDoc()} that tells the server to increment the field's current value by
 * the given value.
 *
 * If either the operand or the current field value uses floating point
 * precision, all arithmetic follows IEEE 754 semantics. If both values are
 * integers, values outside of JavaScript's safe number range
 * (`Number.MIN_SAFE_INTEGER` to `Number.MAX_SAFE_INTEGER`) are also subject to
 * precision loss. Furthermore, once processed by the Firestore backend, all
 * integer operations are capped between -2^63 and 2^63-1.
 *
 * If the current field value is not of type `number`, or if the field does not
 * yet exist, the transformation sets the field to the given value.
 *
 * @param n The value to increment by.
 * @return The `FieldValue` sentinel for use in a call to `setDoc()` or
 * `updateDoc()`
 */ (...t);
        return n.ba = "FieldValue.arrayRemove", new _u(n);
    }
    static increment(t) {
        const n = function(t) {
            return new pr("increment", t);
        }(t);
        return n.ba = "FieldValue.increment", new _u(n);
    }
    isEqual(t) {
        return this.va.isEqual(t.va);
    }
}

/**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */ const fu = {
    Firestore: Qo,
    GeoPoint: vr,
    Timestamp: k,
    Blob: Fr,
    Transaction: Ko,
    WriteBatch: Go,
    DocumentReference: zo,
    DocumentSnapshot: Jo,
    Query: ru,
    QueryDocumentSnapshot: Xo,
    QuerySnapshot: uu,
    CollectionReference: cu,
    FieldPath: lu,
    FieldValue: _u,
    setLogLevel: function(t) {
        var n;
        n = t, T.setLogLevel(n);
    },
    CACHE_SIZE_UNLIMITED: -1
};

/**
 * Configures Firestore as part of the Firebase SDK by calling registerService.
 *
 * @param firebase The FirebaseNamespace to register Firestore with
 * @param firestoreFactory A factory function that returns a new Firestore
 *    instance.
 */
/**
 * @license
 * Copyright 2020 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/**
 * Registers the memory-only Firestore build with the components framework.
 */
function du(t) {
    !function(t, n) {
        t.INTERNAL.registerComponent(new d("firestore", (t => {
            const e = t.getProvider("app").getImmediate();
            return n(e, t.getProvider("auth-internal"));
        }), "PUBLIC" /* PUBLIC */).setServiceProps(Object.assign({}, fu)));
    }(t, ((t, n) => new Qo(t, new Co(t, n), new Bo))), t.registerVersion("@firebase/firestore", "2.0.2");
}

du(t);

export { du as __PRIVATE_registerFirestore };
//# sourceMappingURL=index.memory.esm2017.js.map
