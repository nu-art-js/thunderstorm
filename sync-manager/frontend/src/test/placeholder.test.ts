/*
 * @nu-art/sync-manager-frontend - Placeholder test
 * Copyright (C) 2026 Adam van der Kruk aka TacB0sS
 * Licensed under the Apache License, Version 2.0
 *
 * NOTE: ModuleFE_SyncManager transitively imports SCSS via UI dependencies,
 * which prevents pure Mocha tests from loading. Use Playwright for integration tests.
 */

import {assert} from 'chai';

describe('sync-manager-frontend', () => {
	it('package loads', () => {
		assert.isTrue(true);
	});
});
