---
description: How to run tests using BAI in Thunderstorm projects
alwaysApply: false
---

# Running Tests with BAI

BAI supports running tests with flexible filtering options for any package in your Thunderstorm project. This guide covers all test-related commands and parameters.

## Basic Test Commands

```bash
# Run all tests in selected packages
bai -t -up=my-package -tt=pure

# Run specific test case (grep pattern)
bai -t -tc="My Test Case Name" -up=my-package -tt=pure

# Run specific test file
bai -t -tf="my-test.test.ts" -up=my-package -tt=pure

# Skip build phase (useful when only running tests)
bai -t -up=my-package -tt=pure -nb --verbose
```

## Test Parameters

### Required Parameters

- **`-t` or `--test`**: Enable test phase (required to run tests)

### Test Type Selection

- **`-tt` or `--test-type`**: Filter by test type (can specify multiple)
  - `pure`: Standard TypeScript tests using `ts-mocha` - pattern: `**/*.test.ts`
  - `firebase`: Tests with Firebase emulators - pattern: `**/*.test.firebase.ts`
  - `ui`: UI tests (not implemented yet)
  - `mobile`: Mobile tests (not implemented yet)

### Test Filtering

- **`-tc` or `--test-case`**: Run specific test cases (grep pattern matching test names)
  - Supports regex patterns
  - Multiple patterns can be combined with `|` (OR)
  - Example: `-tc="Build|Deploy"` runs tests matching either pattern

- **`-tf` or `--test-file`**: Run specific test files (glob pattern)
  - Supports glob patterns like `**/*.test.ts`
  - Can specify multiple files

### Package Selection

- **`-up` or `--use-packages`**: Select packages using regex pattern
  - Examples: `-up=build-and-install`, `-up=app-.*`, `-up=.*-frontend`
  - Only matching packages will have tests executed

### Other Useful Flags

- **`-nb` or `--no-build`**: Skip build phase (useful for test-only runs)
- **`-td` or `--test-debug`**: Debug port for test debugging (e.g., `-td=9229`)
- **`--verbose`**: Enable verbose logging for detailed test output

## Test Types Explained

### Pure Tests (`-tt=pure`)

Standard TypeScript tests that don't require external services:
- **Pattern**: `**/*.test.ts`
- **Command**: Uses `ts-mocha` with ESM support
- **Use case**: Unit tests, integration tests that don't need emulators

### Firebase Tests (`-tt=firebase`)

Tests that require Firebase emulators:
- **Pattern**: `**/*.test.firebase.ts`
- **Command**: Wrapped in `firebase emulators:exec`
- **Use case**: Tests that need Firestore, Functions, or other Firebase services

## Examples

### Run All Tests in a Package

```bash
# Run all pure tests in build-and-install package
bai -t -up=build-and-install -tt=pure --verbose -nb
```

### Run Specific Test Case

```bash
# Run a specific test case by name
bai -t -tc="Functions - Build and Push Container Image" -up=build-and-install -tt=pure -nb

# Run multiple test cases (OR pattern)
bai -t -tc="Build|Deploy" -up=my-package -tt=pure -nb
```

### Run Specific Test File

```bash
# Run all tests in a specific file
bai -t -tf="unit-build-push-image.test.ts" -up=build-and-install -tt=pure -nb

# Run multiple test files using glob pattern
bai -t -tf="**/unit-deploy*.test.ts" -up=build-and-install -tt=pure -nb
```

### Debug Tests

```bash
# Run tests with debugger on port 9229
bai -t -up=my-package -tt=pure -td=9229 -nb

# Then attach debugger in your IDE to localhost:9229
```

### Run Firebase Tests

```bash
# Run Firebase tests (requires Firebase emulators)
bai -t -up=my-package -tt=firebase -nb
```

### Run Tests for Multiple Packages

```bash
# Run tests for all packages matching pattern
bai -t -up="app-.*" -tt=pure -nb

# Run tests for all frontend packages
bai -t -up=".*-frontend" -tt=pure -nb
```

### Common Workflows

```bash
# Quick test run (skip build, verbose output)
bai -t -up=my-package -tt=pure -nb --verbose

# Run specific test case with verbose output
bai -t -tc="My Test" -up=my-package -tt=pure -nb --verbose

# Run all tests in package (with build)
bai -t -up=my-package -tt=pure --verbose
```

## Test Execution Flow

1. **Package Discovery**: BAI discovers packages matching `-up` pattern
2. **Test Phase**: For each matching package, runs test phase
3. **Command Building**: `TestsCommandComposer` builds appropriate test command based on test type
4. **Test Execution**: Tests execute with:
   - ESM support (via `NODE_OPTIONS`)
   - Proper TypeScript compilation (`ts-mocha -p src/test/tsconfig.json`)
   - Timeout disabled (`--timeout 0`)
   - Optional debug port if `-td` specified

## Troubleshooting

### Tests Not Found

If no tests are found:
- Check that test files match the pattern for the test type (`**/*.test.ts` for pure, `**/*.test.firebase.ts` for firebase)
- Verify the package matches the `-up` pattern
- Ensure test files are in `src/test/` directory

### Build Errors

If you get build errors when running tests:
- Fix build errors first, then run tests

### Firebase Emulator Issues

If Firebase tests fail:
- Ensure Firebase emulators are properly configured
- Check that `firebase.json` and `.firebaserc` are set up correctly
- Verify emulator ports are available

## See Also

- `bai -h` - View all BAI parameters
- `_thunderstorm/.cursor/rules/project-structure.mdc` - General BAI workflow documentation
